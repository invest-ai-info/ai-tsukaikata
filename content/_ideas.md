# 記事ネタ

思いついたら1行だけ足す。整形はしない。ファイル名が `_` で始まるのでビルド対象外。

## 書き方（2026-08-02 に方針変更）

**コードではなくプロンプトを主役にする。**詳しくは `SESSION_HANDOFF.md` の「記事の書き方」。

ネタを書くときは「どんな指示文が載るか」まで一言添えておくと、あとで書きやすい。

## 次に書く候補

### 🆕 2026-08-05 の作業から出た5本（材料は全部手元にある・実際に動かした結果つき）

書いた1本目＝`stop-filler-sentences`（行数を固定すると埋め草が出る）。残りは次の5本。
**どれも「実際に起きた失敗と、それを直した言葉」が揃っている**ので速く書ける。

- ~~出典にない数字~~ → **2026-08-05 に `numbers-not-in-the-source` として公開済み**
  - ⚠️ 当初「AIが $60/$270 を外挿した」と書いたが**それ自体が誤りだった**。生ページには
    `gpt-5.5-pro $30.00 - - $180.00 $60.00 - - $270.00` と実在する。
    **本当の失敗は、確認をAIに要約させたら「その行は無い」「272Kという但し書きがある」と
    嘘の確認結果が返り、それを信じて正しい数字を消したこと。**記事はこの実例に差し替えた
- **スマホから1行足すだけでAIに調べさせる**（キューファイル方式）
  - `content/_deepdive_queue.md` にURLを1行 → 定時のルーティンが拾って出典を読んで書く
- **公開前に必ず止める安全装置**
  - ファイル名の `_` 1文字で公開/非公開が決まる。自動生成を安全に試す最小の作り
- **RSSが無いサイトの更新を追う**
  - 「クラス名ではなく、ページに埋め込まれているデータから拾って。クラス名はデザイン変更で変わります」
  - ⚠️ `ai-news-auto-collect` の続編になるので、重複しないよう「クラス名で拾うな」に絞る
- **図を作らせると崩れる、を検査で止める**
  - 実例＝副題を枠の上辺が貫いてビルドが止まった。**目視では見落とす種類の崩れ**

- 無料でメールリストを作り、全ページに自動で設置する
  - 「登録フォームを全ページの同じ位置に自動で入れて」
- 静的サイトを自動でビルドして公開する（このサイト自身の作り方）
  - 「記事を書いたら自動で公開されるようにして。ただし機密が混ざっていたら止めて」
~~見積もりを先に出させる言い方（時間・費用・自分の作業量）~~
  → **2026-08-03 に `who-does-what` が吸収した。**時間は§3（手を動かす時間と待ち時間を分けさせる）、
  費用は§4（無料枠がどれくらいか・超えるとどうなるか）。残りは独立記事にするには薄いので落とす。

## 貯めておくもの

- 詰まって解決したこと（解決した直後に1行書く。あとで思い出せない）
  - **そのとき使った言い直しの文をそのまま残す。**要約すると再現できなくなる
- AIに断られた・失敗した指示と、通った指示の対
