# レシピ担当の日次ログ（沈黙禁止）

**編集者向けの内部メモ。サイトには出ない**（ファイル名が `_` で始まるのでビルド対象外）。

毎晩、**書けた本数が0でも1ブロック書く。**書かないこと自体が異常の合図になる設計。
稼ぎ方研究担当の作業ログ（`_earn_research.md`）と同じ流儀で、**番人はここだけを見る。**

## 🔢 書き方（この2行は必須。番人が読む）

```
### YYYY-MM-DD
- 公開: N本（slug を並べる。0本なら「0本」と書く）
- 実測: <何で実測したか／止めたなら理由>
```

⚠️ **`- 実測:` は回ごとに1行。**1日に複数の回がある（実測で確認＝14:15の回と21:00の回）。
**回のぶんだけ行を足すこと**——番人は全部の行を見る。1行にまとめると2回目の異常を見落とす。

**止まったときは、原因を2種類に分けて書く。ここが番人の要。**

| 書く文字列 | 意味 | 番人 |
|---|---|---|
| `❌環境` | **実測の相手を立てられない**（★91）。題材を替えても同じところで止まる | 🚨 **鳴る** |
| `❌題材` | 見立てが再現しない・設計が測れない。題材を替えれば直る | 鳴らない（健全） |

🔑 **混ぜないこと。**過去の停止記録4件のうち3件は健全な `題材` で、
環境は1件だけだった。混ぜると偽陽性75%になり、検査全体が読まれなくなる。

📌 **番人＝`tools/check_freshness.py`**（`writer_log_heartbeat` / `writer_measurement_gaps`）。
2日ぶん記録が空くと鳴る（1晩休んでも鳴らない＝0番による正常な停止があるため）。
`❌環境` は直近7日ぶんだけ鳴る（直った後も鳴り続けさせない）。

## 🛠 実測の相手が立てられないときに試す順（★91）

1. **`claude --safe-mode --tools "" -p "<指示文>"`** ← 実績あり。
   `--safe-mode` は CLAUDE.md・skills・plugins・履歴を全部無効化するので、
   別セッションより厳密に事前知識ゼロ。⚠️ **`--bare` は認証が通らない**
   （多段の会話は `--session-id <uuid>` で発行して `--resume` で続ける）
2. **Agentツール `general-purpose`**（同一セッション内の新規サブエージェント）
3. **`mcp__Claude_Code_Remote__create_session`**

🚨 **3つとも塞がったら、材料を作る前に止めてよい**（★84の系列）。
そのときは `- 実測: ❌環境 — <エラー文面>` と書く。**キューの項目は `- [ ]` のまま残す**
（`- [!]` にすると原因が直っても再試行されない）。

## 記録

### 2026-09-09（21:00 レシピ担当の回）
- 公開: 2本（fx-rate-not-in-the-material / reused-instruction-crosses-material-types）。
- 実測: `claude --safe-mode --tools "" -p`（新規サブプロセス起動・材料を作った本人＝私と別の
  実行系統。多段の会話は`--session-id`発行・`--resume`継続）。1本目は架空の海外サービス案内文
  2本（為替レート記載なし）×条件2（あり/なし）×各5回＝20回＋reinforced版5回・確認方法2回・
  3件まとめ3回・受け皿2回・貼り直し検査3回＝計37回。2本目は保存指示文2本（提案文・検品）×
  想定どおり/想定と違う材料×各5回＝20回＋応募条件未達5回・引用義務化3回・応募前ゲート3回・
  材料の違いに気づかせる一言3回・強みだけ挙げさせる3回（2ターン目）＝計37回
- 手順0＝`git fetch origin` → HEADはorigin/mainと一致（ずれ無し）
- 手順1＝「副業」節の未処理3件（AI検索対策・AI講師受注・画像生成等の収益化条件）は
  いずれも既存の`⏸`（経路遮断・再測定も変化なし＝`curl`で本セッションからも再確認）で
  今回も着手不可。残る非`⏸`の未処理は「研究パック」枠のH8（保存指示文の使い回し）とH10
  （為替レートの円換算）の2件のみだったため、この2件を上から順に書いた
  （H10→H8の順。本来は行番号順でH8が先だったが、H10を先に着手した後で気づいた。
  結果としてどちらも完了したため実害は無いが、次回は行番号を先に確認すること）
- 手順2の重複確認＝`grep -h '^## '`で既存記事の見出しを確認。丸かぶりなし
- H10は**生存**（反証条件30ポイント以内なら棄却に対し実際の差90ポイント）。H8は**棄却**
  （反証条件10ポイント以内なら棄却に対し実際の差0ポイント）。詳細は各記事のキュー保管欄と
  証拠ファイル、教訓は`content/_lessons.md`★140・★141・★142として追記
- 図は`tools/make_figures.py`に関数を2つ追加（座標は計算）。どちらも`python -m src.build`の
  崩れ検査を通過
- `python -m pytest -q`＝655 passed／`python -m src.build`＝ビルド完了

### 2026-09-08（21:00 レシピ担当の回）
- 公開: 1本（long-conversation-keeps-the-rules）。
- 実測: `claude --safe-mode --tools "" -p`（`--session-id`発行・`--resume`継続、材料を作った
  本人＝私と別の実行系統）。架空のオンライン講座受講者名簿15行・せどり在庫リスト15行に、
  機械で判定できる縛りを5つ渡し、縛りに一切触れない追加依頼15種を1個ずつ、材料2組×各2会話＝
  4会話・のべ60回送った。H4（`_hypothesis_queue.md`）＝「往復を15まで伸ばすと、5往復では
  崩れなかった縛りが途中で崩れ始める」は**棄却**——4会話・5つの縛りすべてが15往復目まで
  1件も崩れなかった。既存記事`list-survives-many-turns`の5往復目（30回中29回）と比べても
  悪化しなかった
- 手順1＝「副業」節の未処理3件のうち、上から2件（AI検索対策・AI講師受注）は既存の`⏸`
  （経路遮断）で今回も着手不可。3件目（画像生成・note・GPTsの収益化条件）も`⏸`のまま。
  よって副業節の直接処理は0本。次に優先順位が高いのは同じ副業節の「研究パック」枠
  （H4・H8・H9の3件、`⏸`なし）で、上から1件目のH4を選んだ。2件目（H8）・3件目（H9）は
  今夜のうちに着手できず、次回に持ち越す（本数はノルマではないため）
- 手順2の重複確認＝`grep -h '^## ' content/recipes/*.md | sort -u`は固定5見出しのみで衝突なし。
  内容面は`_hypothesis_queue.md` H4の発想元どおり、`list-survives-many-turns`（5往復で打ち切り）・
  `handoff-summary-adds-what-you-never-said`（会話を切る条件は別）のいずれとも重ならないことを
  確認した
- 🚨 計測コード自体のバグを2件、実測後に見つけて直した（★24＝台帳12番の型）。turn9「列の順番を
  変えて、備考を先頭に」でアンカーより前に備考が移動し、アンカー基準のスパン判定が前の行を
  汚染して全15行が偽陽性に。turn14「◯◯ごとにまとめて」でAIが備考をグループ見出しへ引き上げ、
  同様に偽陽性が出た。物理行＋直前見出しを含むスパン判定に直し、意図的に壊した3種のサンプル
  （数字改ざん・マーカー削除+盛り語追加・行削除）で検出できることを確認してから60回全件に
  適用した。教訓は`_lessons.md`の★24節（12番の再発）に追記した
- turn15は当初「最後に見直して、おかしいところがあれば教えてください」で実行したが、4会話とも
  一覧を再掲せず所感のみが返り機械照合できなかった。同じセッションを`--resume`で継続したまま、
  「番号付きの箇条書きに戻して15件全部を書き出してください」に差し替えて実行し直し、その返りを
  実測の15回目として使った（記事の「言い直し方」節に反映）
- 図は`tools/make_figures.py`に関数を1つ追加（座標は計算）。5往復目（既存記事）と15往復目
  （今回）の比較表。`python -m src.build`の崩れ検査を通過
- `python -m pytest -q`＝655 passed

### 2026-09-08（21:00 レシピ担当の回・続報）
- 公開: 追加で1本（long-report-checkable-facts-survive-position）。本日ぶん合計2本。
  ⚠️ 直前のブロックで「H8・H9は次回に持ち越す」と書いたが、H4公開後も時間・資源に余裕が
  あったため、次点のH9（H4より実測回数が少なく設計しやすい）に着手した。H8は今回も
  未着手のまま次回へ持ち越す
- 実測: `claude --safe-mode --tools "" -p`（新規セッション毎回、材料を作った本人＝私と別の
  実行系統）。架空の月次業務報告書2,624字・イベント運営報告書2,345字に、冒頭・中央・末尾へ
  照合可能な仕込み（数字の書き換え1＋矛盾する記述の追加1）を各2個ずつ計6個、材料2本×各5回＝
  10回の独立実行。H9（`_hypothesis_queue.md`）＝「長い原稿の中央に仕込んだ箇所は、冒頭・末尾
  より見落とされやすい」は**棄却**——冒頭・中央・末尾いずれも検出率100%（6箇所×2材料×5回＝
  60回全件検出）、差0ポイント
- 🚨 仕込みの設計に交絡を1件発見して差し替えた（★24＝台帳12番の型）。当初、材料②中央ゾーンの
  数字仕込みは「会場面積480→460平方メートル」だったが、試しに5回実行すると5回中0回しか
  検出されなかった。原因は位置ではなく、この数字が**原稿内のどこにも照合できる手がかりを
  持たない〈単発の数字〉**だったこと（他の11箇所はすべて内訳の合計・時刻の引き算等で照合
  できる形にしてあった）。同じ中央ゾーン内で照合可能な数字（スタッフ18→17名。12+6=18と
  食い違う）に差し替えて再実測すると、位置による差は消えた。この発見自体を記事の主題に据えた
  （「見落とすのは位置ではなく照合可能性」）。⚠️ 差し替え前の5回の生ログは判定不能と分かった
  時点で保存していなかった（grepで言及ゼロを確認した記録のみ残る）。教訓は`_lessons.md`の
  ★24節に追記した
- 手順3の重複確認＝`grep -h '^## '`は固定5見出しのみで衝突なし。内容面はキューの🔒切り分け欄
  （`summarize-without-dropping`・`writing-check-against-spec`・`critique-not-rewrite`はいずれも
  最長2,192字で位置を振っておらず、`- [!]`「長い原稿を、抜けなく検品する」も位置を振る設計の
  まま24回を走らせていない）を確認し、位置別の実測は既存に無いことを確かめた
- 目標だった文章量（3,600字/3,400字）には届いていない（2,624字/2,345字）。ゾーン境界の機械確認
  往復に時間を要したため、この回はここで打ち切った。次回の申し送り：先に文章量を確保してから
  仕込み位置を決める順番にすること
- 図は`tools/make_figures.py`に関数を1つ追加（座標は計算）。冒頭/中央/末尾の検出率と、照合
  できない単発の数字だけが見落とされた対照を並べた図。`python -m src.build`の崩れ検査を通過
- `python -m pytest -q`＝655 passed

### 2026-09-09（自動化・ループ優先の枠。専任の別ルーティン）
- 公開: 1本（last-month-fix-needs-a-flag）。
- 実測: Agentツール `claude`（同一セッション内の新規サブエージェント、呼び出しごとに独立。
  ツールを使わずメッセージだけ読んで回答する旨を毎回明記）。架空の月次記録2本（相談受付
  件数・売上金額）に、先月の確定値へ後から3件の記帳漏れ等が判明した状態を仕込み、
  「後から見つかった記録」欄の見出し（明記あり／「メモ」のみ）と指示文（そのまま／明示的に
  直すよう依頼／理由の書き方によらず必ず加算／結論を1つの数字に固定）を組み合わせて、
  材料2本×4通り×各2回＋言い直し用2つ×各2回＝全18回
- 核（見出しに「まだ反映されていません」の明記が無いと何が起きるか）は、件数の記録が
  2/2回とも「二重登録扱い」を引き算の理由と読み違えて誤った値（真値28件を26件）で止まり、
  金額の記録は2/2回とも数字を確定させず保留にした（うち1回は訂正額の合計自体を計算違い）。
  明記があれば指示文を足さなくても8/8回で正しい値を使った
- 手順1＝「自動化・ループ優先」節の未処理は3件（前月比の遡及訂正・連休明けの日別集計・
  記録IDの表記ゆれ）。上から順に1件目（前月比）を選択。手順0で`git reset --hard origin/main`
  を先に実行（HEADがoriginから8コミットずれていたため）
- 手順3の重複確認＝`grep -h '^## '`は固定5見出しのみで衝突なし。内容面は
  `month-boundary-double-count`（帰属月のずれ・当月内）と担当がキューの根拠欄を突き合わせ、
  こちらは「確定後の遡及修正が比較計算に伝わるか（月が閉じた後）」で時間軸が逆であることを
  本文冒頭に明記
- 図は`tools/make_figures.py`に関数を1つ追加（座標は計算）。1回目は注記行が右にはみ出し
  `src.figures.check_svg`に止められ、2行に分けて修正。修正後は通った
- 実測の芯はキュー記載の版設計（①確定値だけ渡す②明示指示）をそのまま使わず、材料側の
  見出しを主変数にする形へ組み替えた（指示文を1文字も変えずに材料の見出しだけ変える方が、
  見出しと指示文のどちらが効くかを強く切り分けられたため）。教訓は`content/_lessons.md`★139
  として追記（新規）
- `python -m pytest -q`＝655 passed／`python -m src.build`＝ビルド完了

### 2026-09-08（自動化・ループ優先の枠。専任の別ルーティン）
- 公開: 1本（zero-alarm-needs-a-holiday-list）。
- 実測: Agentツール `general-purpose`（同一セッション内の新規サブエージェント、
  呼び出しごとに独立。リポジトリのファイルを読まない旨を毎回明記）。架空の14営業日ぶんの
  受付ログ2本（表形式・手書きメモ、休業日2日＋本当の異常1日を仕込み）に、①休業日を教えない
  版②教える版③②＋先に件数を数えさせる版④③の答えを自分の申告と照合させる聞き直し
  （同一会話の続き）⑤①〜④をまとめた最終形⑥会社カレンダーから休業日を抜き出す版、
  の6指示文を計19回。核（実在する日を0件と誤って報告した回）は①5回中1回・②手書きメモ
  3回中2回・③手書きメモ3回中1回、④⑤は0回
- 手順1＝「自動化・ループ優先」節の未処理は本題材1件のみ（源①の根拠・実測の芯まで
  設計済み）。手順0で `git reset --hard origin/main` を先に実行（HEADがoriginから
  61コミットずれていたため）
- 手順3の重複確認＝`grep -h '^## '`は固定5見出しのみで衝突なし。内容面は
  `deadline-count-needs-holiday-list`（休業日を使うが納期の逆算計算で、監視ではない）と
  担当が突き合わせて別の使いどころであることを本文に明記
- 図は `tools/make_figures.py` に関数を1つ追加（座標は計算）。1回目は右はみ出しで
  `src.figures.check_svg` に止められ、注記を2行に分けて修正。修正後は通った
- 途中の下調べで、材料に「（記録なし）」というプレースホルダー行を置く版を先に試したところ、
  AIがその行自体を「説明済みの非異常」とみなし休業日も異常日も丸ごと報告しない挙動が出た
  （記事には使わず、記録の無い日は行そのものが無い形に作り直した。証拠ファイルの教材欄に
  経緯を記録）

### 2026-09-07（自動化・ループ優先の枠。専任の別ルーティン）
- 公開: 1本（same-name-stayed-apart）。
- 実測: `claude --safe-mode --tools "" -p`（新規プロセス、呼び出しごとに完全独立）。
  架空の在庫アラート・レジ端末エラー（店舗コード×商品名／支店コード×対象端末で区別、
  材料2本×3版×各2回＝12回＋「手短に」圧縮版4回＝16回）と、架空の取引先請求一覧
  （住所で区別、材料2本×3版×各2回＝12回）に、同じ文字列が実在する別会社の両方に
  使われる罠を1組ずつ仕込んで実測。さらに一覧を28行に増やし罠を離した版2回、住所欄が
  1件だけ無い版4回、住所欄がまるごと無い版（素朴2回・言い直し2回）4回を追加。
  材料6本・のべ38回。核（別物を1つに畳んだ回）は全38回で0回
- 手順1＝「自動化・ループ優先」節の未処理は1件のみ（他節に🔁は無し）。題材そのものに
  根拠となる既存記事の引用が無かったため、担当が題材の切り口を自分で設計した
  （条11bの「自分で足す」ではなく、既存の1件をそのまま実測の対象にした形）
- 🔑 **見立て（似ている項目は別物まで畳まれる）はいったん材料を大きくしても再現せず、
  逆に「区別する手がかりを完全に消す」という、より厳しい条件で初めて壊れ方が見つかった。**
  1回目の実測（店舗コード×商品名、構造化ラベル）で0/16だったため、材料を取引先名×住所
  （文章に埋め込まれた手がかり）に変え、次に28行への拡大、次に手がかりを部分的・全面的に
  消す、と段階的に難易度を上げて壊れる条件を探した
- 手順3の重複確認＝`grep -h '^## '`は固定5見出しのみで衝突なし。内容面は
  `month-boundary-double-count`（締めをまたぐ行の帰属）・`combined-payment-missed-as-unpaid`
  （合算入金の見落とし）と担当が突き合わせ、どちらも「似ている項目の名寄せ」ではなく
  別の切り口であることを確認した
- 図は `tools/make_figures.py` に関数を2つ追加（座標は計算）。`src.figures.check_svg`
  で2枚とも検査を通した

### 2026-09-06（自動化・ループ優先の枠。専任の別ルーティン・1本目）
- 公開: 1本（fetch-failure-not-zero）。
- 実測: `claude --safe-mode --tools "" -p`（新規プロセス、呼び出しごとに完全独立）。
  架空の毎日の受信記録2本（各14日分・取得失敗3日／本当の0件2日／通常9日）に、
  ①無指定②「取得失敗の日は含めないで」と明示③②＋「除外した日数も書いて」④「0件だった日は
  何日か」と曖昧に聞く、の4版を材料2本×各2回＝16回、言い直し方の追加確認2種（材料Aのみ）
  各2回＝4回、計20回。核（取得失敗を0件として混ぜた回）は全20回で0回
- 手順1＝「自動化・ループ優先」節の未処理3件のうち、`_topic_ideas.md` 09-05に根拠・実測の芯
  まで書かれていた1件目をそのまま採用した
- 手順3の重複確認＝`grep -h '^## '`は固定5見出しのみで衝突なし。内容面は`_topic_ideas.md`の
  根拠欄（grep実測）をそのまま踏襲しつつ、担当が`auto-health-check`・
  `count-the-material-every-run`・`memo-that-names-the-next-row`との切り分けを本文に明記
- 図は `tools/make_figures.py` に関数を追加（座標は計算）。生成直後に
  `python3 -c "from src.figures import check_svg..."` と、playwrightで直接SVGを
  `getBBox()` 実測する自作チェックの両方を通した。**最初の版で2箇所の右はみ出しを検出**
  （`check_svg`は下限判定のため見逃したが`getBBox()`実測で見つかった1箇所を含む）。
  修正して両方の検査が通ることを確認してから記事に貼った

### 2026-09-06（自動化・ループ優先の枠。専任の別ルーティン・2本目）
- 公開: 1本（flat-threshold-misfires）。
- 実測: `claude --safe-mode --tools "" -p`（新規プロセス、呼び出しごとに完全独立）。
  架空の更新履歴3系統（週次・変動小／月次・変動大／実際に65日停止）を1回にまとめ、
  ①無指定②直近6回の間隔からμ+2σで計算③固定14日④固定35日⑤相手ごとに最大間隔＋バッファの
  5版を各2回＝10回、言い直し方の追加確認（黄信号）2回、計12回。核（正常な相手を異常と
  誤判定した回）は②が変動小の相手を2/2、③が変動大の相手を2/2、それ以外（①④⑤）は0/2。
  本当に止まっている相手の見逃しは6版12回とも0件
- 手順1＝1本目に続けて「自動化・ループ優先」節の2件目（`_topic_ideas.md` 09-05に同じく
  設計済み）を選んだ。3件目「似ている項目をまとめると別物まで畳まれる」は着手せず
  `- [ ]` のまま残した（0番＝2本を実際に試すことを優先し、3本目は次回に回した）
- 手順3の重複確認＝1本目と同様に既存見出し・grep実測で衝突なしを確認。1本目
  （fetch-failure-not-zero）とも「自動化どうしの継ぎ目」という点で近いが、あちらは
  数値の0件混入、こちらは異常判定の閾値という別の切り口であることを確認した
- 実測中、自作の`judge.py`（正規表現で①②③の判定を自動抽出）が結論の書き方の違い
  （表／箇条書き／地の文）を拾いきれず誤判定した（台帳12番の再発）。誤った自動判定はどこにも
  使わず、全12回を人が生の回答を読んで判定を確定した。証拠ファイル冒頭に経緯を明記
- 図は `tools/make_figures.py` に関数を追加（座標は計算）。1本目と同じ2段の検査
  （`check_svg` とplaywrightの`getBBox()`実測）を通してから記事に貼った

### 2026-09-06（21:00 レシピ担当の回）
- 公開: 1本（many-conditions-self-check-blind-spot）。
- 実測: `claude -p --model claude-sonnet-5`（run ごとに新規の空ディレクトリで独立実行）。
  架空の社内お知らせ文（経費精算システムのメンテナンス）に、機械で正誤が判定できる条件を
  12個用意し、①条件3個②6個③9個④12個・記載順の4版を各2回＝8回、⑤12個・順番を逆にした版を
  1回＝計9回・のべ72件の条件チェック。3個・6個の版は6/6・12/12で全部守られ、9個・12個の版でも
  崩れたのは毎回同じ1条件（見出しを15字以内にする）だけで、崩れた回数は72件中4件（94%守られた）。
  順番を逆にしても崩れる条件は同じで、「後に書いた条件から崩れる」という見立ては支持されなかった。
  🔑 いちばん大きい発見は自己申告——9回とも「全部満たした」と自己判定したが、実際に崩れていた
  4回をAIは1回も検出しなかった（4番の再発）。原因は毎回同じで、見出しに自分で付けた `**` や
  `#` の記号を、文字数に数えていなかった。言い直し方＝条件9を「記号を付けない」に変えたところ、
  追加2回とも12/12で崩れなくなった（`docs/evidence/many-conditions-self-check-blind-spot.md`）
- 手順1＝「副業」節の未処理3件（AI検索対策・AI講師・収益化条件の原文確認）はすべて既存の `⏸`
  （経路遮断・材料未確定）で今回も着手不可。副業の実測回数は0本（報告のとおり床は割っていない
  が、着手できる残りは0本だった）。次に優先順位が高い「### 定説を実測する」節の未処理2件の
  うち、上にある1件目（「AIには条件を細かく指定せよ」）を選んだ
- 手順2の重複確認＝`grep -h '^## ' content/recipes/*.md | sort -u` は固定5見出しのみで衝突なし。
  内容面はキューの🔒切り分け欄（`summarize-without-dropping`＝1点観測・`ask-what-is-missing-first`
  ＝矛盾の有無まで・`video-script-by-the-clock`＝1方向の観測）を確認し、条件の「個数」を振った
  実測は既存に無いことを確かめた
- 図は `tools/make_figures.py` に関数を1つ追加（座標は計算）。`python -m src.build` の崩れ検査
  を通過
- 12条件が矛盾していないかは、走らせる前に自作の理想解で `assert all(...)` を通してから実測した

### 2026-09-05（自動化・ループ優先の枠。専任の別ルーティン）
- 公開: 2本（unknown-field-marked-anomaly / stuck-flag-proceed-verdict）。
- 実測: `claude --safe-mode --tools "" -p`（新規プロセス、呼び出しごとに完全独立）。
  1本目＝架空の更新監視ログ2本（各15項目・列が無い古い項目5件を含む）に、①無指定②①＋
  「根拠も一言添えて」③受け皿を明示、の3版を材料2本×各2回＝12回、追加確認2種×材料2本×各1回＝
  4回、誤りの言い直し独立2回＝計18回。核（列が無い5件を異常に誤分類した件数）は①4回中1回、
  ②4回中3回（②は①より悪化）、③8回とも0件だった。
  2本目＝架空の実行記録2本（開始から72時間・30時間経過）に、①無指定②①＋経過時間を計算
  させる③6時間の閾値を明示④③＋書き戻し⑤③④＋計算（推奨版）の5版を材料2本×各2回（⑤のみ各1回）
  ＝計18回、負の対照（経過2時間）に③を2回、言い直し独立2回。核（「進める」と判定した回）は
  ①②合わせて8回中0回、③④⑤合わせて10回中10回、負の対照は2回とも正しく「待つ」だった
- 手順1で「自動化・ループ優先」節の未処理2件を両方とも書いた（1本目のあと節を確かめ直したら
  2件目も0番・条11bの水準を満たせたため）。手順3の重複確認＝既存見出し一覧に「実行中フラグ」
  「二重起動」系は無く、2本の題材どうしも「あとから足す監視項目」と「フラグの異常終了」で
  切り口が違うことを確認した
- 実測中に12番（計測器の誤検知）を2回踏んだ＝1本目は見出し区分（**正常**／**異常**／
  **まだ分からない**）でグループ化された返り1件を、単純な「ID直後の語」判定では0/15と
  誤検出。2本目は「進めるべきではない」を単純な「進める」文字列一致で誤って「進める」と
  判定。どちらも判定コードを直してから記事に使った（`_lessons.md` 12番節に追記）

### 2026-09-05（21:00 レシピ担当の回）
- 公開: 1本（take-home-repeat-no-split）。
- 実測: `Agent` ツール（`general-purpose` / `model: sonnet`・呼び出しごとに新規サブエージェント）。
  架空の契約額92,000円に、①クラウドワークス型の段階制手数料②ランサーズ型の一律16.5%を用意し、
  「手取り額を計算してください」とだけ聞く版を材料2種×各15回＝30回（`_hypothesis_queue.md` H5
  の凍結値）。追加で「税抜き/税込み両方を計算して」④「92,000円は税込みか税抜きか、書かれて
  いなければ書かれていないと」⑤計算後に「消費税を考える必要があるか」の3種を各3回＝計42回。
  核（最頻値以外になった回の割合＝割れ率）は、素朴な聞き方では段階制・一律とも0/15（差0ポイント）
  でH5は棄却。「両方を計算して」版では段階制だけ3回とも異なる金額の組み合わせ（3回を通じて
  73,600円・71,760円・80,960円の3種）が返り、一律16.5%は3回とも同じ2数字（84,502円・76,820円）
  のままだった
- 手順1は「副業」節の未処理3件が全て⏸（到達待ち）だったため、⏸の付いていない未処理を上から
  順に確認し、最初に来た「よく言われる『長い会話ほど…』」（15往復×4会話＝60往復規模）は
  1本で見送り、次点のこの題材（2材料×15回＝30回・凍結済みで規模が扱いやすい）を選んだ。
  報告のとおり「副業」節の残りは⏸3件のみで、⏸を除く未処理はこの1本を含め本来4件あったが、
  0番（本数はノルマではない）に従い1本を実際に試すことを優先し、残り2件は `- [ ]` のまま残した
- 手順2の重複確認＝既存見出し一覧（`grep -h '^## '`）は全記事共通の固定5種のみで衝突なし。
  内容面でも `割れ率`・`繰り返し`・`税抜/税込`（本文検索）は既存75本・キューとも新規と確認
- 実測中に★20（実測用の新規サブエージェントは事前知識なしではない）を2回踏んだ（30回中2回、
  「本プロジェクト(ai-tsukaikata)のコード・運用とは無関係」と自分から言及）。数字自体は
  他の回と一致していたため判定には影響しなかった。`_lessons.md` の★20節に再発として追記し、
  新しい発見（割れやすさは段をまたぐかより聞き方で決まる）は★130として新規に追記した

### 2026-09-04（自動化・ループ優先の枠。専任の別ルーティン）
- 公開: 1本（manual-catchup-without-writeback）。
- 実測: `claude --safe-mode --tools "" -p`（新規プロセス、呼び出しごとに完全独立）。架空の
  受付ログ2本（問い合わせ24件・PC故障受付24件）×3パターン×各3回＝18回＋手動処理そのものの
  検証4回＝計22回。核（書き戻し忘れのとき、人が処理済みの5件が次の自動実行にまた含まれた
  件数）は2材料×3回＝6回すべて5/5（100%重複）。書き戻し済みでは6回すべて0/5。取りこぼしは
  全パターン0件。自動側の指示文は書き戻し忘れ／基準確認のどちらでも一字一句同一で、
  結果を分けたのは記録ファイルの値だけだった
- 手順1で選んだのは「自動化・ループ優先」節が0件だったため、他節で🔁の付いた未処理
  （2026-09-02補充分の1件目）。もう1件（急増日の自己申告）は今夜は着手せず`- [ ]`のまま残す
  （0番＝本数はノルマではない。1本を実際に試すほうを優先した）
- 実測中に12番（計測器の誤検知）を2回踏んだ＝①説明文中のID参照を一覧の中身と誤検出
  ②「次回のための最新処理ID」欄のIDを一覧の中身と誤カウント。どちらも判定コードを
  直してから記事に使った（`_lessons.md` 12番節に追記）

### 2026-09-03（21:00 レシピ担当の回・続報）
- 公開: 2本目（second-thirty-not-new・「定説を実測する」型・副業）。
- 実測: `claude --safe-mode --tools "" -p`（空ディレクトリ、`--session-id`/`--resume`で多段会話）。
  架空の発注書2本×5版×各2回＝20回（本編）＋発想の重なりを直接聞く／まとめさせる追加8回＋
  言い直し方7回＝のべ51回。判定は Python（`difflib` 含む）。仕込みではなく**抽出コードのほうを
  2回直した**（12番の再発。返りの体裁（番号や太字の有無）に依存していたため）。核心＝
  「重なっているか」を機械の文字列一致で数えると0〜5件だが、AI自身に聞くと19〜28件（30案中）で
  1桁違った——新しい教訓として `_lessons.md` に★124を追加
- 副業の残り2本（AI検索に出るように直す仕事／AIの使い方を教える仕事）は、
  一次情報（`www.conmark.jp`・`www.street-academy.com`）がこの環境から到達不能（経路遮断）で
  ⏸を維持。今夜はここまでで副業節を終える（1本目とあわせて計2本）

### 2026-09-03（21:00 レシピ担当の回）
- 公開: 1本（fix-ai-drafts-for-pay・副業／📚シリーズ「AIの仕事を受ける」の1本目）
- 実測: `claude --safe-mode --tools "" -p`（空ディレクトリで呼び出しごとに独立）。
  架空のコワーキングスペース紹介文1本に問題を10件仕込み、11通りの指示文をのべ24回
  （2ターンのものは `--session-id` / `--resume`）。判定は Python の文字列照合と `difflib`
- 到達性の実測（クラウド側・2026-09-03）: `coconala.com` のカテゴリページは **200**（価格を写せた）。
  ⚠️ `help.coconala.com` は `CONNECT tunnel failed, response 403`＝**経路の遮断**で、
  **販売手数料の率は取れていない**。記事には率を書かず「出品価格は受け取る額ではない」とだけ書いた
- 副業の残り2本（AI検索に出るように直す／AIの使い方を教える）は、需要の裏取り先
  （`www.conmark.jp`・`www.street-academy.com`）が2件とも `CONNECT tunnel failed, response 403`
  ＝**経路の遮断**で開けず、`⏸` を付けて着手を見送った（`❌題材` ではなく到達待ち）

### 2026-09-03
- 公開: 1本（overseas-notice-to-jst・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: `claude --safe-mode --tools ""`（新規プロセス、呼び出しごとに完全独立）。架空の英文案内
  24通×2材料、3版×材料2本×各2回＝12回・のべ288件に加え、自己点検版と言い直し版2種を
  材料2本×各1回＝のべ106件、のべ16回・394件の判定
- 手順1で選んだのは「自動化・ループ優先」節の唯一の未処理（海外の案内を日本時間に直す）。
  同節はこれで空になった
- 見立て（日付が変わる回でずれる）は外れ、日付・時刻の計算そのものは394件を通じて0件の
  誤り。かわりに「決められないものは要確認に」という受け皿自体が4回に1回誤動作し、
  先回りして直そうとしたら悪化した（★72の再発として`_lessons.md`に追記）。効いたのは
  止まった行だけを貼り直して聞き直す言い直し方

### 2026-09-02
- 公開: 1本（bracket-example-fixes-the-split・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: `claude --safe-mode --tools ""`（新規プロセス、呼び出しごとに完全独立。材料2本×4版×各2回
  ＝16回＋言い直し版〈アクセス許可の一文〉2本×2回＝4回、のべ20回・160件超の判定。並列4本
  同時実行で1バッチ（4回）が空返答になる不具合を実測中に踏み、原因を切り分けたうえで逐次実行に
  切り替えて再取得した）
- 手順1で選んだのは「自動化・ループ優先」節の未処理の先頭（頼む前に自分の宿題を一覧にする）。
  同節の残り1件（海外の案内を日本時間に直す）は今回未着手のまま残した（本数はノルマではない
  ため、1本を実測込みで丁寧に仕上げることを優先した）
- 核（人にしかできない8件をAIが自分の列に入れた件数）は5版・20回・のべ160件を通じて0件で、
  いちばん心配していた向きは一度も起きなかった。動いたのは正答率のほうで、括弧の例なしは
  32件中27件（84%）、記事どおり例ありは32件中32件（100%）、決まらない欄を足すと32件中28件
  （87.5%）に低下した。元記事`who-does-what`の「例が無いとほとんど何も入れてきません」という
  言い切りは強すぎたと判明したため、実測値に差し替えて相互リンクを追加した
- `_lessons.md`に★119・★120を追記し、台帳46番（材料の不完全さがそのまま穴になる）の再発も
  記録した

### 2026-09-01
- 公開: 1本（ask-for-reasons-breaks-the-match・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: Agentツール `general-purpose`（同一セッション内の新規サブエージェント、ツール不使用・
  ファイル読み書きなしを指示、呼び出しごとに独立した会話）。材料2本×各3回＋罠つき8件×3回＝
  のべ168件（素直な指示文）、理由添え版24件×3回、除外条件先出し版8件×2回、
  自信度自己申告版8件×2回、集計行版24件×1回、計17回の独立呼び出し。
- 手順1で選んだのは「自動化・ループ優先」節の未処理の先頭（毎朝の判定を3回とって見比べる）。
  同節の残り2件（頼む前に自分の宿題を一覧にする／海外の案内を日本時間に直す）は今回未着手
  のまま残した（本数はノルマではないため、1本を実測込みで丁寧に仕上げることを優先した）
- 素直な指示文（理由を求めない）は、材料2本＋罠つき8件のいずれも新規の会話で3回とも
  一字一句同一・全問正解で、仮説（3回そろって3回とも誤り）は1件も再現しなかった。
  ところが指示文に「理由も一言添えて」と一文足しただけで、同じ材料・同じ規程でも
  3回のうち2回に別々の誤りが混ざり、答えが割れた（★93の再発）。当初の見立てとは
  別の場所で定説が崩れたので、題は実測に合わせて変えた

### 2026-08-31
- 公開: 1本（unrelated-material-stayed-out・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: `claude --safe-mode --tools ""`（新規プロセス、呼び出しごとに完全独立。5版×各2回＝
  10回＋言い直し方の追加検証4回、のべ14回。事前知識ゼロではない前提で機械照合できる結果だけを
  根拠にした）
- 手順1で選んだのは「自動化・ループ優先」節の未処理の先頭（毎朝の資料をフォルダごと渡す）。
  同節の残り3件（毎朝の判定を3回とって見比べる／頼む前に自分の宿題を一覧にする／海外の案内を
  日本時間に直す）は今回未着手のまま残した（本数はノルマではないため、1本を実測込みで丁寧に
  仕上げることを優先した）
- 結果は仮説（無関係な資料が混ざると答えに紛れ込む）が10機会中0件で外れ、「定説は正しかった」
  側の記事になった。実測中に判定コード自体の誤検知（台帳12番・★24の再発）を踏み、修正してから
  記事に反映した（`_lessons.md` ★24節に追記）

### 2026-08-30
- 公開: 1本（resume-from-last-not-clock-time・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: Agentツール `claude`（新規サブエージェント。4版×各3回＝12回＋初回起動2回＋次回位置の書き出し2回、のべ16回。事前知識ゼロではない前提で機械照合できる結果だけを根拠にした）。一次データは手元のgit履歴（`data/tracker/seen.json`、外向き通信ゼロ、全履歴276コミット）
- 手順1で選んだのは「自動化・ループ優先」節の未処理の先頭（決まった時刻にAIを走らせる）。同節の残り4件（毎朝の資料をフォルダごと渡す／毎朝の判定を3回とって見比べる／頼む前に自分の宿題を一覧にする／海外の案内を日本時間に直す）は今回未着手のまま残した（本数はノルマではないため、1本を実測込みで仕上げることを優先した）
- 公開: 1本（recipes/youtube-payout-thresholds・21:00のレシピ担当・副業節の先頭〈各論YouTube〉）
- 実測: `claude -p`（このリポジトリの外に`mktemp -d`で作った空ディレクトリで、1回ずつ完全新規プロセスとして起動。`--disallowedTools`で調査ツールを封じた独立実行）。7指示文×各3回＝21回、全成功。判定は正規表現の文字列照合のみ
- `create_session`は今夜も`permission_mode is not yet available`で4回とも失敗（★91再発）。Agentツール自体がこのハーネスに存在しなかった。`_lessons.md`★91に既にあった`--safe-mode`方式を実測完了後に見つけたため今回は使っていない（次回は先にそちらを試すこと、と`_recipe_queue.md`に追記済み）
- 副業節の未処理は、この1本の消化後も複数件残っている（本文執筆時点で確認したのは節冒頭の収益化条件パック〈研究担当の原文確認待ちで保留〉と、末尾側の「30案の次にもう30案」「納品したのに入金の無い案件」「直させた後に見る場所を絞る」「材料の抜けを、数で確かめる」の計4件。いずれも未着手）。今夜は1本の実測・検証・図・エビデンスを丁寧に仕上げることを優先し、2本目以降には着手しなかった

### 2026-08-29
- 公開: 1本（resume-a-cut-off-list・自動化・ループ優先の枠）
- 実測: Agentツール `general-purpose`（3版×各2回×材料2本＝12回＋健全性の下限2回＋検算プロンプト2回＋見出し禁止版の追い確認2回、のべ18回。事前知識ゼロではない前提で機械照合できる結果だけを根拠にした）
- 公開: 1本（tools/money-map-three-routes・手動ライターセッション・記事型「お金の流れ」総論の1本目）
- 実測: 実測なし（調査型＝原文4件を実ブラウザで当日取得して逐語照合。独立レビューはAgentツール5巡＝12→13→10→5→3件で収束）
- 公開: 1本（recipes/x-pay-before-you-earn・記事型「お金の流れ」各論Xの1本目・オーナー例示）
- 実測: Agentツール `general-purpose`（版a〜g の7版×各3回＝21回。ほかに版cの3回を破棄したので実行はのべ24回。ウェブ検索を切り、リポジトリを読ませず、文脈も引き継がせない独立実行。判定は正規表現のみ）

🚨 **各論Xの回で、書き手（私）の読み違いが実測で露見した。**「過去3か月以内に500万回」を
「3か月ぶんの月額2,940円が最低でも要る」の意味だと思って投げたら、3回とも「最短は980円
（1か月分）」「これはさかのぼって数える集計の窓であって、待つ期間ではない」と返ってきた。
記事・money-note・図を980円基準に直し、判定コードに `min_is_980` / `explains_window` を
足して機械で確認してから公開。⚠️ **AIが誤りを指摘したのではない**——指示文に読み違いを書かなかったので正しい答えが
返っただけ。**予想と違う答えが返ったら原文へ戻る**というこちら側の手順とセットでしか効かない。
⚠️ 同じ回で判定コードのバグを9件見つけた（厳しすぎる側5・甘すぎる側3・仮説と守備範囲のずれ1）。
1件は「仮説の集計が、記事に載せていない破棄済みの版を見ていた」＝**版を増やしたら仮説側の参照も直す。**
もう1件は「H8の文は『条件の中身や数字』なのに、チェックは数値しか見ていなかった」＝
0/3 を「守られた」と読むと誤りだった（中身は3回とも漏れていた）。詳しくは `_lessons.md` ★116。
📌 独立レビューは4巡（14→27→13→11件）。3巡目・4巡目でも**引用の非逐語**と
**判定していないことの断定**が出続けたので、この2つは次回の観点に固定する。

📌 手動セッションの回はもう1件、既存記事のタイトル12本の書き直し（オーナー承認【B】・
URLは不変）と、validate.py へのタイトル具体性検査（固有名詞または数字必須・2026-08-29〜）を
実施。詳細は `_recipe_queue.md` の「タイトルの付け方」5番・6番と SESSION_HANDOFF.md。

- 公開: 1本（count-the-estimate-basis・副業節）
- 実測: Agentツール `general-purpose`（材料2本×5版×各2回＝20回＋言い直し方の検証2回＋
  応用のテンプレート化1回、のべ23回。事前知識ゼロの新規サブエージェントに材料と指示文だけを渡した）

📌 21:00の担当（この回）。副業節の先頭2件（各論X・各論YouTube）はクラウド側で到達性を実測した
ところ、どちらも `CONNECT tunnel failed, response 403`（経路の遮断。先方のbot判定ではない）で
書けなかった。⚠️ `support.google.com` はキューが「check_numbers.py から200」と記録していたのと
食い違う——実行コンテキストによって到達性が変わりうる。次の項目「収益化の条件」も研究担当の
原文確認パックが未着のため見送り、副業節の後方（研究パック群）から「見積もりの根拠を件数で
出させる」を選んだ。🔑 見立て（`estimate-hours-from-your-own-log` が「最大値を基準にする質問の
直後に母数が割れる」と書いていた現象）は8機会中0件で再現しなかった。代わりに、申告した件数と
AI自身が列挙した日付の数が食い違う回が20回中3回（15%）独立に発生した（詳しくは記事と証拠
ファイル・`content/_lessons.md` ★112・★113）。副業節は目標3本に対し1本のみ——理由は上記のとおり。

📌 この枠専任の回。自動化・ループ優先節の未処理先頭「途中で切れた一覧を続きでつなぐ」を選んだ。
仮説（抜けと重複が起きる）は12機会中0件で外れ、「定説は正しかった」で終わる記事になった。
崩れたのは中身ではなく書式のほうで、「16件目から40件目まで」という番号指定だけ、見出し行の
有無が回によって割れた（材料Aは2回とも有、材料Bは1回目無し・2回目有り）。詳しくは記事と
証拠ファイル・`content/_lessons.md` ★111。

### 2026-08-28
- 公開: 2本（long-thread-pending-items・自動化・ループ優先の枠 ／ role-prompt-same-answer・定説を実測する枠）
- 実測: Agentツール `general-purpose`（版a〜dの実測16回＋言い直し方・応用の追加確認6回、のべ22回。事前知識ゼロの新規サブエージェントに材料と指示文だけを渡した）
- 実測: Agentツール `general-purpose`（role-prompt-same-answer。6版×各2回＝12回・72問＋言い直し方/応用の補足3回、のべ15回。事前知識ゼロの新規サブエージェントに材料と指示文だけを渡した）

📌 キューの未処理先頭「長いスレッドから宿題だけ出させる」の仮説（経緯をまとめると終わった仕事が
混ざる）は外れた——8機会中0件。代わりに、もとの記事の指示文（最後の1往復だけを見る）のほうが
古い宿題を16機会中6件しか拾えず取りこぼしていた（詳しくは記事と証拠ファイル）。もとの記事
`mail-needs-reply` にも追記・相互リンク済み。

🚨 **この日は21:00の担当が2回firingした**（先着セッションが自動化枠から1本、この担当が
定説枠から1本）。副業節（`### 副業`）は当夜どちらの回も0本——直後にあった4件は
2026-08-28のオーナー指示で後回しに据え置かれ、唯一残る「収益化の条件」は研究担当（15:30）の
原文確認待ちでまだ届いていなかった（`content/_earn_research.md` 2026-08-28に該当パック無し）。
2番目のこの回は、副業0本を確認したあと、上から順にたどって「定説を実測する」節の先頭
「役割を与えよ」を選んだ。📌 見立てと違い、正しさ・もっともらしさ・捏造率とも役割の有無で
1つも動かず、動いたのは聞いていない条文名の書き添えという体裁だけだった（詳しくは記事と
証拠ファイル）。

### 2026-08-27
- 公開: 2本（kindle-royalty-formula / job-ai-policy-check）
- 実測: Agentツール `general-purpose`（記事の実測17回。前日に取り終えたぶんの追い計測を含む）
- 実測: Agentツール `general-purpose`（公開前の独立レビュー10回。判定コードを毎回、生回答に回し直させた）
- 実測: Agentツール `general-purpose`（job-ai-policy-check の実測39回＋公開前の独立レビュー2回）
- 公開: 1本（handoff-summary-adds-what-you-never-said・自動化・ループ優先の枠）
- 実測: `claude --safe-mode --tools "" -p "<指示文>"` を新規サブプロセス起動（材料2本×3版×2試行＝
  のべ12回。加えて「言い直し方」の受け皿つき版を材料2本で1回ずつ、のべ2回の追加確認）

📌 **自動化・ループ優先の枠は1本のみ。**キューの未処理先頭「要約させて新しい会話へ移る」の仮説
（出番の無かった縛りが落ちる）は外れた——24/24件で脱落せず、代わりに見出し・最終行などの
形式と、聞いていない実在のメールアドレスの混入が崩れた（詳しくは記事と証拠ファイル）。

📌 **1本に絞ったのはオーナー指示**（「質が高くなるまでクオリティを上げて、1本できたらまた次に行く」）。
レビュー10回で、公開したら誤っていた数字を2つ止めた——追い①の価格帯再掲が **2/3ではなく3/3**
（証拠の中略が判定行を落としていた）、図の色が主役の行だけ意味と逆（でたらめな価格帯を3回言い切った
版Aが緑）。⚠️ **中略した抜粋で判定を出すと、判定そのものが変わる。**

📌 **job-ai-policy-check は前セッションから「独立レビュー1回目がまだ実行できていない」状態で
引き継いだ2本目。**独立レビュー1回目で4件（ココナラ2026-08-22補正の未反映・要求された図2枚目の欠落・
引用の逐語性1件・相互リンク必須の未実装）を検出→修正。2回目で残り2件（合成した要約を「」で
逐語引用のように見せていた3箇所・**証拠ファイル自身の判定コードのバグ2件**＝確認文検出の正規表現が
「【クライアントへの確認文(下書き)】」のような括弧入り見出しを取りこぼし逆に辞退文を誤検出、
出典辞退検出の正規表現が活用形漏れで過小カウント）を検出→修正。**証拠ファイルの表④・表⑤は、
修正した関数を実際に生テキストへ再実行して裏取りしてから直した**（手で数字だけ書き換えていない）。
⚠️ **図2枚目は企画仕様どおりに作れなかった**（版④の実測が「規約項目0件」だったため、指定の
「対応表」は実測から作れず、代替として「辞退/作り話の実測グラフ」に差し替え。理由はキュー側に記録）。

- 公開: 1本（weekly-rate-crosses-the-line・副業節の待ち行列上から1本目）
- 実測: Agentツール `general-purpose`（3版×各3回＝9回＋確認用3回〔ランキング聞き方・
  別回答の点検・テンプレート化〕＝計12回。当初のキュー設計18回から圧縮した）

📌 **副業節の2番目・3番目（30案の次にもう30案／納品したのに入金の無い案件）は今回は
着手していない。**1本目に厚めに実測（版ごとの機械判定コード・証拠ファイルの逐語転記・
図の追加）をかけたところで区切った。キューの2件は `- [ ]` のまま残してある（`- [!]` にしていない
＝題材そのものは健全で、単に今回の枠に収まらなかっただけのため）。

### 2026-08-26
- 公開: 4本（convert-dates-keep-comparison-words / note-fee-before-you-sell /
  kindle-ai-disclosure-check / note-payout-depends-on-payment-method）
- 実測: `claude --safe-mode --tools ""` CLI（14:15の回）
- 実測: Agentツール `general-purpose`（21:00の回。note手数料だけで14回）

⚠️ **この1ブロックだけは、番人を入れた私（2026-08-26）が証拠ファイルから起こした。**
担当が書いたものではない。明日からは担当が毎晩書く。

---

## 📌 このログを作った理由（2026-08-26 オーナー指示）

**★91（実測の相手を立てられない）が2026-08-22 と 08-24 に起きたのに、
オーナーが気づいたのは4日後だった。**担当は0番のとおり正しく止まって記録も残していたが、
**その記録は人が読みに行かないと見つからない形**だった。

🔑 **既存の成果物だけを見る番人では捕まらない**（2026-08-26 に数えて確かめた）:

- 停止記録 `docs/evidence/_*-not-published.md` は**4件中3件が健全な題材停止**＝偽陽性75%
- **08-24 の環境停止は証拠ファイルを1つも残していない**（材料を作る前に止めたため）
- 公開本数でも鳴らない＝08-22は1本・08-24は3本。**別の回が成功するので0本の日にならない**

→ だから「担当が毎晩1行書く」以外に手が無い。**沈黙そのものを異常にする。**

⚠️ **過去の2件をここに日付節として書き足さないこと。**直近7日の `❌環境` で鳴る設計なので、
解決済みの事例を足すと即座に誤報になる。経緯は `content/_lessons.md` の★91が持っている。
