# レシピ担当の日次ログ（沈黙禁止）

**編集者向けの内部メモ。サイトには出ない**（ファイル名が `_` で始まるのでビルド対象外）。

毎晩、**書けた本数が0でも1ブロック書く。**書かないこと自体が異常の合図になる設計。
稼ぎ方研究担当の作業ログ（`_earn_research.md`）と同じ流儀で、**番人はここだけを見る。**

## 🔢 書き方（この2行は必須。番人が読む）

```
### YYYY-MM-DD
- 公開: N本（slug を並べる。0本なら「0本」と書く）
- 実測: <何で実測したか／止めたなら理由>
```

⚠️ **`- 実測:` は回ごとに1行。**1日に複数の回がある（実測で確認＝14:15の回と21:00の回）。
**回のぶんだけ行を足すこと**——番人は全部の行を見る。1行にまとめると2回目の異常を見落とす。

**止まったときは、原因を2種類に分けて書く。ここが番人の要。**

| 書く文字列 | 意味 | 番人 |
|---|---|---|
| `❌環境` | **実測の相手を立てられない**（★91）。題材を替えても同じところで止まる | 🚨 **鳴る** |
| `❌題材` | 見立てが再現しない・設計が測れない。題材を替えれば直る | 鳴らない（健全） |

🔑 **混ぜないこと。**過去の停止記録4件のうち3件は健全な `題材` で、
環境は1件だけだった。混ぜると偽陽性75%になり、検査全体が読まれなくなる。

📌 **番人＝`tools/check_freshness.py`**（`writer_log_heartbeat` / `writer_measurement_gaps`）。
2日ぶん記録が空くと鳴る（1晩休んでも鳴らない＝0番による正常な停止があるため）。
`❌環境` は直近7日ぶんだけ鳴る（直った後も鳴り続けさせない）。

## 🛠 実測の相手が立てられないときに試す順（★91）

1. **`claude --safe-mode --tools "" -p "<指示文>"`** ← 実績あり。
   `--safe-mode` は CLAUDE.md・skills・plugins・履歴を全部無効化するので、
   別セッションより厳密に事前知識ゼロ。⚠️ **`--bare` は認証が通らない**
   （多段の会話は `--session-id <uuid>` で発行して `--resume` で続ける）
2. **Agentツール `general-purpose`**（同一セッション内の新規サブエージェント）
3. **`mcp__Claude_Code_Remote__create_session`**

🚨 **3つとも塞がったら、材料を作る前に止めてよい**（★84の系列）。
そのときは `- 実測: ❌環境 — <エラー文面>` と書く。**キューの項目は `- [ ]` のまま残す**
（`- [!]` にすると原因が直っても再試行されない）。

## 記録

### 2026-09-05（自動化・ループ優先の枠。専任の別ルーティン）
- 公開: 1本（unknown-field-marked-anomaly）。
- 実測: `claude --safe-mode --tools "" -p`（新規プロセス、呼び出しごとに完全独立）。架空の
  更新監視ログ2本（各15項目・列が無い古い項目5件を含む）に、①無指定②①＋「根拠も一言
  添えて」③受け皿を明示、の3版を材料2本×各2回＝12回、追加確認2種×材料2本×各1回＝4回、
  誤りの言い直し独立2回＝計18回。核（列が無い5件を異常に誤分類した件数）は①4回中1回、
  ②4回中3回（②は①より悪化）、③8回とも0件だった
- 手順1で選んだのは「自動化・ループ優先」節の未処理2件のうち上から1件目（あとから足した
  監視項目）。残り1件（二重起動防止のフラグ）は今回は着手せず`- [ ]`のまま残す
  （0番＝本数はノルマではない。1本を実際に試すほうを優先した）
- 実測中に12番（計測器の誤検知）を1回踏んだ＝見出し区分（**正常**／**異常**／**まだ分からない**）
  でグループ化された返り1件を、単純な「ID直後の語」判定では0/15と誤検出した。
  見出しを追跡する専用パーサに切り替えて15/15と判定し直した（`_lessons.md` 12番節に追記）

### 2026-09-04（自動化・ループ優先の枠。専任の別ルーティン）
- 公開: 1本（manual-catchup-without-writeback）。
- 実測: `claude --safe-mode --tools "" -p`（新規プロセス、呼び出しごとに完全独立）。架空の
  受付ログ2本（問い合わせ24件・PC故障受付24件）×3パターン×各3回＝18回＋手動処理そのものの
  検証4回＝計22回。核（書き戻し忘れのとき、人が処理済みの5件が次の自動実行にまた含まれた
  件数）は2材料×3回＝6回すべて5/5（100%重複）。書き戻し済みでは6回すべて0/5。取りこぼしは
  全パターン0件。自動側の指示文は書き戻し忘れ／基準確認のどちらでも一字一句同一で、
  結果を分けたのは記録ファイルの値だけだった
- 手順1で選んだのは「自動化・ループ優先」節が0件だったため、他節で🔁の付いた未処理
  （2026-09-02補充分の1件目）。もう1件（急増日の自己申告）は今夜は着手せず`- [ ]`のまま残す
  （0番＝本数はノルマではない。1本を実際に試すほうを優先した）
- 実測中に12番（計測器の誤検知）を2回踏んだ＝①説明文中のID参照を一覧の中身と誤検出
  ②「次回のための最新処理ID」欄のIDを一覧の中身と誤カウント。どちらも判定コードを
  直してから記事に使った（`_lessons.md` 12番節に追記）

### 2026-09-03（21:00 レシピ担当の回・続報）
- 公開: 2本目（second-thirty-not-new・「定説を実測する」型・副業）。
- 実測: `claude --safe-mode --tools "" -p`（空ディレクトリ、`--session-id`/`--resume`で多段会話）。
  架空の発注書2本×5版×各2回＝20回（本編）＋発想の重なりを直接聞く／まとめさせる追加8回＋
  言い直し方7回＝のべ51回。判定は Python（`difflib` 含む）。仕込みではなく**抽出コードのほうを
  2回直した**（12番の再発。返りの体裁（番号や太字の有無）に依存していたため）。核心＝
  「重なっているか」を機械の文字列一致で数えると0〜5件だが、AI自身に聞くと19〜28件（30案中）で
  1桁違った——新しい教訓として `_lessons.md` に★124を追加
- 副業の残り2本（AI検索に出るように直す仕事／AIの使い方を教える仕事）は、
  一次情報（`www.conmark.jp`・`www.street-academy.com`）がこの環境から到達不能（経路遮断）で
  ⏸を維持。今夜はここまでで副業節を終える（1本目とあわせて計2本）

### 2026-09-03（21:00 レシピ担当の回）
- 公開: 1本（fix-ai-drafts-for-pay・副業／📚シリーズ「AIの仕事を受ける」の1本目）
- 実測: `claude --safe-mode --tools "" -p`（空ディレクトリで呼び出しごとに独立）。
  架空のコワーキングスペース紹介文1本に問題を10件仕込み、11通りの指示文をのべ24回
  （2ターンのものは `--session-id` / `--resume`）。判定は Python の文字列照合と `difflib`
- 到達性の実測（クラウド側・2026-09-03）: `coconala.com` のカテゴリページは **200**（価格を写せた）。
  ⚠️ `help.coconala.com` は `CONNECT tunnel failed, response 403`＝**経路の遮断**で、
  **販売手数料の率は取れていない**。記事には率を書かず「出品価格は受け取る額ではない」とだけ書いた
- 副業の残り2本（AI検索に出るように直す／AIの使い方を教える）は、需要の裏取り先
  （`www.conmark.jp`・`www.street-academy.com`）が2件とも `CONNECT tunnel failed, response 403`
  ＝**経路の遮断**で開けず、`⏸` を付けて着手を見送った（`❌題材` ではなく到達待ち）

### 2026-09-03
- 公開: 1本（overseas-notice-to-jst・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: `claude --safe-mode --tools ""`（新規プロセス、呼び出しごとに完全独立）。架空の英文案内
  24通×2材料、3版×材料2本×各2回＝12回・のべ288件に加え、自己点検版と言い直し版2種を
  材料2本×各1回＝のべ106件、のべ16回・394件の判定
- 手順1で選んだのは「自動化・ループ優先」節の唯一の未処理（海外の案内を日本時間に直す）。
  同節はこれで空になった
- 見立て（日付が変わる回でずれる）は外れ、日付・時刻の計算そのものは394件を通じて0件の
  誤り。かわりに「決められないものは要確認に」という受け皿自体が4回に1回誤動作し、
  先回りして直そうとしたら悪化した（★72の再発として`_lessons.md`に追記）。効いたのは
  止まった行だけを貼り直して聞き直す言い直し方

### 2026-09-02
- 公開: 1本（bracket-example-fixes-the-split・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: `claude --safe-mode --tools ""`（新規プロセス、呼び出しごとに完全独立。材料2本×4版×各2回
  ＝16回＋言い直し版〈アクセス許可の一文〉2本×2回＝4回、のべ20回・160件超の判定。並列4本
  同時実行で1バッチ（4回）が空返答になる不具合を実測中に踏み、原因を切り分けたうえで逐次実行に
  切り替えて再取得した）
- 手順1で選んだのは「自動化・ループ優先」節の未処理の先頭（頼む前に自分の宿題を一覧にする）。
  同節の残り1件（海外の案内を日本時間に直す）は今回未着手のまま残した（本数はノルマではない
  ため、1本を実測込みで丁寧に仕上げることを優先した）
- 核（人にしかできない8件をAIが自分の列に入れた件数）は5版・20回・のべ160件を通じて0件で、
  いちばん心配していた向きは一度も起きなかった。動いたのは正答率のほうで、括弧の例なしは
  32件中27件（84%）、記事どおり例ありは32件中32件（100%）、決まらない欄を足すと32件中28件
  （87.5%）に低下した。元記事`who-does-what`の「例が無いとほとんど何も入れてきません」という
  言い切りは強すぎたと判明したため、実測値に差し替えて相互リンクを追加した
- `_lessons.md`に★119・★120を追記し、台帳46番（材料の不完全さがそのまま穴になる）の再発も
  記録した

### 2026-09-01
- 公開: 1本（ask-for-reasons-breaks-the-match・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: Agentツール `general-purpose`（同一セッション内の新規サブエージェント、ツール不使用・
  ファイル読み書きなしを指示、呼び出しごとに独立した会話）。材料2本×各3回＋罠つき8件×3回＝
  のべ168件（素直な指示文）、理由添え版24件×3回、除外条件先出し版8件×2回、
  自信度自己申告版8件×2回、集計行版24件×1回、計17回の独立呼び出し。
- 手順1で選んだのは「自動化・ループ優先」節の未処理の先頭（毎朝の判定を3回とって見比べる）。
  同節の残り2件（頼む前に自分の宿題を一覧にする／海外の案内を日本時間に直す）は今回未着手
  のまま残した（本数はノルマではないため、1本を実測込みで丁寧に仕上げることを優先した）
- 素直な指示文（理由を求めない）は、材料2本＋罠つき8件のいずれも新規の会話で3回とも
  一字一句同一・全問正解で、仮説（3回そろって3回とも誤り）は1件も再現しなかった。
  ところが指示文に「理由も一言添えて」と一文足しただけで、同じ材料・同じ規程でも
  3回のうち2回に別々の誤りが混ざり、答えが割れた（★93の再発）。当初の見立てとは
  別の場所で定説が崩れたので、題は実測に合わせて変えた

### 2026-08-31
- 公開: 1本（unrelated-material-stayed-out・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: `claude --safe-mode --tools ""`（新規プロセス、呼び出しごとに完全独立。5版×各2回＝
  10回＋言い直し方の追加検証4回、のべ14回。事前知識ゼロではない前提で機械照合できる結果だけを
  根拠にした）
- 手順1で選んだのは「自動化・ループ優先」節の未処理の先頭（毎朝の資料をフォルダごと渡す）。
  同節の残り3件（毎朝の判定を3回とって見比べる／頼む前に自分の宿題を一覧にする／海外の案内を
  日本時間に直す）は今回未着手のまま残した（本数はノルマではないため、1本を実測込みで丁寧に
  仕上げることを優先した）
- 結果は仮説（無関係な資料が混ざると答えに紛れ込む）が10機会中0件で外れ、「定説は正しかった」
  側の記事になった。実測中に判定コード自体の誤検知（台帳12番・★24の再発）を踏み、修正してから
  記事に反映した（`_lessons.md` ★24節に追記）

### 2026-08-30
- 公開: 1本（resume-from-last-not-clock-time・自動化・ループ優先の枠。専任の別ルーティン）
- 実測: Agentツール `claude`（新規サブエージェント。4版×各3回＝12回＋初回起動2回＋次回位置の書き出し2回、のべ16回。事前知識ゼロではない前提で機械照合できる結果だけを根拠にした）。一次データは手元のgit履歴（`data/tracker/seen.json`、外向き通信ゼロ、全履歴276コミット）
- 手順1で選んだのは「自動化・ループ優先」節の未処理の先頭（決まった時刻にAIを走らせる）。同節の残り4件（毎朝の資料をフォルダごと渡す／毎朝の判定を3回とって見比べる／頼む前に自分の宿題を一覧にする／海外の案内を日本時間に直す）は今回未着手のまま残した（本数はノルマではないため、1本を実測込みで仕上げることを優先した）
- 公開: 1本（recipes/youtube-payout-thresholds・21:00のレシピ担当・副業節の先頭〈各論YouTube〉）
- 実測: `claude -p`（このリポジトリの外に`mktemp -d`で作った空ディレクトリで、1回ずつ完全新規プロセスとして起動。`--disallowedTools`で調査ツールを封じた独立実行）。7指示文×各3回＝21回、全成功。判定は正規表現の文字列照合のみ
- `create_session`は今夜も`permission_mode is not yet available`で4回とも失敗（★91再発）。Agentツール自体がこのハーネスに存在しなかった。`_lessons.md`★91に既にあった`--safe-mode`方式を実測完了後に見つけたため今回は使っていない（次回は先にそちらを試すこと、と`_recipe_queue.md`に追記済み）
- 副業節の未処理は、この1本の消化後も複数件残っている（本文執筆時点で確認したのは節冒頭の収益化条件パック〈研究担当の原文確認待ちで保留〉と、末尾側の「30案の次にもう30案」「納品したのに入金の無い案件」「直させた後に見る場所を絞る」「材料の抜けを、数で確かめる」の計4件。いずれも未着手）。今夜は1本の実測・検証・図・エビデンスを丁寧に仕上げることを優先し、2本目以降には着手しなかった

### 2026-08-29
- 公開: 1本（resume-a-cut-off-list・自動化・ループ優先の枠）
- 実測: Agentツール `general-purpose`（3版×各2回×材料2本＝12回＋健全性の下限2回＋検算プロンプト2回＋見出し禁止版の追い確認2回、のべ18回。事前知識ゼロではない前提で機械照合できる結果だけを根拠にした）
- 公開: 1本（tools/money-map-three-routes・手動ライターセッション・記事型「お金の流れ」総論の1本目）
- 実測: 実測なし（調査型＝原文4件を実ブラウザで当日取得して逐語照合。独立レビューはAgentツール5巡＝12→13→10→5→3件で収束）
- 公開: 1本（recipes/x-pay-before-you-earn・記事型「お金の流れ」各論Xの1本目・オーナー例示）
- 実測: Agentツール `general-purpose`（版a〜g の7版×各3回＝21回。ほかに版cの3回を破棄したので実行はのべ24回。ウェブ検索を切り、リポジトリを読ませず、文脈も引き継がせない独立実行。判定は正規表現のみ）

🚨 **各論Xの回で、書き手（私）の読み違いが実測で露見した。**「過去3か月以内に500万回」を
「3か月ぶんの月額2,940円が最低でも要る」の意味だと思って投げたら、3回とも「最短は980円
（1か月分）」「これはさかのぼって数える集計の窓であって、待つ期間ではない」と返ってきた。
記事・money-note・図を980円基準に直し、判定コードに `min_is_980` / `explains_window` を
足して機械で確認してから公開。⚠️ **AIが誤りを指摘したのではない**——指示文に読み違いを書かなかったので正しい答えが
返っただけ。**予想と違う答えが返ったら原文へ戻る**というこちら側の手順とセットでしか効かない。
⚠️ 同じ回で判定コードのバグを9件見つけた（厳しすぎる側5・甘すぎる側3・仮説と守備範囲のずれ1）。
1件は「仮説の集計が、記事に載せていない破棄済みの版を見ていた」＝**版を増やしたら仮説側の参照も直す。**
もう1件は「H8の文は『条件の中身や数字』なのに、チェックは数値しか見ていなかった」＝
0/3 を「守られた」と読むと誤りだった（中身は3回とも漏れていた）。詳しくは `_lessons.md` ★116。
📌 独立レビューは4巡（14→27→13→11件）。3巡目・4巡目でも**引用の非逐語**と
**判定していないことの断定**が出続けたので、この2つは次回の観点に固定する。

📌 手動セッションの回はもう1件、既存記事のタイトル12本の書き直し（オーナー承認【B】・
URLは不変）と、validate.py へのタイトル具体性検査（固有名詞または数字必須・2026-08-29〜）を
実施。詳細は `_recipe_queue.md` の「タイトルの付け方」5番・6番と SESSION_HANDOFF.md。

- 公開: 1本（count-the-estimate-basis・副業節）
- 実測: Agentツール `general-purpose`（材料2本×5版×各2回＝20回＋言い直し方の検証2回＋
  応用のテンプレート化1回、のべ23回。事前知識ゼロの新規サブエージェントに材料と指示文だけを渡した）

📌 21:00の担当（この回）。副業節の先頭2件（各論X・各論YouTube）はクラウド側で到達性を実測した
ところ、どちらも `CONNECT tunnel failed, response 403`（経路の遮断。先方のbot判定ではない）で
書けなかった。⚠️ `support.google.com` はキューが「check_numbers.py から200」と記録していたのと
食い違う——実行コンテキストによって到達性が変わりうる。次の項目「収益化の条件」も研究担当の
原文確認パックが未着のため見送り、副業節の後方（研究パック群）から「見積もりの根拠を件数で
出させる」を選んだ。🔑 見立て（`estimate-hours-from-your-own-log` が「最大値を基準にする質問の
直後に母数が割れる」と書いていた現象）は8機会中0件で再現しなかった。代わりに、申告した件数と
AI自身が列挙した日付の数が食い違う回が20回中3回（15%）独立に発生した（詳しくは記事と証拠
ファイル・`content/_lessons.md` ★112・★113）。副業節は目標3本に対し1本のみ——理由は上記のとおり。

📌 この枠専任の回。自動化・ループ優先節の未処理先頭「途中で切れた一覧を続きでつなぐ」を選んだ。
仮説（抜けと重複が起きる）は12機会中0件で外れ、「定説は正しかった」で終わる記事になった。
崩れたのは中身ではなく書式のほうで、「16件目から40件目まで」という番号指定だけ、見出し行の
有無が回によって割れた（材料Aは2回とも有、材料Bは1回目無し・2回目有り）。詳しくは記事と
証拠ファイル・`content/_lessons.md` ★111。

### 2026-08-28
- 公開: 2本（long-thread-pending-items・自動化・ループ優先の枠 ／ role-prompt-same-answer・定説を実測する枠）
- 実測: Agentツール `general-purpose`（版a〜dの実測16回＋言い直し方・応用の追加確認6回、のべ22回。事前知識ゼロの新規サブエージェントに材料と指示文だけを渡した）
- 実測: Agentツール `general-purpose`（role-prompt-same-answer。6版×各2回＝12回・72問＋言い直し方/応用の補足3回、のべ15回。事前知識ゼロの新規サブエージェントに材料と指示文だけを渡した）

📌 キューの未処理先頭「長いスレッドから宿題だけ出させる」の仮説（経緯をまとめると終わった仕事が
混ざる）は外れた——8機会中0件。代わりに、もとの記事の指示文（最後の1往復だけを見る）のほうが
古い宿題を16機会中6件しか拾えず取りこぼしていた（詳しくは記事と証拠ファイル）。もとの記事
`mail-needs-reply` にも追記・相互リンク済み。

🚨 **この日は21:00の担当が2回firingした**（先着セッションが自動化枠から1本、この担当が
定説枠から1本）。副業節（`### 副業`）は当夜どちらの回も0本——直後にあった4件は
2026-08-28のオーナー指示で後回しに据え置かれ、唯一残る「収益化の条件」は研究担当（15:30）の
原文確認待ちでまだ届いていなかった（`content/_earn_research.md` 2026-08-28に該当パック無し）。
2番目のこの回は、副業0本を確認したあと、上から順にたどって「定説を実測する」節の先頭
「役割を与えよ」を選んだ。📌 見立てと違い、正しさ・もっともらしさ・捏造率とも役割の有無で
1つも動かず、動いたのは聞いていない条文名の書き添えという体裁だけだった（詳しくは記事と
証拠ファイル）。

### 2026-08-27
- 公開: 2本（kindle-royalty-formula / job-ai-policy-check）
- 実測: Agentツール `general-purpose`（記事の実測17回。前日に取り終えたぶんの追い計測を含む）
- 実測: Agentツール `general-purpose`（公開前の独立レビュー10回。判定コードを毎回、生回答に回し直させた）
- 実測: Agentツール `general-purpose`（job-ai-policy-check の実測39回＋公開前の独立レビュー2回）
- 公開: 1本（handoff-summary-adds-what-you-never-said・自動化・ループ優先の枠）
- 実測: `claude --safe-mode --tools "" -p "<指示文>"` を新規サブプロセス起動（材料2本×3版×2試行＝
  のべ12回。加えて「言い直し方」の受け皿つき版を材料2本で1回ずつ、のべ2回の追加確認）

📌 **自動化・ループ優先の枠は1本のみ。**キューの未処理先頭「要約させて新しい会話へ移る」の仮説
（出番の無かった縛りが落ちる）は外れた——24/24件で脱落せず、代わりに見出し・最終行などの
形式と、聞いていない実在のメールアドレスの混入が崩れた（詳しくは記事と証拠ファイル）。

📌 **1本に絞ったのはオーナー指示**（「質が高くなるまでクオリティを上げて、1本できたらまた次に行く」）。
レビュー10回で、公開したら誤っていた数字を2つ止めた——追い①の価格帯再掲が **2/3ではなく3/3**
（証拠の中略が判定行を落としていた）、図の色が主役の行だけ意味と逆（でたらめな価格帯を3回言い切った
版Aが緑）。⚠️ **中略した抜粋で判定を出すと、判定そのものが変わる。**

📌 **job-ai-policy-check は前セッションから「独立レビュー1回目がまだ実行できていない」状態で
引き継いだ2本目。**独立レビュー1回目で4件（ココナラ2026-08-22補正の未反映・要求された図2枚目の欠落・
引用の逐語性1件・相互リンク必須の未実装）を検出→修正。2回目で残り2件（合成した要約を「」で
逐語引用のように見せていた3箇所・**証拠ファイル自身の判定コードのバグ2件**＝確認文検出の正規表現が
「【クライアントへの確認文(下書き)】」のような括弧入り見出しを取りこぼし逆に辞退文を誤検出、
出典辞退検出の正規表現が活用形漏れで過小カウント）を検出→修正。**証拠ファイルの表④・表⑤は、
修正した関数を実際に生テキストへ再実行して裏取りしてから直した**（手で数字だけ書き換えていない）。
⚠️ **図2枚目は企画仕様どおりに作れなかった**（版④の実測が「規約項目0件」だったため、指定の
「対応表」は実測から作れず、代替として「辞退/作り話の実測グラフ」に差し替え。理由はキュー側に記録）。

- 公開: 1本（weekly-rate-crosses-the-line・副業節の待ち行列上から1本目）
- 実測: Agentツール `general-purpose`（3版×各3回＝9回＋確認用3回〔ランキング聞き方・
  別回答の点検・テンプレート化〕＝計12回。当初のキュー設計18回から圧縮した）

📌 **副業節の2番目・3番目（30案の次にもう30案／納品したのに入金の無い案件）は今回は
着手していない。**1本目に厚めに実測（版ごとの機械判定コード・証拠ファイルの逐語転記・
図の追加）をかけたところで区切った。キューの2件は `- [ ]` のまま残してある（`- [!]` にしていない
＝題材そのものは健全で、単に今回の枠に収まらなかっただけのため）。

### 2026-08-26
- 公開: 4本（convert-dates-keep-comparison-words / note-fee-before-you-sell /
  kindle-ai-disclosure-check / note-payout-depends-on-payment-method）
- 実測: `claude --safe-mode --tools ""` CLI（14:15の回）
- 実測: Agentツール `general-purpose`（21:00の回。note手数料だけで14回）

⚠️ **この1ブロックだけは、番人を入れた私（2026-08-26）が証拠ファイルから起こした。**
担当が書いたものではない。明日からは担当が毎晩書く。

---

## 📌 このログを作った理由（2026-08-26 オーナー指示）

**★91（実測の相手を立てられない）が2026-08-22 と 08-24 に起きたのに、
オーナーが気づいたのは4日後だった。**担当は0番のとおり正しく止まって記録も残していたが、
**その記録は人が読みに行かないと見つからない形**だった。

🔑 **既存の成果物だけを見る番人では捕まらない**（2026-08-26 に数えて確かめた）:

- 停止記録 `docs/evidence/_*-not-published.md` は**4件中3件が健全な題材停止**＝偽陽性75%
- **08-24 の環境停止は証拠ファイルを1つも残していない**（材料を作る前に止めたため）
- 公開本数でも鳴らない＝08-22は1本・08-24は3本。**別の回が成功するので0本の日にならない**

→ だから「担当が毎晩1行書く」以外に手が無い。**沈黙そのものを異常にする。**

⚠️ **過去の2件をここに日付節として書き足さないこと。**直近7日の `❌環境` で鳴る設計なので、
解決済みの事例を足すと即座に誤報になる。経緯は `content/_lessons.md` の★91が持っている。
