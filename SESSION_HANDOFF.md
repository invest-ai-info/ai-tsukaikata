# SESSION_HANDOFF — AIの使い方（ai-tsukaikata.com）

最終更新: 2026-08-13 07:19 JST

---

## 🚩 次のセッションで最初に見るところ

0. ✅ **【解決】床割れは 8/13 朝に補充した＝未処理 5件 → 10件**（`queue_shortage()` が
   None を返すことを実測済み）。種を替えて再実行（種15語×ツール5×語尾16種=1,200クエリ・
   1,126語）。**追加5件**＝面接練習196語（最大・型かぶり2件の条件つき）／文字起こし174語／
   マニュアル作成98語／図解74語／アンケート集計72語。
   **見送りはチラシ139語・グラフ131語ほか**（ツールの機能が主役で頼み方の型にならない）。
   根拠と見送り理由＝`docs/superpowers/notes/2026-08-10-demand-research.md` の
   「2026-08-13 再実行」節。
   ⚠️ 消化ペースが7本/日の日があるので、**次に床を割るのは早ければ 8/14 夜**。
   同じ手順で再実行できる（種を替えるだけ・約10分）。

0e. ✅ **【解決】8/11 の「作業ブランチに push されてサイトに出ない」は、v2 で直った。**
   8/12 夜の回は**3コミットとも main に直接**入った（21:34 / 21:50 / 21:52）。
   作業ブランチ `claude/serene-hamilton-6jq782` は作られたが**中身は全部 main にあり残骸**。
   🔑 **8/11 の型（実行は緑・報告も正常・記事も存在するのに、サイトにだけ出ない）は
   「静かな欠落」の新種**として台帳に残してある。**再発したら真っ先にここを疑う。**
   ⚠️ **残骸ブランチ2本**（`zen-lovelace-bame8s` / `serene-hamilton-6jq782`）は
   どちらも main に取り込み済み。消してよいが、消さなくても害はない。
0b. ✅ **進化ループ v1 導入済み（2026-08-12 未明）**＝記事を書くたびにサイトが上手くなる
   3つの輪（夜=台帳に教訓を継承／週=コードへ昇格／月=読者データで方向）。
   設計書＝`docs/superpowers/specs/2026-08-11-evolution-loop-design.md`、
   計画＝`docs/superpowers/plans/2026-08-11-evolution-loop.md`。

   | 入ったもの | 何 |
   |---|---|
   | `content/_lessons.md` | **書き方の台帳**。まだコードでない教訓だけの待合室。**種13件入り** |
   | `docs/evidence/TEMPLATE.md` | 試した証拠の様式（返りを要約させない・判定は機械） |
   | キュー9〜11条 | 台帳を読む／証拠を残す／**push先は origin/main** |
   | `queue_shortage()` | 待ち行列が**6件（2晩ぶん）を切ったら週次で知らせる**。本番で1回流して偽陽性ゼロを確認 |

   🔥 **台帳★1「禁止は、受け皿と対で渡す」は5本で出ている＝昇格の最優先候補。**
   ⏭ **次は 8/17(月) の週次1回目**（昇格判定／拾い読み1種／見える改善1件／キュー残量）。
   ⏭ **残るは v1.5＝証拠の機械照合**（計画の Task 9）。証拠が2〜3晩たまってから較正して有効化
0f. ✅ **進化ループ v1 が、初日から丸ごと回った（2026-08-12 夜・検証完了）。**
   夜の担当が**自発的に**やったこと＝①証拠ファイル2本（44KB・41KB）を
   `docs/evidence/` に作成 ②台帳へ追記（43行・35行＝★16と★17） ③**既存番号を参照して
   「★7が再発した」と記録**（新番号を増やさないルールが守られた）
   ④重複で1件を `- [!]` に。**指示していない良い挙動は無い＝全部 v2 の指示どおり。**
   ⏭ **次は 8/17(月) の週次1回目**。昇格の最有力は台帳★1（禁止は受け皿と対で・**6本で出現**）。

0c. 🆕 **日中に手書きでレシピ3本を公開した（2026-08-11・レシピ26本に）。**
   オーナー指示「ストックを小出しにする気は無い。記事はどんどん作って」。
   `quiz-yourself-with-ai` / `fix-typos-not-voice` / `slides-outline-first`。
   **3本とも証拠ファイル付き**（`docs/evidence/<slug>.md`＝進化ループv1の様式を先取り）。
   詳細は下記「2026-08-11 日中に書いた3本」節
0d. ✅ **Search Console を登録した（2026-08-11 20:1x・運営者が実施）＝長く残っていた宿題が消えた。**
   ドメインプロパティ・TXT検証・サイトマップ送信済み（**44ページ検出**）。
   ⏳ **8/12 に見ること**＝TXTが権威DNS3台とも揃ったか（20:16時点では ns2 のみ。手順は下記
   「2. Search Console」節）。検索クエリが見えるのは**1〜2週間後**
1. **AdSense再申請**＝レシピ**33本**で条件は十分。**Search Console も登録済みなので、
   運営者側の前提はすべてそろった。**⏳ 残るは運営者が申請ボタンを押すだけ
   ⭐ 題材選びは下記「検索需要リサーチ」の最後を読むこと
2. **ツール別の深掘りを書くかどうか。**横断1本 `/tools/only-this-tool/` を 8/10 に出した。
   反応を見てから Grok単体・ChatGPT単体…に分けるか決める段取りにしてある
3. **`/start/`（始め方の指南書・2026-08-09 公開）も1枚もの。**同じく反応待ち

### 2026-08-08 22:11 に片付いた分（詳細は各節）

| 前回の宿題 | 結果 |
|---|---|
| マーカーが上限13個に張り付かないか | **データで否定。指示書は変えない**（下記「マーカーの拾い読み」） |
| マーカーの拾い読み（8本未確認） | **8本読了。3本を直した** |
| 図の無い3本に図を足す | **3枚追加・`FIGURE_EXEMPT_SLUGS` は撤去**（例外ごと削除） |

ついでに見つかって直したもの（どちらも「検査が静かに漏らしていた」型）:

| 見つかったもの | 結果 |
|---|---|
| 図の厳密計測が**48枚中18枚しか測っていなかった** | `sitemap.xml` から辿る形にした。実測 71枚・問題0件 |
| 訂正で外した図が1枚**生成され続けていた** | 生成関数ごと削除（29枚に） |

---

## 現在地

**2段目のサイトが本番公開されました。** https://ai-tsukaikata.com

### 📊 2026-08-13 07:19 時点の数字（次のセッションはここから）

| | |
|---|---|
| レシピ | **33本**（8/11 に23本→8/12 で33本。1日で7本の日があった） |
| 証拠ファイル | **11件**（`docs/evidence/`。8/12 以降に書いた記事は全部ある） |
| 書き方の台帳 | **19件**（`content/_lessons.md`。昇格済みはまだ0件） |
| キュー | 未処理 **10件**（8/13 朝に5件補充）／ 保留 `- [!]` **3件** |
| テスト / ビルド / 図 | 438 passed ／ 53ファイル ／ 52枚 |
| 運営者の宿題 | **なし**（Search Console 登録済み・プロンプト v2 貼り替え済み） |

**動いているもの:** レシピ担当（毎晩21:00・v2）／深掘り担当（毎朝08:00）／
トラッカー（毎時 `:17`・実際は2〜4時間おき）／ダイジェスト（毎朝07:22）／
freshness（月曜09:40・キュー残量の床つき）。

⏭ **予定:** 8/14頃 v1.5（証拠の機械照合を `validate.py` へ）／
**8/17(月) 週次1回目**（昇格・拾い読み・見える改善1件・キュー残量）／
Search Console のクエリは 8/25 頃から見える見込み。

| 段 | 内容 | 状態 |
|---|---|---|
| 1 | AI更新情報トラッカー | ✅ 稼働中（2026-08-01〜） |
| 2 | サイト本体 ai-tsukaikata.com | ✅ **公開中**（2026-08-02〜） |
| 3 | 使い分けマップ | ⬜ 保留（トラッカーがデータを貯めてから） |

### 2026-08-06 リデザイン＋ニュース欄（オーナー承認済み・4段階すべて実装）

設計書＝`docs/superpowers/specs/2026-08-06-warm-redesign-news-design.md`（ブレストの決定経緯つき）。
計画書＝`docs/superpowers/plans/2026-08-06-news-on-top.md`。テストは **385 passed**。

| # | 内容 | コミット |
|---|---|---|
| 1 | **トップに「AIアップデート」欄＋/news/**（news.json から生成。お知らせ=3行要約・モデル=ラベル畳み） | `40c60be`（✅本番確認済み） |
| 2 | **「あたたかい紙」配色**（クリーム地・ダークは茶系。見出し帯5色塗り分け） | `dda6b60`（✅デプロイ成功） |
| 3 | **絵と画像**＝hero.svg・アイキャッチ自動生成・ベンダー識別マーク | `d90dee8` |
| 4 | **major お知らせ→深掘りキュー自動追記**（1日3件・二度追記しない） | `b163d74` |

### 2026-08-07 の続き（記事20本到達・場面分類・提案型シリーズ開始）

| | |
|---|---|
| **レシピ20本に到達** | AdSense申請の条件（20本）を満たした。**次にやるのは再申請** |
| **提案型シリーズを開始** | オーナー指示＝「実際に指示をした例ではなく、こう使えますと提案してほしい」 |
| **場面（scene）で探せるように** | トップに場面カード、`/scenes/<name>/` に一覧 |
| ヒーロー刷新 | オーナー生成イラストを実写バナー化。ファビコン・OGPも同じ原画から自動生成 |

**🆕 提案型シリーズ＝本数を出せる型（重要）**

従来のレシピは材料が「実際にやった作業」なので在庫が尽きる（実測: ネタ帳の残り4件）。
提案型は材料が「AIでできること全般」なので尽きない。**ここで本数を出す。**

- ⚠️ **体験の語り口で書かない。**「私はこれで成功した」ではなく「こう頼むとこう返る」。
  既存方針の「体験を主張しない記事は全自動公開してよい」に乗る型なので、崩すと自動化できなくなる
- ⚠️ **提案型は入口。既存の実践記事へ内部リンクで着地させる**（1本あたり3〜5本）
- 候補10本は `content/_ideas.md` の「提案型シリーズ」節にある
- 書いた5本＝`find-what-to-delegate` / `life-paperwork` / `fun-make-things` /
  `batch-instead-of-repeat` / `meeting-to-minutes` / `research-with-sources`

🚨 **本数の上限は 2026-08-11 に撤廃された（オーナー判断）。**以下は撤廃前の経緯。
2026-08-07 に「1日5本の量産はしない」と決めていた＝Googleが「規模を拡大した
コンテンツの不正使用」として明示的に対象にしている領域で、AdSense審査に不利。
**このリスクは消えていない**が、オーナーが承知のうえで「記事はどんどん作る」と判断した。
実測した現実的な密度＝1本あたり指示文10〜12個・図1〜2枚・内部リンク3〜5本。
⚠️ **歯止めは本数ではなく品質ゲートだけになった**（実試行・証拠・密度・拾い読み）。
決定場所は `content/_recipe_queue.md` の冒頭1か所。

**場面（scene）の仕組み**

`content/*.md` の frontmatter に `scene:` を1つ書く。定義は `src/config.py` の `SCENES`。

| scene | ラベル | 本数(2026-08-07) |
|---|---|---|
| `start` | はじめて | 2 |
| `work` | 仕事 | 8 |
| `research` | 情報収集 | 5 |
| `automate` | 自動化 | 3 |
| `life` | 暮らし | 1 |
| `fun` | 遊び・創作 | 1 |
| `choose` | AIを選ぶ | 2 |

- **記事0本の場面は出さない**＝新しい場面を先に定義しておいてよい（入った時点で現れる）
- **知らない場面の値はビルドを止める**（誤字だとその記事だけどの一覧にも出ない静かな欠落になる）
- ⚠️ 場面を足すときは `SCENES` に足すだけ。テンプレートもCSSも触らなくてよい

**ブランド画像は原画1枚から生成する**＝`python -m tools.make_brand_images`。
`static/images/og-card.png`（オーナー生成の原画）から hero-photo.jpg / og-image.jpg /
favicon.ico / apple-touch-icon.png / icon-512.png を作る。**絵を差し替えたら再実行すれば全部そろう。**

⚠️ **ダークモードの主ボタンは白文字にしない。**`--accent` はダークでは明るいコーラルなので、
白を載せるとコントラストが 2.2:1 に落ちる。濃茶（`#1c1712`）にして 8.1:1 にしてある。

**Claude Design 連携（2026-08-07）**＝プロジェクト「AIの使い方 — あたたかい紙」
（`f701e8e7-a0a8-4aee-b566-4e696c5cc0b9`）に12部品を push 済み。
⚠️ **MarketWatch の「Design System」プロジェクトとは別**（`foundations/colors.html` 等が同名で衝突するため）。
生成元＝scratchpad の `build_ds_bundle.py`（CSSとSVGをインラインに焼き込んで自己完結させる）。

### 🆕 レシピ担当（2026-08-08 新設・毎晩21:00 JST）

**クラウドルーティン `ai-tsukaikata-recipe`**（`trig_01FGvebQwr8LyaqjTB8tfbuo`・
cron `0 12 * * *` ＝ 21:00 JST・model=opus-5）。管理画面＝
https://claude.ai/code/routines/trig_01FGvebQwr8LyaqjTB8tfbuo

| | |
|---|---|
| 指示書 | `content/_recipe_queue.md`（題材の待ち行列＋「担当が守ること」各条） |
| 見本 | `find-what-to-delegate.md` と `batch-instead-of-repeat.md`（提案型） |
| 出すもの | 提案型レシピ。**下書きではなく公開する**。⚠️ **本数は指示書の冒頭が唯一の決定場所** |

🚨 **⚠️ push 先＝プロンプト v2 に入れた1節（2026-08-11 夜の事故）**

8/11 の回は `claude/zen-lovelace-bame8s` に push され、**main に入らずサイトに出なかった**。
担当は「セッションの指定に従い」と報告しており、**クラウド環境が作業ブランチを切る**のが原因。
8/8〜8/10 は main に直接入っていたので、環境側の挙動が変わったと見られる。
📌 皮肉なことに、8/10 は**コンテナの古い `main` 参照**があったせいで担当がブランチを
意識して直していた。手順0でそれを潰した結果、今度は素直に作業ブランチへ入れてしまった。

```
🚨 push 先は必ず origin の main です。作業ブランチ（claude/... など）にいる場合は、
最後に main へ入れるところまでやってください:
  git checkout main && git pull --rebase && git merge <作業ブランチ> && git push origin main
最後に `git log origin/main -1` で、自分のコミットが origin/main に載ったことを確かめる。
⚠️ main に入っていないとサイトは更新されません。実行が成功していても、記事は
誰にも届いていません（2026-08-11 に実際に起きました）。
```

**報告にも「origin/main に載ったか」を書かせる**（`git log origin/main -1` の結果をそのまま貼らせる）。

🚨 **ルーティンのプロンプトは claude.ai の中にしか無い。リポジトリを直しても届かない。**
2026-08-10 の「1晩3本」がこれで伝わらなかった（下記「1晩3本への変更」節）。
**作法や本数を変えたら、リポジトリとプロンプトのどちらを直すべきかを必ず考えること。**
🔑 原則＝**プロンプトには手順だけを書き、変わりうる数はリポジトリに置いて参照させる。**
プロンプトは 2026-08-08 の作成以来ほぼ凍っていて、「6条」のような数はすでに腐っていた。

📌 **コンテナの `main` 参照が古い系統を掴むことがある**（2026-08-10 実測）。
担当が「記事8本時点の古い別系統を指していた」と報告し、リモートの実体に合わせ直して
fast-forward で push した。**実害は出ていないが、担当が毎回これを直せる前提にはしない。**
🔑 ルーティンは `git fetch` してから**リモートの `origin/main` を基点にする**のが正しい。

**なぜ全自動で公開してよいか**＝提案型は体験を主張しないので、既存方針
「体験を主張しない調査記事は全自動で公開してよい」に乗る。⚠️ **この境目が崩れると根拠が消える。**
「私はこれで成功した」と書き始めたら、その記事は自動公開の対象外。

**品質の本体は3つ。ここを緩めると仕組みごと無価値になる:**

1. **公開する指示文は、担当が自分で実際に試す**（架空データで処理してみて、返りを見てから書く）。
   試していない指示文を「こう返ってきます」と書かせない
2. **重複を先に確かめる**＝`grep -h '^## ' content/recipes/*.md | sort -u` で既存の節見出しを
   全部並べる（集約型で丸かぶりを踏んだ事故があるため）
3. **迷ったら公開しない**＝キューに `- [!]` と理由を書いて終わるのが正しい仕事、と明記してある

**密度の下限は `validate.py` がビルドで強制する**（レシピ限定・指示文6個/図1枚/内部リンク1本/
本文1800字）。閾値は既存20本の実測より下に取ってある＝手書きの良記事は必ず通る。
⚠️ **下限を満たすことは目的ではない。**満たしていても薄ければ担当が止める。
✅ **図の下限に例外は無い**（2026-08-08 22:11）。`FIGURE_EXEMPT_SLUGS` で3本を名指しで外していたが、
3本とも図を足したので**定数ごと削除**した。テストも「名指しの例外を作らない」を検査する形に置き換え済み
（`test_no_recipe_slug_is_exempt_from_the_figure_rule`）。⚠️ **例外を1つでも復活させないこと。**
名指しの穴があると、次に書く人が「そこに足せばいい」と学ぶ。

⚠️ テストの擬似記事を薄くすると密度ゲートが正しく発火して他の検査のテストが道連れになる。
`tests/test_validate.py` の `_article()` は既定を `pages` に、`tests/test_build.py` の
`RECIPE` は下限を満たす形にしてある。

**レシピ担当の試運転（2026-08-08 21:25 実行・成功）**＝1本目は `mail-needs-reply`。
指示文15個・図1枚・内部リンク4本・本文5,253字。**「試してから書く」ゲートが実際に効いた**＝
架空の受信箱14通で試したところ、キューの題材「返信が要るものだけ抜く」では経費精算や
アンケート督促が静かに落ちると分かり、**担当が自分で3分類（返信/自分の作業/読むだけ）へ
題材を組み替えた**。指示していない改善。この挙動は維持する。

### アイキャッチは「場面（scene）」で絵柄が変わる（2026-08-08 修正）

⚠️ **カテゴリだけで構図を選んでいた版は、レシピ21本が全部ノート＋鉛筆になった**
（オーナー指摘「記事との関連性が薄い」）。いまは scene ごとに主役の道具を変える＝
はじめて=道しるべ／仕事=チェック付き書類／情報収集=ルーペとアンテナ／自動化=歯車と時計／
暮らし=家とレシート／遊び=パレットと絵筆／AIを選ぶ=天秤。

⚠️ **主役は必ず中央 x=270〜450 に置く。**カードのサムネ(84px角)は `object-fit: cover` で
**バナー(720×180)の中央180px幅だけ**を映す（計算で確認済み）。端に置いた小物はカードでは
見えない。`tests/test_make_eyecatch.py::test_focus_region_holds_the_main_motif` が強制する。

⚠️ **生成物を作り直すときは先に `git pull` する。**他の担当（レシピ・トラッカー）が
push した記事があると、その記事だけ旧方式のまま取り残される（実際に1枚起きた）。

**新しい運用ルール:**
- **記事を足したら `python -m tools.make_eyecatch` を1回**（全記事ぶん決定的に再生成・冪等）。
  実行しなくてもビルドは壊れない＝アイキャッチが無い記事は画像なしで組まれる。
  ⚠️ frontmatter の `scene:` を絵柄の選択に使うので、**scene を付け忘れると仕事の絵になる**
- **ベンダーのアイコンは公式ロゴを模倣しない**（商標の誤認回避・オーナー説明済み）。
  頭文字＋連想色の独自マーク＝`src/news.py` の `VENDOR_ICONS`
- `build.yml` の `paths:` に **`data/tracker/news.json` と `tracker/sources.yml` が入った**
  ＝ニュースが増えたコミットだけサイトが再デプロイされる（seen.json だけの更新では動かない）
- トラッカーの check が major お知らせを `content/_deepdive_queue.md` へ自動追記する
  （キュー追記は派生データ＝失敗してもメールと既読保存を巻き込まない。tracker.yml がキューもコミットする）
- **SVGの class は `<style>` に定義されているかをテストが機械照合**（黒塗り事故の再発防止。
  `tests/test_make_eyecatch.py`）

### 🆕 `/start/` 始め方の指南書と、鮮度の仕組み（2026-08-09）

設計書＝`docs/superpowers/specs/2026-08-09-getting-started-guide-design.md`。
計画書＝`docs/superpowers/plans/2026-08-09-getting-started-guide.md`。
**収集した事実＝`docs/superpowers/notes/2026-08-09-start-facts.md`（次に触るときはここから読む）。**

きっかけ＝オーナー指示「トップに各ツールの始め方を。インストール方法・必要なもの・
PCスペックを初心者でも分かるように」。⚠️ **初稿の方針「スペックは関係ない」は
オーナー指摘で撤回した**＝50代60代やPCを買えない中高生は古いPCを使っている可能性があり、
そういう人ほどこのページに来る。「関係ない」は新しいPCを持つ人にだけ成り立つ答えだった。

**🔑 記事の芯になった事実（すべて公式ページの生テキストから。要約は使っていない）:**

1. **4つのうち一番重い Claude Code に公式の最低要件がある**＝`4 GB+ RAM, x64 or ARM64`。
   つまり **4GBあれば全部動く**、と出典つきで書ける。ブラウザ版3社はメモリ非公表
2. **足切り線は Windows 10 1809（＝ビルド 17763）。**Claude Code の `Windows 10 1809+` と
   ChatGPT Windowsアプリの `17763.0 or higher` が同じ線（Microsoft の公開情報で確認）
3. ⚠️ **設定画面の「バージョン」欄は `25H2` 形式で、公式の条件 `1809以降` と形式が違う。**
   初心者には比べられないので、**記事では「OS ビルド」（17763以上）を見るよう書いた**

**⚠️ 設計時の前提が3つ覆った。次に同じ思い込みをしないこと:**

| 思っていたこと | 実際 |
|---|---|
| Claude Code には Node.js が要る | **要らない**（ネイティブ導入）。この機械も Node.js 未インストールで 2.1.104 が動いている。npm版だけが Node.js 22+ |
| ターミナルが必須 | **必須ではない**。公式に「The Desktop app lets you use Claude Code without the terminal」 |
| `openai.com` は403で読めない | **素で200だった。**届かなかったのは `support.anthropic.com` だけ。**先入観で決めず実際に叩くこと** |

**鮮度の仕組み（ここが設計の要）:**

🚨 **「古い」でビルドを止めてはいけない。**`build.py` は「全部通る or 何も出さない」なので、
確認日の古さで止めると、**毎晩21:00のレシピ担当が push した記事が巻き添えで公開されなくなる**。
担当は成功したと記録し、サイトには出ない＝このサイトが一番警戒している「静かな欠落」。

| 何 | どこ | 何をする |
|---|---|---|
| `checked` が未来日付・壊れている | `src/validate.py` の `_checked_errors` | **ビルドを止める**（本人がその場で直せる） |
| `checked` が無いこと自体 | — | **止めない**（全記事必須にすると既存が全部落ちる） |
| 外部リンクの死活／確認日90日超／`checked` の付け忘れ | `tools/check_freshness.py` | **週次で知らせる**（`.github/workflows/freshness.yml`・月曜 JST 9:40） |
| 通知 | GitHub の標準の失敗通知メール | 専用の通知コードは作らない |

- ⚠️ **`checked` を slug 名指しで必須にしない**（2026-08-08 に撤去した `FIGURE_EXEMPT_SLUGS` と
  同じ穴を逆向きに作ることになる）。付け忘れは週次の一覧で拾う
- ⚠️ **自分のリポジトリ・自分のサイトへのリンクは `checked` を求めない**（`OWN_LINK_PREFIXES`）。
  `privacy.md` が自分のGitHubへのリンクで永久に鳴り続ける偽陽性になっていた。
  **死活の検査からは外していない**（自分のリポジトリでも切れていれば直す）
- 📌 **`content/tools/` の3本は毎週鳴り続ける**（公式の料金ページを引用していて `checked` が無い）。
  **これは正しい指摘。**直すかどうかは次の判断。鳴っていること自体は異常ではない

**確認日はページにも出る**（`.checked-note`）＝読者が自分で古さを判断できるほうが、検査より確実。

**🆕 リンクの引っ越しも見る（2026-08-09 追加）。**記事に貼った `docs.claude.com` が、
すでに `code.claude.com` へのリダイレクトになっていた。🔑 **リダイレクトが効いている限り
200が返るので、死活の検査だけでは永久に気づけない。200が返ることと、そのURLが正式であることは別。**
`head()` が状態コードと**実際にたどり着いたURL**の両方を返すようにして、突き合わせている。

⚠️ **実測してから偽陽性を3種類潰した。**どれも「直しようがないのに毎週鳴る」型で、
放置すると一覧そのものが読まれなくなる。**この手の検査は、書いたら必ず本番のリンクで
1回流して鳴り方を見ること**（今回は流さずに出していたら、初週から3件の空振りが出ていた）。

| 空振りの型 | 実例 | どう絞ったか |
|---|---|---|
| 同じホスト内の転送 | `claude.ai/` → `claude.ai/login`（未ログインなだけ） | **ホストが変わったときだけ**鳴らす |
| bot判定の403 | `claude.ai/` が `cf-mitigated: challenge` 付きの403 | **notes に回して失敗させない**（人が開けば見える）。⚠️ 目印の無い403は従来どおり問題として扱う |
| 転送先を記事が既に貼っている | `gemini-3-6-flash` が出典1番に移転先を貼ったうえで旧URLを注記していた | 記事内の他のリンクと**突き合わせて黙る** |

⚠️ **`Report(problems, notes)` の notes で週次を失敗させないこと。**
黙って消すと「確かめた」と誤解されるので表示はするが、直せないものでメールを飛ばさない。

**検証済み:** 428 passed ／ ビルド42ファイル ／ ブラウザ厳密計測 73枚・786要素で**問題0件** ／
スマホ375pxでページの横スクロールなし（図だけ枠内でスクロール＝設計どおり）／
本番で `/start/` と図2枚が200・トップに導線・確認日表示を実測。

### 🚨 1晩3本への変更（2026-08-10 オーナー指示）と、初回が1本だった件

オーナー指示「1日1本ではなく最低3本」。`content/_recipe_queue.md` の冒頭を3本に変更した。

🚨 **その後、2026-08-11 に上限そのものを撤廃した（オーナー判断「記事はどんどん作る」）。**
いまの決定は `content/_recipe_queue.md` 冒頭の「🔢 上限なし。作法を守って書けるだけ」。
規模拡大コンテンツのリスクは承知のうえ。**歯止めは品質ゲートだけになった。**
📌 **日中のセッション（Claude）も書き手に回る**＝夜のルーティン専任ではなくなった。
⚠️ 同じ題材を二重に書かないよう、**書いたら即 `- [x]` にして push する**。

**🚨 初回（2026-08-10 夜）の結果は1本**（`translate-for-internal-use`）。3本ではない。

### ✅ 原因は確定した（2026-08-11 13:51・実行セッションを実際に読んだ）

🔑 **ルーティンのプロンプトが「1本」のままだった。**本数が**2か所**に書いてあり、
直したのは**リポジトリ側だけ**だった。claude.ai の中にあるプロンプトには届いていない。

実際に 8/10 の実行へ送られた指示文の1行目＝
**「今日ぶんのレシピを1本書いて公開してください。」**、手順1＝**「題材を1つ選ぶ」**。
→ **担当は指示どおり正確に1本出しただけで、何も間違えていない。**
`- [!]` が無いのも当然（本人の認識では完遂している）。

⚠️ **当初の①②はどちらも外れ。同じ立て方をしないために残す:**

| 当初の見立て | 実際 |
|---|---|
| ① 質を守れないので止めた | ✗ 止まっていない。完遂して要約と通知まで出している |
| ② 1回の実行に3本が入りきらない | ✗ **1本あたり12分**。実行履歴＝8/10は21:09開始→21:21コミット、8/9は21:14→21:27、8/8は21:25→21:32。**3本でも約36分**で上限に当たらない |

🚨 **引き継ぎに書いていた切り分け方「今夜も1本なら②」は成り立たなかった。**
③でも今夜は1本になるので、この判定では②と③を区別できない。
**そのまま②と誤診してルーティンを3分割していたら、原因が1文の言い忘れでも
「3本出るようになった」ので間違った直し方が正解に見えていた。**
→ 🔑 **ルーティンの挙動を疑う前に、まず「実際に送られた指示文」を読む。**
コミットの `Claude-Session:` トレーラに実行セッションのURLが入っている
（8/10 以降のコミットのみ。`git log --format=%(trailers:key=Claude-Session,valueonly)` で出る）。

### ✅ 対処済み＝プロンプトを貼り替えた（2026-08-11・運営者が実施）

管理画面 https://claude.ai/code/routines/trig_01FGvebQwr8LyaqjTB8tfbuo の鉛筆アイコンから、
下記6点を反映した全文に差し替え済み。**今夜（8/11 21:00）の実行が最初の検証になる。**
**本数はもうプロンプトに書いていない**——書くとまた片方が古くなる。

| # | 直した場所 | 直した内容 |
|---|---|---|
| 1 | 冒頭「…レシピを**1本**書いて公開してください。」 | 「…レシピを公開してください。**何本書くかは `content/_recipe_queue.md` の冒頭に書いてあります。この指示文には本数を書きません**（2か所に書くと片方が古くなる。実際に 2026-08-10 にそれで1本しか出なかった）。」 |
| 2 | 「『担当が守ること』**6条**」 | 「『担当が守ること』**の各条**」（数を書くとまた腐る。実際いまは0〜8の**9条**あるのにプロンプトは6条のまま） |
| 3 | 手順1「題材を**1つ**選ぶ」 | 「題材を、**キューが指定する本数だけ上から順に**選ぶ。⚠️ **1本ずつ順に仕上げる**（並行に書かない）。1本仕上げるたびに、次の題材がいま書いたものと切り口で衝突しないか確かめ直す」 |
| 4 | 手順9「コミットして push する」 | 「コミットして push する。⚠️ **1本ごとに**コミットして push する（まとめて1コミットにしない。途中で止まっても書けたぶんは残る）」 |
| 5 | 最後の報告 | 「**何本公開したかを必ず書く。目標より少ないときは、その理由と、書かなかった題材を `- [!]` にしたことも書く**」を足す |
| 6 | 手順の先頭に**手順0**を新設 | 「`git fetch origin` して、HEAD が `origin/main` の続きかを確かめる。ずれていたら**まだ何も書いていないうちに** `git reset --hard origin/main` で取り直す」（下記のコンテナ `main` 参照の件） |

🔑 **5番が本命の再発防止。**今回いちばん困ったのは「1本だった」ことではなく、
**足りないことが誰にも報告されず、人が数えて気づくしかなかった**こと。
このサイトが一番警戒している「静かな欠落」がルーティンの報告に開いていた。

⚠️ **プロンプトの本数を下げて辻褄を合わせないこと。**出た1本の質は高い。

⚠️ **指示書に入れた歯止めは維持すること:**
- **0番「本数はノルマではない」**＝本数を増やすと最初に削られるのが「実際に試す」。
  そこが消えるとこのシリーズは「それらしい指示文の一覧」になり価値が消える
- **同じ晩に書く3本どうしの重複検知**＝公開済みとだけ突き合わせても、3本が
  似た切り口になったことには気づけない。**並行ではなく1本ずつ順に書かせる**

**出た1本の中身は良かった**（この挙動は維持する）＝架空の英文リリースノートで17個の指示文を
試し、**3つを実測で直した**。とくに時差の換算を**実際に1時間間違えた**のを捕まえている
（9:00 AM PT → 日本時間 午前2時と返したが、2026年の米国夏時間は3月8日開始なので正解は午前1時）。

### 🆕 2026-08-11 日中に書いた3本（レシピ23→26本）

オーナー指示「別にストックを小出しにして時間を稼ぐつもりはないので、記事はどんどん作って」。
**3本とも架空データで全指示文を試し、判定は Python でやった**（目視判定なし）。
証拠は `docs/evidence/<slug>.md` に生の入出力ごと残してある。

| 記事 | 需要 | 実測で見つけたこと |
|---|---|---|
| `quiz-yourself-with-ai` | 勉強213語 | 「説明して」だと**分かった気になる**。出題させると穴が出る。⚠️ **「〜するな」だけでは問題が作れない**（「〜が正解になる問題に」と受け皿が要る） |
| `fix-typos-not-voice` | 校正58語 | 素朴に「校正して」＝欠陥6/6直るが**守りたい語0/4**（本件・別紙・稟議・体言止めが全滅）。**申告7件に対し実際の差分13件**＝一覧に出るのは「直した」と自覚した分だけ |
| `slides-outline-first` | スライド277＋プレゼン134語 | 箇条書き14件が**18枚に転記**される（構成ではない）。**口で言うことは画面の3.9倍**＝これが読む資料との違い |

🔒 **境界を2組、キューの両側に明記した**（次に書く人が迷わないように）:
- `fix-typos-not-voice`（**表記**のみ）↔ 「批評させる（直させない）」（**中身**のみ・未執筆）
- `slides-outline-first`（**口頭発表**のみ）↔ 「企画書・A4資料」（**読む資料**のみ・未執筆）

🔥 **昇格条件を満たした教訓が出た＝「禁止は、受け皿と対で渡す」（3本連続）。**
「〜するな」だけを渡すと、止めたくないものまで一緒に止まる。実例＝出題（問題が作れない）／
校正（直ってほしい重複まで残る 3/6）／スライド（「反対を1枚立てて」だけだと
「ご指摘は当たりません」調の反論になる）。**月曜の週次で最優先の昇格候補。**
（`translate-for-internal-use` の「用語の言い換えと原文厳守が衝突」も同じ根）

⚠️ **もう1つの教訓＝計測スクリプト自体を検算する。**スライド記事で、末尾ブロックの
取りこぼしにより「画面の最大175字」という**誤った値**が出た（正しくは24字）。
記事に載せる前に気づいて直したが、**記事に載る数字は生成物ではなく計測器のほうを疑う**。

### 🆕 2026-08-12 の記録（レシピ28→33本・1日で7本）

**日中のセッションが5本、夜のルーティンが2本。全部が架空データでの実測＋証拠ファイル付き。**

| 記事 | 実測でいちばん重い発見 |
|---|---|
| `weekly-menu-from-fridge` | 🚨 **えびアレルギーを条件で渡したのにえび料理が入った**（冷凍室にむきえびが実在する状態）。献立自体は読んでもおかしくないので気づけない。**「まだ作らないでください」で条件確認を先に挟むと 0/7 → 7/7** |
| `weekly-report-from-memo` | 🚨 メモの事実**9件中2件しか残らない**。消えた7件は全部「進んでいない」情報。**消えた場所は空欄にならず先週の言葉で埋まる**（「値引きの可否を相談したい」→**「特になし」**）。メモに無い**「進捗85%」**（先週70%からの補間） |
| `ask-what-is-missing-first` | 🚨 **「この頼み方を良くして」は逆効果**＝AIが決めた設定が **4件→7件**。増えた3件は事実関係（3日遅延・5年以上の取引・初めて）。正しくは**「足りない情報を挙げて。まだ書かないで」**＝0件／質問7件 |
| `critique-not-rewrite`（夜） | 🚨 **読み手を名指しすると、書かれていない欠陥 5/5 と引き換えに文中の欠陥が 0/5 になる。**視点を1つに決めるともう一方が見えない → **2回に分けて聞く** |
| `try-the-runbook`（夜） | 🚨 **「添削して」は、間違いを指摘ではなく削除で処理する。**根拠のない断定が**申告にも出ないまま本文から消えた**＝本人は自分の断定に根拠が無かったことを知らないまま |

⚠️ **キューの題を1本、実測に基づいて改題した**＝「聞き方を整えてもらう」→
「**足りない情報を聞き返してもらう**」。**題のまま書いていたら、逆のことを勧める記事に
なっていた**（台帳15番＝切り口が間違いと分かったら組み替えてよい、の実践）。

📌 **私（日中のセッション）が起こした事故2件**。どちらも台帳に入れてある:
- `SESSION_HANDOFF.md` を**PowerShell で文字化けさせて push**（★13）。`06eccc9` から復旧
- `make_figures.py` に**BOM を付けた**（同じ罠）。ただし**台帳が効いて diff で即検出・即除去**

### 🆕 2026-08-11 夜のルーティン（本数上限を撤廃してからの初回・レシピ28本に）

**結果＝2本公開＋2件を `- [!]` で停止**（21:15開始・21:46終了）。

| | |
|---|---|
| `summarize-without-dropping` | 架空の社内通知（54文・2,192字）に**落としてはいけない情報14件**を仕込んで要約させた。素朴に「要約して」＝**458字で4/14しか残らない**。⚠️ **落ちる10件は種類が偏る**＝「ただし・〜に限り・対象外・〜ないでください」を含む10文は**0文しか残らなかった**。「大事なところを落とさずに」は**効かない**（4→5件）。**落ちる原因は長さではない**＝41字短い417字の版が14/14 |
| `tidy-files-without-deleting` | 架空の散らかったフォルダ45件。素朴に「整理して」＝**9件が「スクリーンショット5点」に丸められて一覧から消える**。🚨 **削除をすすめられた14件に、更新日時でいちばん新しいファイルが入っていた**（名前の「最終_確定」を信じ、**12日17時間ぶん新しいほうを旧版と判定**） |

✅ **止め方が正しかった。**3本目に入る前の重複確認で、**日中のセッションが書いた記事と丸かぶり**と
判明したため2件を `- [!]` にして停止した（企画書A4→`slides-outline-first` と4点既出／
表記ゆれ→`fix-typos-not-voice` の節見出しそのもの＋同じ晩の2本目と型が同じ）。
🔑 **「書ける条件」（既出に乗らない核は何か）までキューに書き残している。**この挙動は維持する。

⚠️ **本数の上限は 2026-08-11 にオーナーが撤廃した**（「上限なし。作法を守って書けるだけ」）。
📌 日中のセッションはこの変更を `git add -A` で自分のコミットに巻き込み、
**コミットメッセージに書かなかった**。害は無かったが、他人の編集を黙って混ぜている。
**`git add -A` の前に `git status` を見ること**（プロンプト v2 にも入れた）。

### 🆕 検索需要リサーチ（2026-08-10・930クエリ / 5,104語）

記録＝`docs/superpowers/notes/2026-08-10-demand-research.md`（**手順のコードごと残してあるので再実行できる**）。

Googleサジェストを日本語で総当たりし、既存22本と突き合わせて
**「需要があるのに1本も書いていない」4領域**を抽出してキューの先頭に入れた。

| テーマ | 語数 | 状態 |
|---|---|---|
| 翻訳させる | 230 | ✅ 8/10 に公開（`translate-for-internal-use`） |
| 勉強・学習に使う | 213 | ✅ **8/11 に公開**（`quiz-yourself-with-ai`） |
| 校正・添削させる | 58 | ✅ **8/11 に公開**（`fix-typos-not-voice`。「批評させる」との境界をキューに明記済み） |
| スライドを作らせる | 38 | ✅ **8/11 に公開**（`slides-outline-first`。8/11の再実行では411語で最大テーマ） |

⚠️ **「語数」は検索回数ではなく言い回しの種類の多さ。**順位の目安でしかない。
ノイズも混ざる（「哀 例文」が実際に混入した）。

📌 **2026-08-11 に再実行した**（種15語×語尾16種＝1,200クエリ・1,430語→除外後1,268語）。
キューへ5件補充（要約・PC内のファイル整理・企画書A4・表記ゆれ・献立）。見送りも理由ごと
記録してある（読書＝実態が感想文の代筆でサイトの信頼と衝突、等）。詳細は同じ notes の
「2026-08-11 再実行」節。⚠️ **「ゼロ／あり」の機械判定は語面の一致だけ。**プレゼンは
「ゼロ」と出たがスライドと同一需要、「整理」は「あり」に見えたが該当記事は無かった。
**束ねと最終判断は人間がやること。**

⭐ **次に本当に効くのは Search Console の登録**（運営者の5分作業）。
今回使ったのは全部「世間一般の推測」だが、Search Console だけは**このサイトに実際に
来ている検索語**が出る。**大規模化より先にこれをやったほうが、同じ手間で桁が変わる。**

### 🆕 ツール横断記事（2026-08-10 公開）

`/tools/only-this-tool/` ＝ ChatGPT / Claude / Gemini / Grok の
**「1社しか書いていないこと」**。オーナー要望「ツールごとの強み・得意ジャンルを知りたい」から。

🔑 **「〜でしかできない」とは書かない。**他社が「できない」ことを示す必要があるのに、
公式ページはできないことを書かない＝原理的に検証できない。主張は全部
**「2026-08-10 に4社の公式を見た範囲で、書いていたのは1社だけ」**の形にしてある。
**得意ジャンルも断定しない**（各社のベンチマークは自社測定なので比較にならない）。
代わりに**各社の自称を引用**した。

拾えたもの＝ChatGPT: 会議の録音そのもの（ChatGPT Record）／Claude: 手元のファイルを直接編集／
Gemini: 音楽生成と Gmail 連携／Grok: X のリアルタイム連携（**同じ会社が X を持っている**＝
技術ではなく資本による差なので、他社が真似しにくい）。

⚠️ **また前提が2つ覆った。先入観で決めず、毎回実際に叩くこと:**

| 引き継ぎの記録 | 2026-08-10 の実測 |
|---|---|
| 「x.ai は403でbotブロック。OpenRouter経由が唯一」 | **200で読める**（製品ページも） |
| 「openai.com は素で200だった」（8/9） | **403（cf-mitigated: challenge）**。`help.openai.com` から確認した |

📌 深掘り担当も 8/11 に同じ壁で正しく停止している（`5560d6a`＝openai.com は bot 判定で
本文が読めず、URL自体がフィードから消えていた）。**この止まり方は維持する。**

### 🆕 マーカーの拾い読み（2026-08-08 22:11・8本読了）

黄色だけ順に読んで意味が通るかの検査。**2026-08-07〜08 の8本**（提案型6本＋`mail-needs-reply`＋
`gpt-5-6-sol-luna`）を通し読みし、**3本を直した**。全部「マーカーが単体で意味を持たない」型。

| 記事 | 直したもの | 種類 |
|---|---|---|
| `batch-instead-of-repeat` | 「最後の一文がこの記事で一番大事です。」 | **禁止されている「次の文への矢印」そのもの** |
| `meeting-to-minutes` | 主語（「会議で一番重要な情報は」）がマーカーの外だった | 範囲の切り方 |
| `gpt-5-6-sol-luna` | 「迂回はしていません。」→「ブロックの迂回はしていません。」 | 単体で何の話か分からない |

⚠️ **矢印マーカーは1本目の型が一番出やすい。**「最後の一文が」「後半が本体です」のように、
**マーカーの外にある文を指す言い方**。ルールに名指しで書いてあるのに1件すり抜けたので、
拾い読みは今後も続ける（機械には判定できない）。

**マーカー上限13個への張り付きは、データで否定された。**前回「試運転1本目が13個ぴったり」を
警戒事項にしていたが、レシピ21本を数え直すと**平均10.8・中央値11・範囲8〜13**で、
**13個ちょうどは4本**。うち3本（`claude-code-memory` / `limit-what-ai-touches` /
`verify-before-report`）は**レシピ担当ができる前に人が書いたもの**。
→ **13は「濃い記事の正常な着地点」であって担当の癖ではない。**
⚠️ **指示書に「上限の8割を目安に」は足さないこと**（足すと濃い記事が薄くなる）。
再検討する条件を決めておく＝**担当の記事が3本続けてちょうど13になったら**、そのとき初めて疑う。

### 🆕 図の厳密計測は、一覧を手で書かない形にした（2026-08-08）

🚨 **`tools/measure_figures.js` が18枚しか測っていなかった。**測る図をファイル名で並べる作りで、
その間に図は48枚に増えていた＝**30枚が黙って未測定のまま「問題なし」と出ていた。**
検査そのものが「静かに0件」を起こしていたことになる。

- 直し方＝**`sitemap.xml` → 各ページ → `<img>` の順にたどる**。実際にサイトに貼ってある図を全部拾うので、
  記事を足しても直さなくてよい。⚠️ **一覧を手で書き戻さないこと**
- 実測（2026-08-08 22:00 頃・37ページ）＝**図71枚・文字要素738個・問題0件**。
  旧方式の48枚指定より多いのは、**アイキャッチのSVGも拾うようになった**ため

✅ **訂正で取り残されていた図を消した**（2026-08-08・運営者判断）。
`static/images/extrapolated-numbers.svg` が**どのページからも参照されていない唯一の図**だった。
2026-08-05 の訂正（gpt-5.5-pro の $60/$270 は**実在する**）で `numbers-not-in-the-source` の図が
`summary-vs-raw.svg` に差し替わったときの取り残しで、**中身は「その数字は出典に無い」という、
いまは誤りと分かっている主張の図**。読者には見えないが `make_figures.py` が毎回生成し続けていた。
**生成関数 `extrapolation_chart()` ごと削除**（図は29枚になった）。

🔑 **この手の取り残しは、参照の突き合わせでしか見つからない。**記事を差し替えても図は残る。
上のsitemap方式の計測が「どのページからも参照されていない図」を炙り出せるようになったのが発見の経緯。

### 2026-08-02 夜のセッションでやったこと（コミット12件）

すべて push 済み・本番反映済み・作業ツリーは空。

| | |
|---|---|
| レシピ記事 | **5本 → 7本**（`verify-before-report` / `limit-what-ai-touches`） |
| サイトの方針 | 「動かしたものだけを書く」の約束を撤去し、**未実行の調査記事も書ける**形に |
| 作法の番人化 | マーカー上限・見出しclassを `validate.py` がビルドで強制 |
| トラッカー | アクション版上げ・**更新停止の検出**・死んだ4ソースの入れ替え（15→13） |
| GitHub Actions | checkout v7 / setup-python v7 等へ更新。本番実行で動作確認済み |

---

## 次のセッションで最初にやること

### 0. 🚨 まず読む — 前のセッションが入れた誤りを訂正した件（2026-08-05）

**「AIが `gpt-5.5-pro` の $60/$270 を外挿した」という記録は誤りだった。**生ページには
`gpt-5.5-pro $30.00 - - $180.00 $60.00 - - $270.00` と**実在する**（見出しは Short context / Long context の8列）。
ルーティンの下書きが正しく、**誤ったのは検証した人間側**＝確認をAIに要約させ、「その行は無い」「272Kという
但し書きだけ」という**事実でない確認結果**を信じて、正しい数字を消していた。`272K` はそのページに1度も出てこない。

- 訂正済み＝`content/tools/claude-opus-5.md`（数字を復元）／`content/recipes/numbers-not-in-the-source.md`
  （中心の実例を差し替え・図も `summary-vs-raw.svg` へ）／深掘りルーティンのプロンプト／`_ideas.md`／
  `_deepdive_queue.md`／`tests/test_check_numbers.py`
- 🔑 **教訓＝数字の確認は、要約させずに生の行を見る。**要約は、書いていないことを書くだけでなく、
  **書いてあることを消す**こともある。`tools/check_numbers.py` の `fetch()` が生テキストを返すので、
  それを自分で grep するのが速い（WebFetch は要約を返すので確認には使わない）
- ✅ **Gemini記事も同じ方法で再検証済み**。標準料金・Batch料金とも生ページの行と全件一致。
  `Flash Cyber` が料金ページに0回出現＝「行が無い」も正しい

### 0b. 次にやること

1. **レシピ残り2本**（材料は `content/_ideas.md` に実例つき）
   - RSSが無いサイトを追う（クラス名で拾うと次のデプロイで静かに0件）
   - 図の崩れを検査で止める（**検査が見落とす崩れ**の実例も加わった＝CSSクラス未定義でSVGが黒塗りになる件）
2. **深掘りの待ち行列は現在すべて `- [!]`（止まっている）。**新しい題材を足すなら、
   **到達性は必ずクラウド側で測る**（手元のPCの結果は移らない。下記「到達できるかどうかは〜」節）
3. **Qwen3.8** ＝ 許可リストに `help.aliyun.com` と `qwen.ai` を足せば再開できるが、**重みが未公開**なので
   足しても書けない可能性がある。**GPT-Live** ＝ `openai.com` の Cloudflare 判定次第。そのまま再実行の価値あり

### 0c. ⏰ ダイジェストの停止警告 — **配線もワークフローもシロ。残るはメール現物の確認だけ**（2026-08-03 21:30 更新）

> ✅✅ **2026-08-04 08:29 JST の回で状態コミットまで確認できた**（`ebdeb7b`）＝送信経路は完全にシロ。中身は OpenAI 1件
> ＋停止警告。以後この項目は**閉じてよい**。
>
> ✅ **2026-08-03 08:14 JST（`23:14:37Z`）に digest run #2 が実行・成功した。**cron は 07:22 なので **52分の遅延**＝
> 下記「schedule は定刻に来ない」の実測どおりの範囲。**受信箱に届いているはずのものを見るだけ**が残作業。
> 件名は **`⚠️ AIトラッカー: 1件のソースに問題があります`**（`pending_minor` が0件だったため警告側の文面）。
> 本文の該当行＝`⚠️ hf-zhipu: 47日あたらしい記事なし`

`hf-zhipu` が **2026-06-16 で47日更新なし**の状態で `seen.json` に記録済み。閾値30日超なので停止警告が1件出るはず。

**2026-08-03 朝に、送信せずに配線だけ先に確かめた。** `seen.json` を1バイトも触らずに `run_digest` と
同じ順序で `store`/`notify` を呼ぶドライランを実行 → `stale_sources() -> [('hf-zhipu', 47)]`、
本文に「更新が止まっているソース」の節が出ることまで確認済み。**`run_digest → stale_sources → build_body`
は繋がっている。**疑う先は**ワークフロー側＝GitHubが scheduled run を作るかどうか**だけに絞られた。

- **受信箱で探す目印**＝件名が普段の `📮 AI更新ダイジェスト` ではなく **`⚠️ AIトラッカー: 1件のソースに問題があります`**。
  `pending_minor` が0件のとき `build_subject` が警告側の文面に切り替わるため
- ⚠️ ダイジェストは cron 上 07:22 JST だが**実測では8〜9時台にずれる**（下記「schedule は定刻に来ない」）。
  07:41 時点で未発火だったが異常ではない。**9時台まで来なければ、ワークフローの run が作られたかを Actions で見る**
- 📌 **次に効いてくる警告**＝`openrouter-xai` が **2026-08-08 に30日到達**。x.ai は 403 で直取得できず
  OpenRouter が唯一の実測済みルートなので、ここが鳴ったら「xAIが出していない」のか「経路も静かになった」のかの切り分けが要る

### 1. ✅ Enforce HTTPS 設定済み（2026-08-07・運営者が実施）

`https_enforced: True`。実測で `http://ai-tsukaikata.com/` と `http://www...` の
両方が **301 → https://** を返すことを確認済み（設定前は200のまま平文だった）。

### 2. ✅ Search Console 登録済み（2026-08-11 20:1x・運営者が実施）

**ドメインプロパティ**（`ai-tsukaikata.com`）で登録。検証は**DNSのTXTレコード**。
サイトマップ `sitemap.xml` を送信済みで、**44ページ検出**＝こちらのビルド結果と完全一致。

- TXTの値は `google-site-verification=` で始まる。⚠️ **絶対に消さないこと**（Googleは定期的に
  再確認する。消すと所有権が外れてデータが見られなくなる）
- ドメインプロパティにしたので `www` と `http` も1つにまとまる（URLプレフィックスなら3つ必要だった）

🔑 **エックスドメインの権威DNSは3台あり、反映がずれる**（2026-08-11 実測）。
TXT追加の約20分後、**ns2 だけに反映され ns1/ns3 は未反映**だった。Cloudflare(1.1.1.1)は
見えるのに Google(8.8.8.8) では見えない、という状態になる。
⚠️ **1台だけ引いて「反映されていない」と判断しないこと**（実際に一度そう誤判定した）。
確かめるときは3台とも引く:

```
foreach ($ns in @("ns1.xdomain.ne.jp","ns2.xdomain.ne.jp","ns3.xdomain.ne.jp")) {
  Resolve-DnsName -Name ai-tsukaikata.com -Type TXT -Server $ns }
```

📌 **DNSを触る前に、いまの姿を控えること。**Aレコード4件（`185.199.108-111.153`）＋
`www` の CNAME（`invest-ai-info.github.io`）。これがあれば事故っても復旧できる。

**次にやること（データが溜まってから・進化ループの輪3）:**
検索クエリは**1〜2週間後**から見えるようになる。溜まったら「検索パフォーマンス」→
クエリをCSVエクスポート → キューと既存記事に突合して題材を決める。
⭐ **「表示されているのにクリックされていない記事」は題材ではなくタイトルの問題**なので、
直し方が別になる（ここは今まで一度も測れていない領域）。

### 3. ✅ 記事20本を達成（2026-08-07）— 次は再申請

**レシピ10本＋ツール2本**（2026-08-05 にレシピ3本・ツール2本を追加）。ネタは `content/_ideas.md`。

⚠️ 数え方に注意。about / privacy は固定ページで審査の実質的な対象にならないので、**レシピ記事の本数で数える**こと。レシピは20本まであと10本。

2026-08-02 にサイトの方針が変わり、**未実行の調査記事も書けるようになった**（「動かしたものだけを書く」の約束を撤去）。全ツールを実運用するのは時間的に不可能なため。ただし体験談の語り口で書かないこと（about の「書き方のルール1」）。

### 4. ファビコンが無い

`base.html` にアイコンの `link` が1つも無く、タブに白紙アイコンが出ている。`og:image` も無いのでSNSに貼っても絵が出ない。

案は6つ出して保留中だったが、**2026-08-06 に有力候補が変わった**＝ヒーロー（`static/images/hero.svg`）の**小ロボットの顔**を流用する案。サイトの世界観と一致し、16pxでも目・アンテナで判別できる。SVGファビコンは新しめのブラウザで動くが、**Safari 用に PNG/ICO が要る**（この環境に画像ラスタライザは無いので、純Pythonで小さなPNGを書くか、オーナーが変換サイトを使うかの判断が要る）。

---

## 公開までに詰まったこと（再発したら思い出す）

**GitHubのDNSチェックが1時間「進行中」のまま止まった。**DNS設定自体は最初から正しかった（権威DNS・公開DNSとも Aレコード4件）のに、GitHub側が古い結果を掴んでいた。

**直し方: Custom domain をいったん空にして Save → 入れ直して Save。** これで `DNS check successful` に変わり、証明書が発行された。

- 入力欄を空にして保存しても、値は `ai-tsukaikata.com` に戻る。ビルド成果物の `CNAME` から復元されるため。**これは正常**
- DNSは**エックスドメイン**（`ns1.xdomain.ne.jp`）で管理。Aレコード4件（ホスト名は空欄）＋ `www` の CNAME
- **push が Push Protection で拒否された。**テストのダミートークンが Slack トークン形式に一致したため。GitHubのUIで「テストで使用」として許可して解決。以後、ダミーは接頭辞と本体を分けて組み立てる（`tests/test_validate.py` の `FAKE_TOKENS`）

---

---

## 環境

| | |
|---|---|
| 作業フォルダ | `C:\Users\info0\ai-tsukaikata`（**OneDrive外**。同期が `.git` を壊すため） |
| GitHub | `invest-ai-info/ai-tsukaikata`（public） |
| ドメイン | `ai-tsukaikata.com`（2026-08-01 取得済み。GitHub Pages への接続は2段目の作業） |
| Python | 3.12.10。依存は `requirements.txt`（feedparser / PyYAML / pytest / Jinja2 / Markdown） |
| テスト | `python -m pytest -q` → **408 passed**（2026-08-08 実測） |
| 記事の数字の照合 | `python tools/check_numbers.py <記事>` → 出典に無い数字を報告（ビルドには組み込まない） |
| 図の生成 | `python tools/make_figures.py` → `static/images/` に再生成。**座標は計算で出す** |

**Windows の注意:** ローカルで `python -m tracker.run` を叩くときは `$env:PYTHONUTF8=1` を付ける。コンソールが cp932 なので、日本語の出力で `UnicodeEncodeError` になることがある。

**marketwatch-ai とは運用が違う。** あちらは OneDrive 内・git リポジトリではなく API 同期で「SYNC禁忌」ルールがある。こちらは**普通の git リポジトリ**なので、その手の制約は一切ない。混同しないこと。

---

## 1段目（稼働中のトラッカー）

### 動いているもの

- cron 上は毎時 `:17` に `--mode check` — 13ソースを取得し、major は即メール、minor は溜める

#### ⚠️ schedule は定刻に来ない（2026-08-02 実測）

**「毎時」は設計値であって実態ではない。**schedule 実行11回の間隔を測ったところ:

| | |
|---|---|
| 平均間隔 | **112分**（設計は60分） |
| 最短 / 最長 | 61分 / **224分** |
| 定刻（70分以内）だった回 | **10回中3回** |

朝は61分間隔で綺麗に回るが、日中から夕方にかけて3〜4時間飛ぶ。`skipped` も `cancelled` も記録はゼロで、**GitHubが実行自体を作っていない**。1日24回のつもりが実際は13回程度。

- **帰結: 「major は即メール」は実際には「最大3〜4時間後」。**説明文を書くときはこの前提で
- **実行が来ないことを障害と誤診しないこと。**2時間空くのは正常
- 定刻性が必要になったら、外部スケジューラ（cron-job.org 等）から `workflow_dispatch` を叩く構成に変える
- ⚠️ 状態確認のためにGitHub APIを短間隔で叩かないこと。**未認証は60回/時**で、45秒間隔にして403で焼き切った。3分間隔（20回/時）なら安全
- 毎朝 `07:22 JST` に `--mode digest` — 溜まった minor と死活警告を1通で送る
- 状態は `data/tracker/seen.json`（git にコミットされる。**生成物ではなく状態**）

### 設計上、触ると壊れるところ

- **`--mode check` は `--mode bootstrap` の後でしか走らせない。** `main()` にガードがあり、状態ファイルが無ければ exit 1 で止まる。これが無いと初回に1000通超のメールが飛ぶ（OpenAI のフィードだけで1105件）
- **ワークフローのコミットステップに `if: always()` を付けない。** 送信失敗時に状態をコミットしないことで「既読にしたのに届いていない」を防いでいる。付けると送信失敗が永久のデータ損失になる
- **`load_state` が壊れたファイルで例外を投げるのを「握り潰して空状態にフォールバック」しない。** 全 uid が新着に戻り、過去の major まで再送されて溢れる。落ちて人間に気づかせるのが正しい
- **`record_result` には生の取得件数を渡す。** `select_unseen` 後の新着件数を渡すと、更新の少ないソースが3時間で死亡扱いになる
- **`notify.py` の送信は `store.save_state` より前に呼ぶ。** 逆にすると送信失敗時に更新が失われる

### 既知の課題（調整期間の主題）

- **HuggingFace 系の major 率が高い**（実測: DeepSeek 87% / Kimi 89% / GLM 77% / Qwen 63%）。研究成果物リポジトリ（`eagle3_*`、`SAE-Res-*` 等）や同日の `-Base`/`-Instruct` 兄弟が多いため。**1時間に1通が上限**なので暴走はしないが、うるさければ派生除外ルールを足すか OpenRouter 経由（実際に提供開始されたモデルだけ）へ置き換える
- **`hf-zhipu` が47日更新なし**。ただし OpenRouter の `z-ai` も同じ `glm-5.2`（2026-06-17）が最新なので、**差し替えても1日も新しくならない**。ソースの問題ではなく Zhipu 自体が出していない。停止警告に任せて実態を見る

### 解決済み: 更新が止まったソースを検出できるようになった（2026-08-02）

`record_result` が見ていたのは取得の成否だけだったので、**凍ったフィードは古い記事を返し続けて永久に健康判定**だった。実測で `hf-xai` 344日・`qwen-blog` 313日・Moonshot 50日・Zhipu 47日・Qwen 37日が止まっていたのに `dead_sources` は0件。

- ソースごとに観測した最新の公開日を `state["latest"]` に記録し、`STALE_DAYS`（30日）を超えたら `stale_sources` が返す
- ダイジェストでは **`dead`（取得できない）と `stale`（取得はできるが中身が止まっている）を別枠**にする。打つ手が違うため
- **日付は巻き戻さない。** フィードが一時的に古い記事だけ返した回に上書きすると、止まっていないソースが突然「停止」に見える
- 記録の無いソースは警告しない（「止まっている」ではなく「まだ分からない」）。既存の `seen.json` に `latest` が無くても初回に誤警告しない
- 閾値を14日にしないこと。ELYZA や PFN の通常の静かな期間まで拾って警告欄が読まれなくなる

### 📦 ニュースをサイトに載せる（2026-08-04 着手・段階1完了）

オーナーの要望＝**①自分がいち早く知りたい ②ついでに集客**。「自分が欲しいものは他の人も欲しい」。

| 段 | 中身 | 状態 |
|---|---|---|
| **1** | 集めた記事を `data/tracker/news.json` に貯める | ✅ **完了**（179件で初期化済み） |
| **2** | 日本語で要約（お知らせは3行 / モデルは畳む） | ✅ **稼働中**（120件・埋め草0件） |
| **3** | サイトに一覧ページ（バッジ・鮮度・SEO） | ⬜ **未着手**（news.json は186件たまっている） |
| **4** | 深掘り記事の自動生成（キューファイル） | ✅ **稼働中**（2本公開・2本は書かずに停止） |

**要約の粒度は「ソースの種類」で分ける**（`major`/`minor` では分けない）。HuggingFace系は major 率が 63〜89% なので、
major で絞っても37件が25件になるだけで絞り込みにならない。**新しいモデルが出たことは「見る」もの、発表は「読む」もの**：

- **お知らせ系**（`anthropic_news` / OpenAI news / 各社ブログ）→ **1件ずつ3行の日本語要約**
- **モデル系**（huggingface / openrouter / github_releases）→ **ベンダー単位で1〜2行＋件数**。`-Base`/`-Instruct` 等の
  兄弟は畳む（`classify.is_variant_model` に `-instruct` は**未登録**なので足す）

⚠️ **「1日37件」は毎日ではない。**日別の実測は 8/2=37件・8/3=3件・8/4=実質3件で、**37件の日は 8/2 20:02 の
ソース入れ替えコミット `c9004e6` と一致**＝新ソースの過去分が流れ込んだ事故。普段は1日3件程度。それでも
ベンダーがモデル群を一気に出す日は本当にあるので、まとめる設計自体は正しい。

**段階2の実装**（`tracker/summarize.py`・`--mode summarize`）: `GEMINI_API_KEY` は 2026-08-04 に登録済み。

- **SDKは使わず REST を urllib で直叩き**。このプロジェクトは依存5個で `fetch.py` も urllib 直叩きの流儀。
  モデルは MarketWatch 実績順のフォールバック（`gemini-2.5-flash` → `2.0-flash` → `2.5-flash-lite`）。
  キーは**ヘッダ**（`x-goog-api-key`）で渡す＝URLクエリに載せない
- **要約するのは「お知らせ」だけ**（`rss` / `anthropic_news`）。モデル系は名前と件数で足りるので回さない
- **メール送信とは別の実行**にしてある。要約は派生データで、Geminiが落ちた日にメールまで止まるのは筋が悪い。
  ⚠️ **キー未設定でも exit 0**・ワークフロー側も `continue-on-error: true`＝**後続の既読コミットを絶対に止めない**
  （止まると送信済みの記事が未読に戻って再送される）
- 歯止め＝1回60件・8件ずつ・要約は180字で切る。頼んでいないuidの応答は捨てる（別記事に他人の要約が付くため）
- 生成物には `summary_source: "gemini"` を残す＝機械生成である旨の明示

⚠️ **本番のAPI呼び出しはまだ一度も通っていない。**キーがGitHub側にしか無く、ローカルで実弾検証ができないため。
**確認方法＝ログではなく `data/tracker/news.json` を見る**：`summary_ja` を持つ項目が増えていれば成功。
初回の対象は133件で、1回の実行で最大60件なので**3回の実行で追いつく**。

**段階4は既存方針にそのまま乗る**＝「体験を主張しない調査記事（『こう書かれています』）は全自動で公開してよい」（下記
「自動公開の設計方針」③）。起動は**キューファイル方式**（`content/_deepdive_queue.md` にURLを1行足す＝スマホから3タップ）。
⚠️**出典を実際に読ませること。**見出しと短い要約だけで長文を書かせると作り話が混ざる。

**ルーティン**＝`ai-tsukaikata-deepdive`（`trig_016PgcvDcqv3FtTsMw5RfBwj`・毎日08:00 JST・model=opus-5）。
初回は公開せず `content/_draft-<slug>.md` に下書きを書く。**ビルドの除外は「ファイル名が `_` で始まるもの」**
（`content.py` の `path.name.startswith("_")`＝**ディレクトリ名ではない**）ので、この命名である限りサイトには出ない。

🚨 **到達できるかどうかは、必ずクラウド側で測ること。手元のPCで測った結果は移らない**（2026-08-05 実例）。
私が手元から `openai.com/index/...` を叩いて 200・505KB を得たので「到達できる」と待ち行列に書いたが、
翌日クラウド側では **`cf-mitigated: challenge` の403**だった。**IPが違えば bot 判定の結果が変わる。**
- 遮断には**2種類ある**。切り分けないと直し方を間違える:
  - `CONNECT tunnel failed, response 403` ＝**経路（環境の許可リスト）**。ドメインを足せば直る
  - `cf-mitigated: challenge` などの応答ヘッダ付き403 ＝**先方の bot 判定**。許可リストに足しても直らない
  - 見分け方＝**同じドメインの別パスが通るか**。`openai.com/news/rss.xml` は 200 なのに HTMLページは 403 だった
- **UA偽装での迂回はしない**（x.ai と同じ方針）。読めないものは「読めなかった」と書いて止まる

🚨 **クラウドルーティンは既定では各社の発表ページを読めない**（2026-08-04 実測・次に同じことをやる人が必ず踏む）。
環境のネットワークは既定が **Trusted**＝許可リスト方式で、GitHub とパッケージレジストリと `docs.claude.com` /
`claude.ai` は入っているが **`www.anthropic.com` も `openai.com` も入っていない**。初回実行は
`Host not in allowlist: www.anthropic.com` の403で停止した（プロキシ迂回でも同じ403＝先方のbotブロックではない）。

- 直し方＝claude.ai/code の**メッセージ入力欄のすぐ上にある雲アイコン**から環境の歯車を開き、
  Network access を **Custom** にして各社ドメインを追加。⚠️**「Also include default list of common package
  managers」に必ずチェック**（外すと pip も GitHub も届かず、この環境を共有する MarketWatch の18本が全部壊れる）
- ⚠️ 公式ドキュメントに**「専用の設定ページも直接URLも無い」**と明記されている。セレクタからしか開けない
- ⚠️ 許可ホストを変えると**環境キャッシュが再構築**される＝次の1回だけ起動が遅い
- 📌 **ルーティンの止まり方は正しかった**＝取得できないことを待ち行列に `- [!]` で記録し、
  「出典を読めないまま二次情報で書かない」と明記して終了。下書きは作らなかった。この挙動は維持すること

**AdSenseは設計判断の理由にしない**（2026-08-04 オーナー判断）＝単価はA8のほうが高く、審査を気にして面白くない
サイトを作るのは本末転倒。集約ページのAdSenseリスクを理由に機能を削らない。

### ソース構成（14件）

`tracker/sources.yml` が単一ソース。追加はこのファイルに数行足すだけでコード変更不要（新しい種別を足すときだけ `fetch.py` に parser が要る）。

種別は5つ: `rss` / `github_releases` / `huggingface`（`org` 指定） / `openrouter`（`org` = ベンダー接頭辞） / `anthropic_news`。

⚠️ **ソースを足したら、必ず「現時点の記事を既読にする」処理を先に走らせること。** 足しただけで次の毎時実行を迎えると、
そのソースの全件が新着扱いになり過去の告知がまとめて即時メールで飛ぶ（`anthropic-news` 追加時の実測で、6〜7月の
`Introducing Claude Opus 5` 等 **major 8件**が飛ぶ寸前だった）。`--mode bootstrap` は **使えない**——`pending_minor` を
空にするので、溜まっている他ソースの minor を捨ててしまう。1ソースだけ `record_result` + `record_latest` + `mark_seen`
して保存する。

### Anthropic だけソースが2階建てになっている理由（2026-08-04）

**公式news は RSS を出していない**（`/rss.xml`・`/news/rss.xml` とも404を再実測）。旧方針の「**releases 2本で実質的に
カバーする**」は**誤りだった**。料金改定・利用上限の変更・**モデルの提供終了**は GitHub のリリースには一切現れず、
2か月以上まったく拾えていなかった。

- ページ自体は**素のUAで 200**（403ではない）＝UA偽装なしで取得できる
- 抽出は**埋め込みの RSCペイロード**（Next.js + Sanity CMS）から。`publishedOn` / `slug` / `title` が隣り合っている
- ⚠️ **CSSのクラス名では拾わない。**実物は CSS Modules のハッシュ付き（`PublicationList-module-scss-module__KxYrHG__date`）で
  **ビルドのたびに変わる**＝次のデプロイで静かに0件になる。テストにこの罠を踏む回帰ケースを入れてある
- **1件も取れないときは例外を投げる**（空リストを返さない）。空リストだと「取得できたが中身が無い」と区別がつかず、
  静かに0件を返し続ける壊れ方になる。例外は `fetch_source` が握って死活記録に落とし、3回続けばダイジェストに出る

### ⚠️ Claude Code の更新は、構造上ぜったいに即時メールにならない（2026-08-04 実測）

これは**不具合ではなく意図した状態**。直そうとする前にここを読むこと。

- `github_releases` が major になる道は2本だけ（①タイトルに発表語 ②patch番号が0）。直近**100リリース**
  （v2.1.98 / 4月9日 〜 v2.1.221 / 8月4日）で、発表語 **0/100**・patch=0 **0/100**。タイトルが `v2.1.221` という
  素のバージョン文字列だからで、**100%が minor**
- 対照的に `anthropic-sdk` は patch=0 が実際に出る（v0.118.0 / v0.119.0 / v0.120.0 が major）＝**ヒューリスティック自体は
  正しく、Claude Code のバージョンの振り方と噛み合っていないだけ**
- そもそも**トラッカーは CLI の更新通知に勝てない。**Claude Code の通知は npm 公開に反応するが、トラッカーが見る
  GitHub リリースは**直近30件で 30/30 npm より遅い**（最小23分 / **中央値178分** / 最大739分）
- → major にすると「既に知っていることを月30通、遅れて届ける」メールになる。**やらない。**必要なら
  `sources.yml` の `claude-code` を npm registry 参照に変える案があるが、効果はダイジェストの鮮度だけ

⚠️ **HuggingFace の org 名は大文字小文字が効く。** `moonshotai`・`zai-org` は動くが `MoonshotAI`・`THUDM` は0件を返す。

⚠️ **OpenRouter の接頭辞は推測で書かない。** Zhipu は `zhipu` ではなく **`z-ai`**。間違えると例外もエラーも出ず、静かに0件を返し続ける。追加前に `https://openrouter.ai/api/v1/models` を実際に叩いて接頭辞を確認すること。実測済み: `x-ai` / `qwen` / `moonshotai` / `z-ai` / `deepseek`。

⚠️ **x.ai は 403 で bot ブロックされている。User-Agent の偽装で迂回しない。** OpenRouter 経由が唯一の実測済みルート。

**2026-08-02 の入れ替え。** HuggingFace の org フィードは研究成果物が混ざるうえ提供開始と連動しない。実測で Qwen 37日 / Moonshot 50日の途絶に対し OpenRouter は 6日 / 17日だったため差し替えた（`openrouter-qwen`・`openrouter-moonshot`）。344日止まっていた `hf-xai` と313日の `qwen-blog` は削除。15件→13件、停止警告5件→1件。`hf-deepseek` は HuggingFace 側が生きている（2日）ので触っていない。

---

## 2段目（サイト本体・`site` ブランチ）

### できているもの

| | |
|---|---|
| 生成 | `python -m src.build` → `build/` に15ファイル。`build/` は `.gitignore` 済み |
| プレビュー | `ai-tsukaikata-preview`（`http://localhost:8791`）。設定は `.claude/launch.json` |
| 記事 | レシピ8本 + about / privacy。5本は実運用中の自動化から、3本（`verify-before-report`・`limit-what-ai-touches`・`who-does-what`）はその5本で効いた指示文の集約 |
| 図 | `static/images/` に18枚（手描きSVG）。記事に `<figure>` で埋め込み。集約系の2本は図なし |
| 記事の型 | **プロンプト中心**（2026-08-02 改訂）。プロンプト100個・コードブロック2個 |
| コピー | `static/js/copy.js` が指示文にコピーボタンを足す。JSが無くても指示文は読める |
| 配信 | `.github/workflows/build.yml` — push で テスト → ビルド → Pages |

### 記事の書き方（2026-08-02 に方針変更）

**コードではなくプロンプトを主役にする。**初版はPythonとYAMLが並んでいて、読者定義（「Excelマクロは触ったことがある程度の会社員」）と矛盾していた。運営者自身がコードを手で書いていない以上、公開する資産は「頼んだ言葉」のほう。

5ブロック構成のうち3番と4番が変わった。

1. これで何ができるか
2. 前提（**プログラミング不要**を明記）
3. **AIへの頼み方** — 指示文＋「なぜこの言い方か」
4. **うまくいかないときの言い直し方**
5. 応用・次の一手

**コードを残す基準:** 読者が自分で打つものは残す。AIが書くものは載せない。

指示文は生HTMLで書く。**Markdownの引用（`>`）は使わない**（コピーしたとき原文どおりになるように）。

```html
<div class="prompt">指示文をそのまま書く。
改行はそのまま出る。</div>
```

**「なぜこの言い方か」を必ず添える。**指示文だけ並べても真似できない。効く理由（「実際に開いて確認して」を付けないと存在しないURLを書いてくる、など）が本体。

⚠️ **指示文の中にタグや `**` を書かない。** 生HTMLなので変換されず、`**` は画面にそのまま出るし、タグはコピーしたときに混ざる。`validate.py` が検出してビルドを止める。

### 見出し（区切り）の書き方

h2 は帯＋アイコンで「ここで話題が変わる」ことを見せる。アイコンは class で選ぶ。

```markdown
## これで何ができるか {: .what }      🎯
## 前提 {: .need }                     📋
## AIへの頼み方 {: .ask }              💬
## うまくいかないときの言い直し方 {: .fix }   🛠️
## 応用・次の一手 {: .next }           🚀
```

「言い直し方」の節の中の h3 だけ `{: .trouble }` を付ける（赤い縦線＋「？」になる）。

- **class を付け忘れても崩れない。**既定のアイコン `◆` が出る（固定ページはこれ）
- 絵文字は**異体字セレクタ付き**で書く。`🛠` は付けないとモノクロの記号になる
- ✅ **付け忘れと誤字は `validate.py` がビルドで止める**（2026-08-02〜）。ブラウザで数を突き合わせる手作業は不要になった。判定は「h2に1つでもclassが付いていれば、全部に必要」＝固定ページ（全部無し）は通り、混在だけが落ちる

### マーカー（色）の使い方

**読み飛ばす人がマーカー部分だけ拾えば筋が通る**ように引く。文字色は変えず、下半分に色を敷く（コントラストが落ちないため）。太字も併用する（色の見分けが苦手な人のため）。

```html
<mark>そこだけ読めば分かる一文</mark>
<mark class="warn">やると事故ること</mark>
```

- **1記事あたり13個が上限。**増やすと目印にならなくなる
- **赤（`warn`）は「やると事故る」だけ。**多用すると効かなくなる。上限5個
- ✅ **上限2つは `validate.py` がビルドで強制する**（2026-08-02〜）。下限は課していない。短い記事で永久に消えない偽陽性になり、検査全体の信用を落とすため
- 引いたら、**マーカーだけ順に読んで意味が通るか**を確認する。ここは機械にできないので人間が読む。「後半が本体です」のような**次の文への矢印にマーカーを引かない**——単体で意味が取れる文にだけ引く

### 🆕 図は既定で入れる（2026-08-04 オーナー指示）

**「文字が多いと目が疲れる。グラフとか図解が欲しい。もう少し多めに。内容の濃度も必要だが見やすさを改善してほしい。
見やすい・わかりやすい・便利を常に自立進化させていってほしい」**——深掘り記事1本目（7,420字・表3つ・散文中心）を
公開した直後の指摘。**正確さを追うと文字が増えて読まれなくなる**という、この種の記事の構造的な弱点。

- **数値比較の記事はグラフ必須。図は2枚以上**。深掘りルーティンのプロンプトに反映済み
- **座標は計算で出す**＝`tools/make_figures.py`（手描きしない。実際 `changed_chart` で副題を枠の上辺が
  貫いてビルドが止まった＝目視なら見落とす崩れ）
- 生成 → `python -m src.build` の崩れ検査 → ブラウザの `getBBox()` で厳密計測、の2段構え（下記）
- ⚠️ **一度の手直しで終わらせない。**次回から自動で効く場所（生成スクリプト・ルーティンのプロンプト・検査）へ落とす

### 図の足し方

`static/images/` にSVGを置き、記事に生HTMLで書くだけ。

```html
<figure class="figure">
<img src="/static/images/なにか.svg" alt="読み上げでも意味が通る説明">
<figcaption>キャプション</figcaption>
</figure>
```

- **参照先の実在と `alt` は `validate.py` が強制する。** 存在しない画像を貼るとビルドが落ちる
- SVGには `width`/`height`/`viewBox` を全部書く（無いとレイアウトシフトする）
- SVGに `<style>` を持たせ、`@media (prefers-color-scheme: dark)` を入れる。`<img>` 経由でもダークモードは効く。**外部CSSは当たらない**ので配色はSVGの中に持つ
- viewBox幅は720で揃えてある。CSSが `min-width: 600px` を効かせるので、スマホでは枠内で横スクロールする（縮めると図中の文字が5px相当になって読めない）

### 図の崩れは2段構えで検査する

**目視では見落とす。**実際に「見出しを枠線が貫く」「ラベルが枠から12pxはみ出る」を公開直前まで見逃した。

**① `src/figures.py`（ビルドで自動・誤検知しない）**

`python -m src.build` が全SVGを検査し、崩れがあれば止める。検査するのは3つ。

- 文字を横線が貫いていないか（**縦位置だけで判定できるので厳密**）
- 枠から上下にはみ出していないか（同上）
- 枠・画面から右にはみ出していないか（幅は推定なので**ありうる最小幅**で判定）

文字幅はブラウザ実測315要素から較正してある（推定は実測の1.00〜1.15倍。下限で判定するので**誤検知は起きない代わりに、数pxのはみ出しは見逃す**）。

⚠️ **枠とみなすのは線のある矩形だけ。**角を四角く見せる当て板（塗りだけ）は囲いではない。**当て板に線のあるクラスを使うと、その上辺が文字を貫く。**上記の不具合はこれが原因だった。

**② ブラウザでの厳密計測（図を足したら手で1回）**

`getBBox()` で実寸を測る。①が見逃す数pxのはみ出しまで拾える（実際に3pxと22pxを検出した）。Claude に「図の厳密計測をして」と頼めば実行できる（`tools/measure_figures.js` の中身をブラウザのコンソールに流す）。図を追加・変更したときだけでよい。

計画書: `docs/superpowers/plans/2026-08-01-ai-tsukaikata-site.md`

### 設計上、触ると壊れるところ

- **`validate.py` のチェックを「うるさいから」で外さない。** これは機密漏れをコードで止める唯一の層で、記事化のたびに人間の注意力に頼らないためにある。誤検知が出たら**除外条件を精密にする**（例: 穴埋め語の許可リストに足す）。チェック自体を消さない
- **`build.py` は「全部通る or 何も出さない」。** 検証エラーが1件でもあれば `build/` に一切触らず exit 1 する。「エラーの記事だけ飛ばして続行」にしない。直った記事だけ新しく、壊れた記事だけ古い、という状態が公開に出る
- **`build.yml` の `paths:` フィルタを外さない。** トラッカーが `data/tracker/seen.json` を push するので、外すとその都度サイトがリビルドされる（実測で1日13回前後）。逆に `tracker/**` と `tests/**` は `paths:` に入っていないので、**トラッカー側だけの変更を push してもサイトは再デプロイされない**
- **`build/CNAME` の生成を消さない。** 生成HTMLをコミットしない方式では、artifact に CNAME が無いとデプロイのたびに独自ドメインが外れる
- **記事の内部リンクは実在チェックが効く。** まだ書いていない記事へリンクするとビルドが落ちる。書いてから貼る

### 公開の構成（2026-08-02 完了）

| | |
|---|---|
| 本番URL | https://ai-tsukaikata.com |
| ホスティング | GitHub Pages（Source = **GitHub Actions**。branch方式ではない） |
| DNS | エックスドメイン。Aレコード4件（ホスト名 空欄）＋ `www` は CNAME → `invest-ai-info.github.io` |
| 証明書 | GitHub が自動発行・自動更新 |
| 配信 | `main` への push（`content/` `templates/` `static/` `src/` `requirements.txt` の変更時）→ テスト → ビルド → Pages |

⚠️ **`build/CNAME` の生成を消さない。** 生成HTMLをコミットしない方式では、artifact に CNAME が無いとデプロイのたびに独自ドメインが外れる。

### 解決済み: 常駐ソフトとのファイル競合（2026-08-02）

トラッカーのテストが全実行の約3%で `PermissionError` で落ちていた。**Norton 360 が `os.replace` 直後のファイルを掴む競合**で、`save_state` に短いリトライを入れて解消（70回中0回・導入前は2回）。

- 最初 `load_state` 側だと見立てたが外れ。**実際は `save_state` の `os.replace` が本命**だった
- 壊れたJSONは従来どおり即座に落とす（リトライしない）

### 検証済みであること（2026-08-01〜02）

- 230 passed
- 実弾テスト: 機密（GitHubトークン・生メールアドレス・`C:\Users\` 絶対パス）とリンク切れ・広告表記漏れを含む記事を `content/` に置いてビルド → **5件すべてを一度に検出して中止し、`build/` は1バイトも変わらなかった**
- ローカルプレビューで、CSS適用・スマホ幅375pxで横スクロールなし・コードブロックが各自の枠内で横スクロール（12個中10個）・コンソールエラーなし を確認
- 図18枚すべてで、参照先が200・`viewBox`あり・`width`/`height`あり・ダークモード対応あり・`alt`は最短94文字 を確認
- 図中テキストの `getBBox()` を全18枚で実測し、viewBox からのはみ出しゼロ（当初2枚はみ出していたので2行に分割して修正済み）
- スマホ幅375pxでは図が枠内で横スクロールし、図中の最小文字は約9.6px。デスクトップ1280pxではスクロールなし
- 指示文のコピー: Markdown原文 → 生成HTML → コピーされる文字列が**14個すべて完全一致**。実クリックで本物のクリップボードに入ることも確認（`is-done` 付与を観測）
- JSを剥がした状態でも指示文が読めて選択できることを確認

### 次に書く記事

`content/_ideas.md` に置いてある。AdSense申請は**記事20本を超えてから**なので、レシピ記事であと12本（いま8本）。

**集約型は3本で打ち止め**（`verify-before-report`＝検証／`limit-what-ai-touches`＝範囲／`who-does-what`＝分担）。
既存記事に脇役として散らばっている指示文を集めて主役にする型で、材料が全部「実際に効いたもの」になるので速く書けて信用も高い。
ただし**4本目を探すと既存記事と重複する**。実際 `_ideas.md` の「見積もりを先に出させる」は書く前に材料を洗った段階で
`limit-what-ai-touches` の§2/§3と丸かぶりだと判明し、切り口を「自分の作業量」へ寄せて重複を回避した（＝`who-does-what`）。
**次に集約型を書くときは、先に既存記事の節見出しを全部並べて衝突を確かめること。**

残る「メールリスト」「このサイト自身の作り方」は新規に作る話なので、実際に作ってから書く。

### 自動公開の設計方針（2026-08-02 に議論・①のみ実装済み）

運営者から「ChatGPTと協力してループさせ、点数が基準に達したら自動公開できないか」という提案があった。**3層に分けて、①だけ実装した。**

**① 機械的検査＝コード（実装済み）**
LLMを使わない。確定的でぶれず無料。マーカー上限・見出しclass・機密・リンク・alt・指示文の混入。**初日に運営者側ではなく書き手（Claude）の公開済み記事の上限超過を検出した**ので、価値は実証済み。

**② LLM批評＝保留**
**別モデル（ChatGPT）を使うのは本質的に正しい。**同じモデルが書いて採点すると盲点がそのまま通るため。ただし出力は**点数ではなく指摘リスト**にする。ゲートは「重大な指摘0件」。

⚠️ **点数方式は採らない。**MarketWatch の B2信頼度スコアが、HIGH/MID/LOW で 42.8%/41.1%/43.2% と横一線になり「有害ですらなく無益」だった前例がある。過去データでは綺麗に単調だったものが本番で消えた。記事の点数も同じく87〜92点に張り付いて機能しなくなる。**点数は中身が隠れるのが最悪**で、「87点」からは何も分からないが「3節目に根拠がない」なら真偽が判定できる。

未着手の理由＝この環境に OpenAI へ届く経路が無い（`codex`・`openai`・`node` いずれも未インストール）。APIキー方式か手動コピペ方式かを運営者が選ぶ必要がある。**①がどれだけ拾うか数本ぶん見てから決める**方針。

**③ 人間のゲート＝残すが小さくする**
①も②も検査できないのは「**その指示文は本当にこの環境で効いたのか**」の1点だけ。これはサイトの価値そのもの。ただし確認項目がそこだけなら、通読ではなく数分で済む。

**全自動にできる線引き**: 体験を主張する記事は人間のゲートが要るが、**体験を主張しない調査記事（「こう書かれています」）は全自動で公開してよい**。運営者の体験を主張していないので、運営者が確認すべきものが無い。格言シリーズが自動公開できているのと同じ理屈。

**①が拾えなかったもの（②の守備範囲）**: どのマーカーを削るかの判断。「ここだけは注意してください」は数の上でも文法的にも正しいが、**単体では意味が取れない**。マーカーだけ通し読みして「次の文への矢印」を見抜くのは機械にはできない。

---

## 運用メモ

### 手動実行

```
https://github.com/invest-ai-info/ai-tsukaikata/actions
```
各ワークフローの「Run workflow」から。両方 `workflow_dispatch` 対応済み。

### GitHub 側の設定（済み）

- Workflow permissions = **Read and write**（これが読み取り専用だと `seen.json` の push が 403 で落ちる）
- Secrets = `GMAIL_USER` / `GMAIL_APP_PASSWORD`（`ALERT_RECIPIENT` は未設定＝送信元と同じ宛先に届く）

### 検証済みであること

2026-08-01 に end-to-end で実証済み。既読から Grok 2件を意図的に外して再検出させ、**メールが実際に届くこと**と **`github-actions[bot]` が `seen.json` をコミットできること**の両方を確認した（`ee3fbd6` → run #3 → `5e19d7d`）。

---

## 収益化の位置づけ

このサイトは [[project_monetization_strategy]] の「収益の第2の柱」。読者は**自動化したい非エンジニア**。

- AdSense 申請は**記事20本を超えてから**（不合格理由の1位はコンテンツ不足）
- AIツール分野は YMYL ではないので、投資サイトより審査は通りやすい
- アフィリエイトを入れたら「広告」または「PR」表記が法的に必須。サイト側の `validate.py` でコードから強制する設計になっている（2段目で実装）
