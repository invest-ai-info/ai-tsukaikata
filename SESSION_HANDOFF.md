# SESSION_HANDOFF — AIの使い方（ai-tsukaikata.com）

最終更新: 2026-09-22 JST（夜）

## 🚩 次のセッションは、ここから

### ✅ 2026-09-22 夜（続き）「ひどいものだけ」既存記事も直した — 追えない文118個を0に

オーナー「直した文章を一度見せてもらえませんか」→ 見本を出したうえで**「ひどいものだけ直す」**を選択。
対象＝**120字以上の文を持つ59本（118文）**と、**題の区切り記号が違う9本**。⚠️ **意味は変えず、並べ方だけ。**

| | 直す前 | 直した後 |
|---|---|---|
| 120字以上の文 | 118文（59本） | **0文** |
| いちばん長い文 | 249字 | **119字** |
| 1文の平均（59本） | 44.9字 | 42.0字 |
| 文の数（59本の合計） | 4,801 | 5,055（割ったぶん増える） |

- **直し方は「三つ以上並んでいるものを箇条書き・表にする」が主**（建議 Ⅲ-3 ウ）。
  文を割っただけの箇所もあるが、249字→109字のような大物はすべて列挙のリスト化で落ちた
- 🔒 **中身が変わっていないことを機械で確かめた**＝直す前（`609cbf4`）と照合して
  **数字（種類）・指示文の本文・見出し・外部URL が59本とも完全一致**。道具は使い捨て（scratchpad）
- **題の区切りを `——` に統一**＝`―`（U+2015）2本・前後に空白の `—` 7本を直した（固定ページ `start.md` も）。
  ⚠️ **URL（slug）は変えていない**ので既存のリンクは切れない。文言も記号以外は触っていない
- ⚠️ **題の前半24字超が11本残っている**（`long-report-checkable-facts-survive-position` の34字など）。
  これは `ERA` の線より前なのでビルドは止まらない。**直すなら言い回しを変える必要があり、意味が動く**＝
  「並べ方だけ直す」の範囲を超えるので手を付けていない。次にオーナーが決めること
- 途中で**確認担当の夜間コミットと1か所だけ衝突**（同じ段落の内部リンク文言を直していた）＝
  **両方取って解決**（担当のリンク文言＋こちらの文の分割）。他の記事は衝突なし

### ✅ 2026-09-22 夜 「文章とタイトルが分かりづらい」を測って、止める仕掛けにした

オーナー相談「タイトルも文章も分かりづらい。文章術やキャッチコピーの**書籍をリサーチして学習**できるか」。
**できる。ただし先に測った**ら、感覚ではなく数字に出ていた（地の文だけ。指示文・図・表・英語引用は除く）:

| | 古い20本（8/01〜8/12） | 直近20本（9/15〜9/22） |
|---|---|---|
| 1文の平均 | 28.3字 | **44.7字** |
| 60字以上の文 | 2% | **21%** |
| いちばん長い文 | 105字 | **249字** |

🔑 **犯人は「不誠実」ではなく「誠実」のほう。**「試行回数を本文に書く」「条件を省かない」を
守った結果、実測の条件が1文に流れ込んでいる。249字の文は「①…②…③…④…の4段階で、材料6本・
のべ38回」＝**列挙を1文に詰めたもの**。**削る話ではなく、並べ方で読めるようにする話。**

⚠️ **8/16 のリサーチ（行動経済学・キャッチコピー）は「クリックされるか」の研究**で、
**「読んで分かるか」は一度も調べていなかった**（だから上書きせず、節を分けて足した）。
今回の出どころ＝文化審議会建議「公用文作成の考え方」（令和4年1月7日）Ⅲ-3「文の書き方」ア〜ス。
本多勝一『日本語の作文技術』の「長い修飾語ほど先に」は建議クと同じ原則で、**建議のほうが
無料で全文を読めて出典に書ける**。決まりの本体は `content/_recipe_queue.md`「✍️ 文章の書き方」。

**3層で効かせた**（決まりを文章で書くだけだと、13〜16字の題と同じく静かに守られなくなる）:

| 層 | 見るもの | どうなるか |
|---|---|---|
| 書いた直後 | `python tools/check_readability.py <記事>` | 割合と長い文の実物（目安15%以下） |
| ビルド | `src/validate.py` | **120字以上の1文／題の区切りは `——`／題の前半24字**で止まる |
| 週次 | `check_freshness.py` の `readability_drift` | 直近2週間の記事で15%超なら鳴る |

- 📌 **既存記事は直さない**（オーナー選択）＝`src/readability.py` の `ERA`＝**2026-09-23 から**効く。
  🚨 線を引かずに週次で直近2週間を見たら**46本**鳴った＝直さないと決めたものが毎週並び、
  9/14 の死活警告と同じ「打つ手のない赤」になるところだった
- ⚠️ 深掘りの「落ちたら記事を削除」に例外＝**読みやすさで落ちたら直してから公開**
  （直し方が機械的で、出典や数字の正しさとは無関係。消すと正しい記事が体裁で消える）
- 🔑 **計測器を先に試した**＝最初の測り方は `https?://\S+` が `>）。` まで食べて2文を連結し、
  「395字の文」に見せていた。回帰テストを `tests/test_readability.py` に入れてある

🔎 **見るもの**＝**9/23 以降に公開される記事**（レシピ担当21:00・深掘り08:00）。
ビルドが読みやすさで止まったら、担当が直して公開するのが正しい動き。
2本め以降も止まり続けるようなら、閾値（120字・前半24字）が実態と合っていない疑い。
📌 直近20本のうち**12本**は120字以上の文を持っていた＝**最初のうちは実際に止まる**想定。

### ✅ 2026-09-22 週次の検査（Freshness Check）が4週赤だった件を診断・30件→5件に

オーナーが失敗メール（9/21分）を見せてくれた。8/31・9/7・9/14・9/21 と**4回連続で失敗**していた。
ログは要ログインなので手元で再現（30件）。原因4種類と直し方は CLAUDE.md
「🆕 週次の検査（Freshness Check）が4週続けて赤だった（2026-09-22 診断・修正）」。要点:

- **検査の誤検知8件**を直した（Google のヘルプの HEAD 404／Adobe の Akamai 403）。テスト 657 → 671 passed
- **リンクの貼り替え14件**（docs.claude.com → platform.claude.com ほか）。`check_numbers.py` で新ページに数字があることを照合
- **副業の節が `###` の日付見出しで切れて「0件」と誤報**していた（実際は3件）→ `####` に下げ、番人は切れていること自体を鳴らす。
  ネタ探し担当の補充2件に無かった `需要:` の行も補記（検索して要約の水準で書いた）
- **回転のすり抜け**（担当が自分で書いた `→保管:` の下の長い報告70件≈2,300行）→ `rotate_queue()` を直し、手で1回回した。
  `_recipe_queue.md` 4851→2482行・`_deepdive_queue.md` 967→784行。保管庫は `_recipe_queue_archive.md` の「2026-09-22 回転」
- **待ち行列の「散文」も回転の対象にした**（オーナー質問「中身を整理して必要のないものは削除できませんか」への答え）。
  実測＝待ち行列2,009行のうち**920行が散文**で、中身は `_earn_research.md`・`_writer_log.md`・`_hypothesis_queue.md` と
  重複していた（毎晩3〜4担当が読み直していた）。`rotate_archives.py` に `prose=True` を足し、**レシピキューだけ**
  ①日付つきの補充・研究パックの経緯（**未処理が1件でも残る節は動かさない**＝並び順の指示が混ざるため）
  ②担当の日誌（詰まったところ・仮説キューの在庫補充・源ごとの結果ほか。項目が無い節は節ごと）
  ③`## 処理済み` の日報（直近3日ぶんだけ残す）を保管庫へ。**項目・マーカー行・字下げの詳細・節の決まりは1行も動かさない**
  （実測で確認＝`- [ ]`20/`- [x]`140/`- [!]`22・字下げ501行が前後で同数、消えた943行はすべて保管庫にある）。
  結果＝`content/_recipe_queue.md` **2,482→1,570行**（約58k→36k トークン）。予算2500に対して930行の余裕ができたので、
  **予算を上げる必要はなくなった**（毎日04:43の回転が同じことを自動でやる）

🔎 **見るもの**＝9/28（月）の Freshness Check。下の5件が残るので**まだ赤**のはず。5件以外が出たら新しい件。
GitHub Actions からは Google のヘルプに GET で届くはず（手元では 200）。届かなければ IP の問題＝注記扱いを検討。

✅ **残り5件は「A」で全部片づけた**（2026-09-22 夕方・オーナー指示「続けてください。Aで進めてください」）:

| 件 | やったこと | 結果 |
|---|---|---|
| `_lessons.md` 106件（上限15） | 91件を既存14節＋★137の節に `####` で束ね直した（**中身は1文字も変えていない**）。その過程で**番号の重複5組**（★125・★176・★177・★178・★179）が見つかり、外から参照されていない側を ★194〜★198 に振り直した | **15節**（赤が消えた） |
| `_hypothesis_queue.md` 1,074行 | `rotate_hypotheses()` を追加＝📤変換済み・✅生存・❌棄却・🚫検定不能を保管庫へ。⏳未着手と🔬検証中は動かさない。跡地に索引1行（H番号・題・状態） | **207行**（予算900） |
| `_deepdive_queue.md` 967行 | `- [!]` のうち**印（`🛑 再試行対象外`）が付いた行だけ**回転。openai.com の6件に印を付けた。⚠️ **語句では自動判定しない**（実測7件はどれも「bot判定」と「許可リスト」の両方を含み、Sakana FIG は読めたのに型が合わない行だった） | **436行**（予算600） |
| `_topic_ideas.md` 1,625行 | 8/15〜8/24 の申し送り10節・37件を読んで閉じた（訂正済み12・状況が変わった15・別の場所で片づいた8・**残り2件**）。日次の1行29件は `## 日次の記録` に移した | **1,176行**（予算1500） |
| `_earn_research.md` 1,391行 | 予算を 1,200 → **1,500** に上げた（中身の約1,030行が指示・様式・台帳＝回転で減る部分ではないことを確かめたうえで。理由はコードのコメント） | 緑 |

✅ **ネタ探しの申し送りから引き継いだ2件は、両方とも片づけた**（オーナー指示「1は直してください。
2は必要があれば作ってください」・2026-09-22 夜）:

**1. 源③（Googleサジェスト）の欠陥4点を直した**（`1948f89`）。4つとも別の原因で、1つ直しても供給は増えない:

| 欠陥 | 直し方 |
|---|---|
| 週次の自動実行が `automate` 固定（候補4本とも automate・`earn` は 8/23 の手回し1本きり） | `bank_for_week()`＝ISO週番号で automate→earn→safety→work。`topics.yml` の既定を `auto` に。**次は 9/26（第39週）が work、10/10 が earn** |
| 中国語のサジェストが実質語に算入（`ai 自動化 接 案` は台湾華語の「案件を受ける」＝**副業語に見える**） | 現代日本語で使わない字＋見分けの付かない語（案例・排程・接案）。⚠️ 個・機・会・学・例は**入れない**（`個` で `ai 個人情報 取り扱い` を落とした）。既存候補4,845語で誤検知0・拾ったのは39語 |
| 61語目以降が保存されない | 畳んで全部残す（「…ほか N 語」を廃止） |
| 失敗しても exit 0 でファイルを書く | 2割超で書かずに exit 1。1件でも失敗したら表の前に⚠️ |

⚠️ **同じ束は同じ語しか返さない**ので、これで増えるのは4週に1回ぶん。
**種そのものの入れ替えは別の宿題**（`SEED_BANKS` のコメントに残した）。

**2. 改稿の枠を作った**（`717bbdf`）＝`content/_recipe_queue.md` の「### 🔧 改稿」節。**必要だった**——
改稿が誰のキューにも無く、8/18 と 9/11 の担当が「改稿として扱うほうが筋が通る」と気づきながら
「夜の担当の仕事ではない」ので足さずに終わっていた（止まり方は正しいのに、止まったことが渡らない）。
決まり＝**入れてよいのは「公開後に repo の中で測った数字が本文と食い違った」ときだけ**
（⚠️「証拠ファイルが無い」は理由にしない＝23本のやり直しになる）・1晩1本・副業3本のあと・
新規の本数に数えない・食い違った箇所だけ直す。

🚨 **1件目で分かった実害**＝`auto-health-check` の「90分のバッファ」は GitHub Actions が相手だと足りない。
**毎日のワークフロー5本・125回で、予定時刻からの遅れが90分超は 92/125（74%）**・最大718分
（`writer-watch` は最小でも270分）。**記事の助言どおりに作ると、記事自身が警告している「狼少年」になる。**
毎時トラッカーも測り直したら 2026-08-02 の倍以上ひどい（平均245分・**17日間の99区間すべてが2時間超**・
1日5〜7回）。CLAUDE.md の数字と帰結（「最大3〜4時間後」「2時間空くのは正常」）も直した。
🔎 **見るもの**＝9/26（土）10:23 JST の Topic Candidates が `work` の束で緑になるか（遅れて昼過ぎに動く）。

🟢 **手元で週次を通したら、まるごと緑になった**（`195本を見て、直すべきものはありませんでした`・exit 0）。
少なくとも 8/31 以降で初めて。⚠️ **クラウド側は bot 判定の結果だけ変わりうる**（注記なので失敗はしない）。
🔎 **見るもの**＝9/28（月）9:40 JST の Freshness Check が緑で終わるか。赤なら中身は新しい件。
🔎 **もう1つ見るもの**＝**9/22 21:00 のレシピ担当が足した教訓の番号**。台帳を束ね直したのが 21:28 で、
担当はそれより前の版を読んでいる可能性がある＝`### ★194` を足すと、振り直した ★194 とぶつかる。
ぶつかっていたら**後から足したほう（担当の分）を ★199 へ**振り直す（外からの参照が無い側を動かす）。
新しく足した `lesson_number_duplicates()` が次の週次で鳴るので、見落としはしない。

### ✅ 2026-09-21 トップ画像とロゴを差し替え・「視覚要素を多く」の決まり・記事内イラストの相談（ChatGPT連携は保留）

- **深掘りは 9/21 08:06 JST に動いた**＝9/20 に手で足した `introducing-chatgpt-images-2-5` の行で
  `content/tools/chatgpt-images-2-5.md` を公開（図3枚・要旨は RSS、数字は `developers.openai.com`）。
  発表ページが読めなくても振り替え経路で書ける、が実際に確かめられた
- **検索は本番で確認済み**（`?q=Gmail` 1件・`副業` 72件・`github actions` 4件・`メール` 15件＝Python の索引と一致）
- **トップの画像**を差し替え（`3ad28d2`・`hero-photo.jpg` と `og-image.jpg` は同じ絵・1200×686）。
  **ロゴ**（切り抜いたロボット）をサイト名の左に置き、favicon / apple-touch-icon も同じ絵にした（`3e0a9b5`）。
  どちらもオーナー提供の絵。古い絵は git の履歴にある
- **「記事を書くときに図解・画像・リスト・グラフなど視覚で見てわかるものを多く」**（オーナー指示）を
  CLAUDE.md「図は既定で入れる」・`_recipe_queue.md` 規則4・`_writer_sop.md`・auto-memory に書いた（`746e001`）。
  CLAUDE.md はクラウドのルーティンにも読み込まれるので全担当に届く。⚠️ ルーティンのプロンプト本体には
  まだ書き足していない（足すかはオーナー判断。深掘りのプロンプトは元から「図2枚以上」）
- **記事の中に絵（イラスト）を入れる相談** → 🛑 **ChatGPT との連携は保留（2026-09-21 オーナー判断）**。
  「プログラムから ChatGPT に頼む」には API キーが要ること（チャット画面の自動操作は規約違反で壊れやすく、
  クラウドからは chatgpt.com に届かない）を説明したうえでの判断。⚠️ **次のセッションで蒸し返さない。**
  再開の合図があったときの選択肢だけ残す:
  1. ChatGPT の「タスク」機能で毎朝作らせ、届いた絵を GitHub に `static/images/illust/<slug>.png` として置く（鍵不要）
  2. 既存の `GEMINI_API_KEY` で GitHub Actions が生成する（新しい鍵は不要・無料枠か課金かは実物で確認）
  3. OpenAI の API（新しい鍵が要る。GPT Image 2 の Medium で1枚 $0.041＝約6円）
  - **表示の仕組み（ファイルがあれば記事に出す）は未実装**。作るなら `render.py` で `static/images/illust/<slug>.*` の
    有無を見て `article.html` に差し込み、CSS は `.article-body .figure img` の `min-width: 600px` を継がない別クラスにする
  - Claude Design（Artifact の Design 型）は使える（デザインシステム未登録）が、作れるのは図解・ベクター調で、
    いただいた2枚のような絵柄の画像生成ではない

### ✅ 2026-09-20 稼ぎ方の研究パック10件を公開・ルーティン確認・深掘りの材料を補充

- **9/16〜9/18 の研究（A〜J）を記事10本にした**（レシピ6本・`tools/` 4本・図26枚・証拠111回）。
  9/20 に `23fdeef` でコミット→push→**Build & Deploy 成功・本番で 200 を確認**。
  推定の月収・年収は「本人の頑張り次第なので推定で」というオーナー指示どおり、根拠の数字（JILPT 等）に
  紐づけて書いた。作業の記録は `_writer_log.md`（9/18）・`_earn_research.md`（9/16・9/18）・`_lessons.md`（★176〜★181）
- **ルーティンは全部正常**（Actions・claude.ai の6本とも定刻に起動しコミットあり）。
  トラッカーの1〜3時間間隔・ダイジェストの約2時間遅れは CLAUDE.md の実測どおり
- **深掘りの材料切れ**＝未処理0件・保留は `openai.com` の bot 判定6件＋FIG（オーナー確認待ち）で、
  `news.json` の 9/18 以降に「読めるホストの major お知らせ」が無く自動追記が止まっていた。
  → `_deepdive_queue.md` に**手で3件**（ChatGPT Images 2.5・GPT-Live-1 in the API・Sakana Marlin 更新）を
  足した（`8d13086`）。OpenAI の2件は `developers.openai.com` の料金・仕様ページ（URL に `.md` を付けると
  Markdown で読める）へ出典を振り替える前提。到達性は手元で測った値＝担当がクラウド側で測り直す

- **サイト内検索を作った**（設計 `docs/superpowers/specs/2026-09-20-site-search-design.md`・計画
  `docs/superpowers/plans/2026-09-20-site-search.md`）。全ページのヘッダーに窓＋`/search/?q=`。索引は `search.json`
  （タイトル・説明文・タグ・見出し。予算 400KB をテストが見る）。運用メモは CLAUDE.md「🔎 サイト内検索」

🔎 **見るもの**（→ ✅ 9/21 に書けた。上の節）＝9/21 08:00 JST の深掘りが `introducing-chatgpt-images-2-5` の行で記事を書けたか
（発表ページは読めない前提。`developers.openai.com` だけで書けたか、止まったなら理由）。
FIG（`sakana.ai/frontier-intelligence-group/`）は**オーナーが見送りを決めるまで `[!]` のまま**。

⚠️ **残っている小さな件**（どれも壊れてはいない）:

- `_deepdive_queue.md` が **925行**で予算600行を超えている（9/20 の追記前から 878行）。中身の大半は
  `openai.com` の `[!]` 6件（bot 判定＝再試行対象外）。回転は `- [x]` しか移さないので、これらを
  保管庫へ移すかどうかはオーナー判断（週次の `check_freshness.py` が予算超過を毎週報告する）
- ~~既存記事の note のリンクが `www.help-note.com` へ転送されるようになった~~（→ 9/22 に3記事を貼り替えた）
- Adobe（`helpx.adobe.com`）はクラウドから**経路遮断**（`CONNECT tunnel failed, response 403`）＝週次の到達確認で
  赤くなりうる（→ 9/22: 週次側は Akamai の定型403を「確かめられない」の注記にした）。記事の数字は 9/16・9/18 に実ブラウザで確認済み（`docs/evidence/ai-stock-images-adobe-vs-pixta.md`）

### ✅ 2026-09-14 死活警告を直した（pfn-blog 廃止・日数表示・ソース別閾値）

ダイジェストの `pfn-blog: 98回連続で失敗 (404)` と停止警告3件（Moonshot 59日・ELYZA 53日・xAI 32日）の件。

- **pfn-blog はフィード廃止**（301 → `www.preferred.jp/ja/blog/tech/feed` で 404・新サイトに RSS 無し）→
  `sources.yml` から外した。失敗記録は次の `--mode check` で自動的に消える。
  🔎 **9/15 07:22 のダイジェストで警告欄が空になっているか見る**（新着が無ければ、そもそも送られない）
- 停止3件は先方が本当に出していない（正しい警告）。ただし実測の間隔が30日を超えるので
  `stale_days`（xAI 60・Moonshot 90・ELYZA 90）で個別に延ばした。⚠️ 確かめていないソースに付けない
- コード: `since`（失敗の始まり）→「N日間 取得できていません（M回連続）」、404 のヒント、`転送先=` の記録。
  テスト 625 → 636 passed。詳細は CLAUDE.md「解決済み: 死活警告が…（2026-09-14）」
- 📌 PFN を戻したければ `sitemap.xml` 起点の専用パーサ（未着手・オーナーの要望があれば）

### ✅ 2026-09-14 深掘りの材料切れを直した（読める会社の minor・再試行の決まり・相対URL）

オーナー質問「なぜ新しい記事が生み出せないのか」。**レシピは毎日1〜3本出ている（138本・サイト反映済み）。
止まっていたのは `tools/`（9/7 が最後）**。原因と対策は CLAUDE.md「🆕 深掘りの材料切れを直した（2026-09-14）」。

- コード: `deepdive_minor`（sources.yml・いまは deepmind-blog だけ）／`parse_feed` の相対URL補完／
  テスト 636 → 646 passed
- キュー: 冒頭に「再試行の決まり」（経路遮断だけ・新しい行から・3回まで）。Admin plugin は打ち止め
- 次に試される行＝Sakana の Fugu Max（`sakana.ai` は許可リスト待ち）

✅ **3つとも 9/14 夕方に片づいた:**

1. 許可リスト＝**オーナーが追加済み**（`sakana.ai` ほか。効いたかは 9/15 朝の深掘りの再試行で分かる）
2. 深掘りルーティンのプロンプト＝**API（`RemoteTrigger`）で差し替え済み**。手順1が「キューの『再試行の決まり』に
   従う」になった。差し替え後の本文を取得し直し、意図した文面と完全一致を確認済み。⚠️ 選び方をプロンプト側に書き戻さない
3. 源0＝`_hypothesis_queue.md` に**在庫の決まり**（登録した日は変換しない・`⏳` を1件残す）を置き、
   **H16** を `⏳` のまま登録した（種）。稼ぎ方研究担当の手順書（`_earn_research.md` 源⓪）はそこを指す。
   プロンプトは触っていない（手順書が単一ソースのため）

🔎 **見るもの**＝9/15 朝の深掘りが Sakana の行を再試行しているか（許可リストを足していれば記事になる）。
DeepMind の minor が次に出た時、`- [ ]` に `自動追記（minor・…）` で入るか。

📌 やっていないこと＝OpenAI の振替ルート（RSS＋developers.openai.com）での自動化。手で書けば書ける
（9/5 の GPT-6 Astra が実例）が、題材によっては薄くなるので判断はオーナーに残した。

### 🛑 2026-09-10 自前の性能比較は打ち切った（オーナー判断）

**「モデル名が名乗れないんだったら比較する意味がないのでやめましょう」。**

経緯＝APIは 9/7 に「セキュリティが不安」で却下 → チャット画面に貼る方式へ変更 →
ChatGPT で6件実測 → **画面にモデル選択欄が無く、どのモデルが答えたか分からない**
ことが判明。モデル比較の記事にならないため打ち切り。

- **やること＝モデル比較は公式の料金・仕様の数字だけで書く**（CLAUDE.md の決まりどおり）。
  実例が `content/tools/gpt-6-astra.md` にある
- 🛑 **自前計測を再開しようとする前に CLAUDE.md の「🛑 自前の性能比較はやらない」を読む。**
  APIもチャット画面も両方つぶした上での結論
- 📌 測ったデータは残してある（ChatGPT Plus・一時チャット・2026-09-10・名指し4/6）。
  ⚠️ モデル比較には使わないこと。「材料を1つ欠いて渡すと6回中2回は黙って埋める」
  という**モデル名の要らない主張**の材料としてなら使える
- 📌 `tools/model_bench.py` と `tests/test_model_bench.py` は**削除した**（用途が無くなったため。必要なら git の履歴から戻せる）
- 🔑 **教訓＝計測器のほうが壊れる。**実測のたびに欠陥が1つずつ出た
  （判定3件＋入力の文字落ち＋クリップボードの使い回し）。
  **照合の仕掛けを先に作っていなければ、全部そのまま「結果」になっていた**

### 🆕 2026-09-07 性能比較からAPIキーを外した（貼り付け方式に変更）

⚠️ **これは経緯の記録。この道具は 9/10 に削除した**（上の🛑の節を参照）。

オーナー指示「**各社のAPIを使うのはセキュリティの面で少し不安なので、使わずにできる比較で**」。

- `tools/model_bench.py` から**鍵と通信を丸ごと削除**した。`--emit` で課題6件を
  フォルダに書き出し、人がチャット画面に貼り、返りを `*.reply.txt` で保存 → `--score` で採点
- **判定コードは1文字も変えていない**＝物差しは同じ。テスト 652 → 654 passed
- 🔒 再発防止＝`test_module_never_touches_api_keys` /
  `test_module_makes_no_network_calls` が、鍵の名前・`os.environ`・`urllib` の再登場で落ちる

📌 **オーナーに伝えてある後片付け**: 2026-09-05 に作った `OPENAI_API_KEY` は
**OpenAI の管理画面で失効させる**（手元の環境変数と GitHub Secrets の削除は、その後）。
Anthropic の鍵は**作らないことにした**。

📌 **貼る準備は済んでいる**（`docs/evidence/_raw/bench/` に3フォルダ・各6件）:
`single-gap_gpt-6-astra_*` / `single-gap_claude-fable-5-1_*` / `single-gap_gemini-3-1-pro_*`。
各フォルダの `手順.md` が単一ソース。⚠️ **1件につき新しいチャット**（同じチャットで
続けると2件目から課題に気づいて全部当たる）。

⚠️ 2026-09-05 にAPIで測った GPT-6 Astra の 5/6 は、**チャット画面の結果と並べない**
（足回りが違う）。当該フォルダに `README.md` で明記した。


### 🆕 2026-09-05 ニュース側の深掘りをもう1本公開した（GPT-6 Astra）＋照合ツールのCDATA不具合を直した

オーナー指示「ChatGPT-6、アストラについて深掘り記事を書いて」。**この2つは同じもの**で、
OpenAI の **GPT-6 Astra**（API 名 `gpt-6-astra`）。

- **公開**: `content/tools/gpt-6-astra.md`（図3枚・出典4件すべて取得成功・`check_numbers.py` 17/17・
  pytest 625・build 140・ブラウザ厳密計測 0問題・マーカー13個/警告5個）
- 🔑 **記事の芯＝いちばん大きな変更は性能ではなく「聞き返して止まりやすくなった」こと。**
  OpenAI 自身が公式ガイド「Using GPT-6 Astra」でそう書き、**直すための指示文をそのまま公開している**。
  記事はその指示文を日本語に直して並べたもの＝このサイトの型（プロンプト中心）に完全に乗る題材だった
- 単価は GPT-5.6 Sol の **2.5倍**（入力 $10 / 出力 $50）。公式は「出力トークンが減るので1件あたりは
  安くなる」と書いているが、⚠️ **その数字は非公開**なので記事では比率を出していない

🚨 **openai.com は手元からも読めない**（2026-09-05 実測）＝`openai.com/index/path-to-astra` は
`Cf-Mitigated: challenge` 付きの 403。`help.openai.com` も 403。**先方の bot 判定で、UA偽装での
迂回はしない。**CLAUDE.md の振替ルート（① `openai.com/news/rss.xml` で要旨 ②
`developers.openai.com` で数字）は**両方とも 200 で生きていた**ので、記事はそれだけで書いた。
読めなかったページは記事の「出典一覧」に明記してある。

🔧 **`tools/check_numbers.py` の不具合を1つ直した（`strip_markup` を新設・テスト3本）。**
本文抽出が `<![CDATA[ ... ]]>` を**タグとして丸ごと落としていた**（中身に `>` が無いので
`<[^>]+>` から見ると1個のタグに見える）。そのため **RSS を出典にした記事の数字が全部消えて**、
実在する `40%` を「出典に無い」と誤報した。⚠️ **OpenAI は発表ページが読めず RSS が正規の
振替先**なので、ここが効かないと OpenAI 記事の照合はほぼ機能しない。直したうえで
既存の `claude-fable-5-1.md` でも 47/47 が通ることを確認済み。

⚠️ **スマホ幅の横スクロールは、この記事に限らずサイト全体で起きている**（375px で実測＝
gpt-6-astra 424px / claude-fable-5-1 399px / gemini-computer-use 421px に対し画面は360px）。
原因は**出典の長いURLと横に広い表**で、図は枠内に収まっている（設計どおり）。
CLAUDE.md の「スマホ幅375pxで横スクロールなし」は**いまは成立していない**。別件として登録済み。
### 🆕 2026-09-02 `anthropic_news` が `/news/` の外の目玉発表（featuredGridLink）も拾うようになった

**症状**＝Fable 5.1 / Mythos 5.1 の発表（2026-09-01・`/claude-fable-and-mythos-5-1`）が
`news.json` に0件・major メール無し・深掘りの自動追記にも乗らなかった。newsページ上では
`post`（`publishedOn`+`slug`）ではなく **`featuredGridLink`** というオブジェクトで1回だけ現れるため。

**直したこと**（`tracker/fetch.py` の `parse_anthropic_news`・テストは `tests/test_fetch.py` の
`grid_link` 4本・pytest **622 passed**）:

- `featuredGridLink`（`date` / `title` / `url` / `summary`）も拾う。URLは `https://www.anthropic.com` + `url`
  （`/news/` は付けない）。uid は URL のパスから作る（`/news/<slug>` なら post と同じ slug＝両方に出ても畳む・post 側が先勝ち）
- ⚠️ **実物は `{"_key":"…","_type":"featuredGridLink",…}` と `_type` の前に `_key` が付く。**
  タスクの写しどおり先頭の波括弧に `_type` を密着させて書いたら**本番ページで0件**だった（手元で実測して発見）。
  並び順に依存しない形に直してテストにも `_key` を入れてある。**CSSクラス名では拾わない・0件なら例外**は従来どおり
- 手元で本番ページを実際に読ませた結果＝30件中、`/news/` の外は **2件**:
  `/claude-fable-and-mythos-5-1`（2026-09-01・**major**）と `/features/making-of-claude-code`（2026-07-06・minor）

🚨 **次の毎時 `--mode check` で、この2件は「新着」として扱われる（意図してそうしている）。**

- Fable 5.1 は本物の major 発表で、オーナーが見たかったもの＝**1通だけメールが飛ぶのを容認**する
- `making-of-claude-code` は minor＝`pending_minor` に溜まり、翌朝のダイジェストに1行出るだけ
- **`--mode bootstrap` は走らせない**（`pending_minor` を空にする）。`seen.json` にも手を入れていない
- 深掘りの自動追記は**重複しない**＝`select_candidates` がキュー内のURLを見て飛ばし、Fable のURLは既に
  `- [x]`（公開済み `content/tools/claude-fable-5-1.md`）で並んでいることを `queued_urls` で確認済み

### 🆕 2026-09-02 ニュース側の深掘りを手元で1本公開した（Fable 5.1）

オーナー指示「AIアップデート側だけでなく、ニュースの方からも1件深掘りを。Fable 5.1 の性能や
Gemini の新モデルなど。毎日1件ニュースから選んで書けるか」。

- **公開**: `content/tools/claude-fable-5-1.md`（図3枚・出典6件・`check_numbers.py` 47/47・
  pytest 618・build 139・ブラウザ厳密計測 0問題）。記事の芯＝**単価は据え置き $10/$50、
  値下げはキャッシュ読み取り $1→$0.25 の1項目だけ。それで公式は「約25%（最大約45%）安い」**
- 🔑 **「毎日1件」は既にある**＝`ai-tsukaikata-deepdive`（毎朝 08:05 JST・**実機は sonnet-5**。
  ⚠️ CLAUDE.md の「model=opus-5」は実態と違う）。8/31 の枯渇対策以降、8/31・9/1 と連日 `tools/` を
  公開している。**止まるとしたら題材の供給側**（自動追記は `major` のお知らせだけ・1日3件・
  読めるホストのみ）
- 🚨 **Fable 5.1 の発表は `news.json` に入っていなかった。**URLが `/news/` の外
  （`/claude-fable-and-mythos-5-1`）で、RSC 上の型が `featuredGridLink`。`parse_anthropic_news` は
  `publishedOn`+`slug` の `post` しか拾わないので**目玉発表ほど落ちる**。→ **同日に直した**（上の節）
- Gemini の2件（Omni 1.1 Flash・3.5 Transcribe）は `minor` 判定で自動追記の対象外だったので、
  キューに**手動で `- [ ]` を2件追加**（到達性の注記つき）。未処理は 4件（reflect / agentic-video /
  omni-1-1 / transcribe）＝4日ぶんの在庫

### 🆕 2026-08-31 シリーズの枯渇を直した（オーナー指示「枯渇したままで終わらせない」）

**枯れていたのは3系統で、原因は3つとも別だった。**

| シリーズ | 状態（8/31 朝） | 原因 |
|---|---|---|
| 深掘り（`tools/`） | 自動公開が **8/21 で止まり10日間ゼロ**・未処理0/保留6 | 保留6件が**全部 openai.com**（bot判定＝再試行の対象外）で、**枠を独占していた** |
| 連載「売る場所の原文から」 | **4/4 で完結**（8/26） | 後継が未着手 |
| 記事型「お金の流れ」 | 連載として繋がっていなかった（`series:` 未設定） | 各論YouTubeは 8/30 に公開済み＝**停止していなかった** |

🔑 **いちばんの発見＝源①は汲み尽くしていなかった。**台帳の2枚目（ココナラ）に
「🚨 クラウドワークスの87区分に1つも無いグループ＝生成AI活用・開発・制作」と書いてあり、
**その9区分が9件ともキューに0件・記事に0件**だった。写したのに変換していなかった。
⚠️ **「源①は汲み尽くした」は1枚目についてだけ正しい。**

**入れたもの（2026-08-31 のセッションで実装・コミット済み）:**

1. **`tracker/deepdive.py` に `UNREADABLE_HOSTS`**＝読めないと実測済みのホストに枠を取らせない。
   ⚠️ 載せるのは**先方の bot 判定だけ**（経路遮断は載せない＝許可リストで直った日に自動で戻したいため）。
   サブドメインも含めない（`developers.openai.com` は200）。飛ばした件数は `run.py` が必ず出す。
   あわせて**読めるホストの未処理を3件補充**（未処理 0→3件）
2. **枯渇を鳴らす番人3つ**（`tools/check_freshness.py`）
   - `deepdive_queue_shortage()`＝未処理0件で鳴る（**これまで番人が無かった**）
   - `hypothesis_stock_empty()`＝⏳未着手が0件で鳴る。あわせて仮説キューに状態 **`📤変換済み`** を追加
     ＝🔑 **このファイルは自分が空であることを言い表せなかった**のが本体
   - `earn_queue_shortage()`＝**`⏸`（いま着手できない）を在庫に数えない**
3. **新シリーズ「AIの仕事を受ける」**を副業節に開始（未変換9区分のうち3件を `- [ ]` 化・
   需要は検索結果の要約水準で取得日つき。残り6区分は非チェックボックスで一覧化）。
   あわせて**記事型「お金の流れ」の3本に `series:` を付けた**＝総論／各論X／各論YouTube
   （`youtube-payout-thresholds` は 8/30 に公開済みだった。連載ナビが tools/ と recipes/ をまたいで繋がる）

### ⏭ 次の一手（優先順・2026-08-31 更新）

1. 🔴 **`hypothesis_stock_empty()` は今いま鳴っている**＝源0（最優先の源）が実際に空。
   バックログ7件は「反証条件・試行回数が未設計」で止まっており、**設計する担当がどこにも
   定義されていない**（リポジトリ全文をgrepして確認）。⚠️ **オーナー判断が要る**＝
   誰がH番号を発行するか。決まるまで週次は赤いままになる（**真の赤なので消さないこと**）
2. **新シリーズ3件の一次情報を、研究担当（15:30）が実ブラウザで取る。**
   `coconala.com` は 2026-08-18 に到達実績があるが**それ以降は未測定**。
   🚨 まず `curl -sS -o /dev/null -w "%{http_code}"` で測り、結果をキューの行に書き足すこと
3. ✅ **`support.google.com` は開通済み**（8/30 21:00 に curl 200・許可リストが効いた）。
   🔑 **同じ日の15:30 は EGRESS_BLOCKED だった＝時刻で到達性が変わる。**
   ⚠️ **1回の測定で `⏸` を付けない・諦めない。**時間を置いて測り直すこと。
   残る `⏸` は「画像生成・note・GPTs の収益化条件」1件だけ
   （`help-note.com`・`helpx.adobe.com`・`help.openai.com` が未到達）
4. 📌 深掘りキューに残る `- [!]` 6件は全部 `openai.com`。**再試行の対象外**なので、
   枠を取らせない対策が入った今は放置でよい（行は重複防止の台帳なので消さない）


### （8/29 ぶん）未決の2件は両方決着し、実行まで完了・push済み

**【A】記事型「お金の流れ」**＝オーナー判断「**総論→Xの順**」（推奨どおり）。
**総論を当日公開した**＝`content/tools/money-map-three-routes.md`
（「副業のお金は3つの流れで届く」・図2枚・出典4件を実ブラウザで当日取得・独立レビュー5巡
12→13→10→5→3件で収束）。記事型の定義6点と各論キュー（X→YouTubeの順・到達性の注意つき)は
**キュー副業節の「🆕 記事型『お金の流れ』」が単一ソース**。
- 🔑 取材で判明＝YouTubeは原資を「広告掲載から得られる収益の分配」と明記・**Xは原資を書かない**
  （計算方法だけ公表）。この非対称は各論Xの主役候補
- ⚠️ help.x.com は**実ブラウザ200・urllib（check_numbers.py）はHTTPError**＝bot判定は経路依存。
  各論Xを自動ルーティンに書かせるなら、先にクラウド側の到達実測（キュー該当項目に注記済み）

**【B】タイトル3点セット**＝オーナー判断「**3点とも実行**」。全部実施済み:
1. 規則追加＝キュー「タイトルの付け方」に5番（固有名詞または数字必須）・6番（`——`以降は
   未読者に通じる言葉だけ）を追記。誇張の線（渡すものは強く言う／渡さないものだけ約束しない）も明文化
2. 機械検査＝`validate.py` に `TITLE_CONCRETE_ERA`（2026-08-29〜・recipes/tools のみ・
   数字は漢数字まで広く・「AI」は固有名詞に数えない）。テスト9本追加＝603 passed
3. **既存12本の題を書き直した**（URL不変・アンカーテキスト5箇所と証拠ファイル見出し1箇所も追随）:
   承認済み5本（three-samples／split-into-stages／date-decides／weekly-rate／deadline-count）＋
   内輪語度の高い7本（tilt-direction／queue-done-marks／memo-that-names／reply-template／
   can-i-take-this-job／mark-the-ones-not-read／monthly-check。数字は各記事のdescriptionの実測値から）

### ⏭ 次の一手（8/29 時点。上の8/31版が新しい）

1. ✅ **各論Xは 2026-08-29 に公開済み**＝`recipes/x-pay-before-you-earn`。
   実測24回（版a〜g 各3回＋破棄した版c 3回）・独立レビュー4巡（14→27→13→11件）。
   → **次は各論YouTube**（キュー「記事型『お金の流れ』」の `- [ ]` 先頭）。
   `support.google.com` は check_numbers.py からも200なので、Xと違って**機械照合が効く**。
   🔁 **2026-08-30 にオーナーが環境の許可リストへ `support.google.com` と `help.x.com` を追加した。**
   前日（8/29 夜）の実測ではどちらも `CONNECT tunnel failed`（＝経路の遮断）でクラウドからは取材できず、
   各論Xが書けたのは手動セッションで実ブラウザを使ったから。
   🚨 **効いたかどうかは、必ずクラウド側で測ること**（手元のPCの結果は移らない）。
   次にクラウド実行する担当は `curl -sS -o /dev/null -w "%{http_code}" https://support.google.com` を
   実測し、**結果をキューの行に書き足してから**進めること。⚠️ 経路が通ることと、
   `check_numbers.py` の機械照合が効くことは別（X は HTTPError のままかもしれない）
2. 研究担当（15:30）の既定タスクは従来どおり＝昇格項目「画像生成・note・GPTs・YouTube収益化条件」の
   原文確認（総論の取材で YouTube/KDP/CW の3件は当日スナップショットあり＝`_earn_research.md` 8/29節）
3. 📌 図の厳密計測で**既存図に20件**の軽微なはみ出し・画面外が出た（今回触った図は0件）。
   環境差の可能性もあるので、次に図を触るついでに実物確認を（一覧はこのセッションの計測ログ・
   再実行すれば同じものが出る）

---

### 🆕 進行中（2026-08-27・手動ライター作業。自動ルーティンとは別系統）

**earn場面のキュー（`content/_recipe_queue.md`）を1本ずつ、質が上がるまで書き直しては次へ進める作業を実行中。**
（オーナー指示：並行ではなく順番に、1本が高品質になったら次へ）

- ✅ **1本目公開済み**：`content/recipes/kindle-royalty-formula.md`（連載4/4・11回の独立レビュー後に公開・`1502dac`）
- ✅ **2本目も公開済み**：`content/recipes/job-ai-policy-check.md`（案件のAI利用ルールを確かめる／
  実測39回。独立レビュー2回を経て公開。詳細は `content/_writer_log.md` の2026-08-27節と
  `content/_recipe_queue.md` の該当エントリの「→保管」注記）
  - 独立レビュー1回目＝ココナラ2026-08-22補正の未反映・図2枚目の欠落・引用の逐語性1件・
    相互リンク必須の未実装を検出→修正
  - 独立レビュー2回目＝残りの合成引用「」表記（3箇所）・**証拠ファイル自身の判定コードのバグ2件**
    （確認文検出・出典辞退検出の正規表現の活用形漏れ）を検出→修正し、修正した関数を実際に
    生テキストへ再実行して裏取り済み
  - 🔑 企画仕様から意図的に外した2点（理由はキューの「→保管」注記に記録）：
    ①図2枚目は指定の「規約項目とポリシー対応表」ではなく実測結果（版④は0件しか規約を挙げなかった）
    に合わせて差し替え　②💰金額ブロックは`take-home-from-the-contract-amount`と内容が完全重複するため省略
  - commit・`git pull --rebase`・push まで完了（このセッションの末尾に実施）
- ⚠️ **このメモは古かった訂正済み**＝「3本目・時給の推移」は自動ルーティンが8/27夜のうちに
  既に公開していた（`weekly-rate-crosses-the-line`）。以下は8/28時点の実態
- 🆕 **2026-08-28 オーナー指示2件（反映済み）**：①**AdSenseは制約にしない**（「前提にしたことも無い」。
  CLAUDE.md の古い記述を掃除済み・量産抑制の根拠は検索の質評価に差し替え）
  ②**earn記事の目的＝「AIと協力してどう収入を得るか」の具体例・具体的方法の研究**。
  言葉遊びのような複雑な文章にしない（キュー副業節🎯・`_earn_research.md`・CLAUDE.md 書き方に反映済み）
- 🆕 **上の②を受けてキューを差し替え済み（2026-08-28）**＝定説実測型4件（30案・見積もりの根拠・
  直させた後・材料の抜けを・納品したのに入金の無い）を後回しに、「画像生成・note・GPTs・YouTube
  収益化条件」を先頭へ昇格（研究投資は保持・削除なし・番人の床は割っていない）。
  **次＝研究担当（15:30）が昇格項目の原文確認、または「X/YouTube/Instagramで稼ぐ」次シリーズの
  準備（到達表→需要語裏取り）に着手 → 書き手（21:00）がパックを受けて書く**（詳細はキュー副業節冒頭）
- 🆕 **書く手順＝`content/_writer_sop.md` に従う**（2026-08-27 設計承認・2会話分割＋並列実測）
- 別件：`note-fee-before-you-sell.md`のdescription回数修正（`task_9b8da380`）はオーナーが別セッションで実行中・本件とは無関係

### ⓪ いま動いているもの（2026-08-25 19:33 実測）

| | |
|---|---|
| 記事 | レシピ **86本**（8/22時点79本から+7）／ 深掘り（tools）**8本** |
| 検査 | `pytest` **555 passed** ／ `src.build` **116ファイル**（同+11） ／ 週次 **問題なし（緑・8/24分で赤5件も解消）** |
| 自動 | 7本（担当6＋機械1。時刻表は変更なし・CLAUDE.md/旧引き継ぎ参照） |

⚠️ **前回セッションからローカルが71コミット遅れていただけ**（担当は3日間止まらず動いていた）。`git pull --ff-only` で追随済み。未コミットの `docs/superpowers/specs/2026-08-14-income-engine-design.md` は保留のまま維持（オーナー判断待ち・下記②-4）。

**行数予算**（超えたら週次が赤くなる＝回転が止まったシグナル）:

| ファイル | いま | 予算 | 8/22比 |
|---|---|---|---|
| `_recipe_queue.md` | 2,082 | 2,500 ⚠️ 余裕少ない | +42 |
| `_topic_ideas.md` | 1,127 | 1,500 | −191（回転が効いた） |
| `_earn_research.md` | **1,048** | 1,200 🆕⚠️ **急接近** | **+317** |
| `_review_log.md` | 600 | 800 | +7 |
| `_deepdive_queue.md` | 296 | 600 | −88（回転が効いた） |

🆕 **`_earn_research.md` が3日で731→1,048に急増**（予算の87%）。`rotate.yml` は毎日04:43に動いているが、
稼ぎ方研究担当が同じ晩に複数件（研究パック＋追記＋見送り）を書く日があり、回転の消化が追いついていない。
次回赤くなる前に、この節の「済んだ扱いにできる行」の基準を稼ぎ方研究担当かオーナーが一度見ること。

### ① 8/22夜〜8/25に入った大きな変更

**A. 🔴最優先だった「実測用セッションが立てられない」問題が解決した**（8/24 2回目・オーナー指示で再試行）

`create_session` は `scheduled_trigger` 起点のセッションでは今も同じ `permission_mode` エラーで使えない
（サーバー側は未修正）。だが**オーナーが明示的に「進めて」「もう一度試す」と指示した**ため、
**Agentツール（同一セッション内の新規サブエージェント）で条1（独立回答）を満たせる**ことが実証された
（18回＋念押し3回・台帳★91に記録）。⚠️ 同一セッション内なので、**サブエージェントに渡すのは依頼＋材料だけ**
（仕込みの答え・真値は渡さない）という運用も同時に確立済み。
🔑 **次に同じ壁に当たったら、この手順（Agentツール・材料だけ渡す）をそのまま使えばよい。**新しいレシピが
3本（返事待ち・工程分け・出来ばえ監視）続けて公開できた。

**B. earn族の重複は「打ち止めにしない」で決着**（8/22 オーナー判断・`content/_recipe_queue.md` に明記済み）

**C. 週次の赤5件が解消**（8/24朝・稼ぎ方研究担当）＝同一ドメインの前後測定で遮断の切り分けが裏付いた。
残り1件は直る見込みが無い宿題として打ち切り、オーナー案件に回した（詳細は`_earn_research.md`）。

**D. 記事7本を公開**＝返事待ち相手を毎朝出す／毎朝の仕事を工程分け／毎朝の出来ばえを3件監視／
受注日から納期を逆算／積んだメモの矛盾を日付で解決／よく返す返信を型にする／深掘り1本
（check-instructions-for-filler-source）。

**E. 稼ぎ方研究は8/23〜25と3日連続0件採用**（副業節の源①は汲み尽くしたまま）。題材枯渇の兆候。

### ② オーナーの判断が要るもの（急ぎ順・8/22から未処理ぶんを維持＋新規）

1. ✅ **8/24の確認レビューの🚩7件を全件修正済み**（詳細＝`_review_log.md` 8/24節末尾の✅追記×2）＝
   `deadline-count`（「4回とも別々」→「3通り・2件は区別不可」、図も修正）／`three-samples`
   （「20回」→「16回」、図から⑥の誤ったバー値を削除）／`split-into-stages`×2（「2回とも0件」→
   「0件と1件」／「申告と一致」の対象を明確化）／`reply-template`（n=1の明記）／
   兄弟記事2組（automate・earn）に相互リンク追加／**タイトル5本**（オーナー確認済み・ダッシュ以降を
   落として13〜16字に短縮）。`pytest`555・`build`116・マーカー上限内を確認済み。
2. 🟢 **キューの下位5節17件のうち14件（詐欺・セキュリティを除く全部）を②で解消**
   （定説3件・補充08-13床割れ6件・もとから5件を、自動化節の手前へ位置ごと移設済み。
   中身・場面タグは無変更・詳細は`_recipe_queue.md`の移設先／`_topic_ideas.md`8/22節に注記）。
   ⚠️ **詐欺(1)・セキュリティ(2)だけ対象外のまま**＝詐欺は2/4件が公開済みで到達実績あり、
   セキュリティ2件は出典3社がEGRESS_BLOCKEDなので位置を変えても`- [!]`になる公算大（要相談）
3. 🟢🆕 **稼ぎ方研究3日連続0件の原因を特定**＝この担当が`EGRESS_BLOCKED`と報告するドメイン
   （`bizseek.jp`・`shufti.jp`・`zenn.dev`・`note.com`）を別環境（許可リスト制約なし）から
   実測すると**全部200で生きている**。担当の実行環境の許可リストが原因の可能性が高い
   （`sagooworks.jp`だけはDNS解決自体が失敗＝ドメイン不在）。詳細は`_earn_research.md`8/25追記。
   **オーナーへ＝この担当の環境設定（Network access→Custom）にこの4ドメインを追加できるか確認を**
4. ⏸️ **収入エンジン設計書の修正、コミット可否は保留のまま**（オーナー確認待ち）＝
   中身は直し終わっている・pullでも衝突なし。前回「保留」との指示だったため、今回もコミットしていない
5. **教訓台帳の昇格先が無い**（構造問題・変化なし）＝★1系（禁止と受け皿）が最優先候補のまま。
   `tools/check_freshness.py` の `lesson_promotions()` が週次で候補を数えている
6. **GA4プロパティ作成**（測定IDをもらえば5分で配線）／**AdSense申請ボタン**（変化なし）
7. ✅ **リモートの`claude/*`ブランチ6本を削除済み**（全部main にfast-forward済み・
   `review-2026-08-19`は`48f1f17`で手動回収済みだったことを確認のうえ削除）

### ③ 今日これから見るもの

**21:00 レシピ**（①-Aの手順が使えるはず）／**22:40 確認**（8/25公開分＝深掘り1本の読み直し）。

---

## 📚 関連ドキュメント

- **CLAUDE.md** — サイトの構成・自動化の設計・運用ルール（評価不要な参照情報。読むのは基本セッション開始時だけ）
- **SESSION_ARCHIVE.md** — 過去の日次ログ

このファイルは「今どうなっているか・次に何をするか」だけを持つ（2026-08-15 スリム化・2026-08-22 再スリム化）。
