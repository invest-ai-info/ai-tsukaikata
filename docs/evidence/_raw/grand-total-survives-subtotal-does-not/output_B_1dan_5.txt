C13セルに以下を入力し、C13:C15にコピーしてください。

```
=SUMIF($A$2:$A$12,A13,$C$2:$C$12)
```

これで、集計欄（13〜15行目）自体は集計範囲（2〜12行目）に含まれないため、行が増減しても数式を直す必要はありません。データ行を追加する際は2〜12行目の範囲内（例えば12行目の上）に挿入すれば、Excelが自動でSUMIFの範囲を拡張してくれます。
