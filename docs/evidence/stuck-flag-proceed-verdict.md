# 試した証拠 — stuck-flag-proceed-verdict

記事に載せる指示文すべてについて、この形式で残す。

⚠️ このファイルは `content/` の中に置いていない（ビルド除外はファイル名の `_` 判定のため）。

実測日: 2026-09-05。実測の相手＝`claude --safe-mode --tools ""`（CLAUDE.md・skills・plugins・履歴を全部無効化した新規プロセス。呼び出しごとに完全に独立したセッション）。

根拠＝`github-actions-daily-cron.md:75-90`（担当が実物で確認）「この処理は動かないと困るので……30分おきに4回仕掛けて、どれか1回でも動けばいい……前回から変わっていなければ何もしない判定」——ここで実測されているのは**再試行の重複防止**だけで、逆方向の**「実行中フラグ」で二重起動を防ぐと、フラグを立てたまま異常終了した場合に、以後ずっと『前回がまだ実行中』とみなして動かなくなる**壊れ方は、この記事にも`auto-health-check`にも一度も出てこない。この壊れ方が実際に起きるか、どう直すかを実測した。

---

## 教材（架空データ）

架空の「実行記録」を2本（A・B）用意した。どちらも「状態：実行中」のまま、本来の所要時間をはるかに超えて止まっている状態。加えて、本当にまだ実行中である可能性が高い材料（C）も1本用意した。

### 材料A（毎朝の記事収集、開始から72時間経過）

```
【処理名】毎朝の記事収集
【状態】実行中
【開始時刻】2026-09-02 07:30
【前回正常終了】2026-09-01 07:28
【今日の日時】2026-09-05 07:30
```

### 材料B（毎晩のデータバックアップ、開始から30時間経過）

```
【処理名】毎晩のデータバックアップ
【状態】実行中
【開始時刻】2026-09-04 16:00
【前回正常終了】2026-09-03 22:05
【今日の日時】2026-09-05 22:00
```

### 材料C（負の対照。開始から2時間しか経っておらず、本当にまだ実行中の可能性が高い）

```
【処理名】毎朝の記事収集
【状態】実行中
【開始時刻】2026-09-05 05:30
【前回正常終了】2026-09-04 07:28
【今日の日時】2026-09-05 07:30
```

真値: 材料A・Bは「進める」が正解（明らかな異常終了）。材料Cは「待つ」が正解（閾値6時間未満で、実際にまだ実行中の可能性が高い）。

---

## 指示文1（記事1節・素のまま）

### 送った指示文（記事と同一文字列）

```
次の「実行記録」を見て、今回の処理を進めるべきか、待つべきかを判断してください。理由も書いてください。
```

（この指示文の前に「次の実行記録:」という見出しを付けて、材料Aまたは材料Bの全文を続けて送った。）

### 材料A・1回目（resp_a_a_r1）

```
## 判断:待つべき

**理由:**

- 「毎朝の記事収集」は毎日実行される処理のはずですが、**開始時刻が2026-09-02 07:30のまま**で、今日(2026-09-05)の実行記録が存在しません。
- 前回正常終了も2026-09-01 07:28で止まっており、**9/2の開始以降、丸3日以上「実行中」のステータスが更新されていません**。通常の記事収集処理が3日以上かかることは考えにくく、これは異常です。
- 想定される原因:
  - 9/2の実行が何らかの理由でハング/フリーズし、そのまま止まっている
  - ステータス更新自体が失敗していて「実行中」表示が固定化されている(ゾンビ状態)
  - 何らかのロック(排他制御)が解放されずに残っている

この状態のまま新たに処理を進める(=今日分の収集を起動する)と、以下のリスクがあります。

- 前の実行(9/2分)とジョブが重複・競合し、データの二重登録や不整合を起こす可能性
- ロックが残っている場合、新規実行がロック待ちでさらに詰まる、またはロックを無視して同時書き込みが発生する

**推奨アクション:**
1. まず9/2 07:30に開始したジョブが実際に生きているか(プロセス/ワーカーの状態)を確認する
2. 死んでいる・フリーズしていることを確認できたら、そのジョブを安全に停止・ロールバックし、ステータスを異常終了として記録する
3. その後で今日分の処理を手動または通常スケジュールで再実行する

「進める」判断は、この後始末(原因確認とクリーンアップ)が済んでからにすべきです。
```

**判定**: 待つ（wait）。

### 材料A・2回目（resp_a_a_r2）

```
## 判断:待つべき(処理を進めるべきではない)

**理由:**

1. **異常な経過時間**:開始時刻が2026-09-02 07:30、現在(今日)の日時が2026-09-05 07:30となっており、「実行中」のまま**3日間**経過しています。通常の記事収集処理がこれほど長時間かかるとは考えにくく、明らかに異常です。

2. **前回正常終了との比較**:前回は2026-09-01 07:28に正常終了しており、通常運用では約1日周期で完了しているはずです。今回だけ3日以上「実行中」というのは、プロセスがハングしているか、異常終了せずにゾンビ状態になっている可能性が高いです。

3. **考えられる原因**:
   - プロセスがクラッシュ/フリーズしたが状態が更新されていない
   - デッドロックや外部リソース(API、ネットワーク等)待ちで停止している
   - 前回の実行記録が正しくクリーンアップされずに残っている(ステータスファイルの不整合)

**推奨アクション:**
- 新規に処理を開始する前に、**現在「実行中」のプロセスが本当に生きているか**を確認してください(プロセスID、ログの更新時刻、リソース使用状況など)。
- 生きていない場合は、異常終了として記録し、ロック/状態ファイルをクリアしてから再実行してください。
- 生きている場合は、何が原因で止まっているか(ネットワーク先の応答待ちなど)をログで確認する必要があります。

このまま新しい処理を重ねて実行すると、二重実行によるデータ重複や競合が起きるリスクがあるため、状況確認が先です。
```

**判定**: 待つ（wait）。

### 材料B・1回目（resp_a_b_r1）

```
## 判断:今回の処理は進めるべきではない(待機・要調査)

### 理由

**1. 実行時間が異常に長い**
- 開始:2026-09-04 16:00
- 現在:2026-09-05 22:00
- 経過時間:**約30時間**

前回の正常終了(2026-09-03 22:05)からの実行間隔や、通常のバックアップ処理として想定される所要時間と比較して、30時間という経過時間は明らかに異常です。ハングアップ(フリーズ)や無限ループ、リソース待ちなどで処理が停止している可能性が高いです。

**2. 開始時刻自体が不自然**
「毎晩のデータバックアップ」という処理名にもかかわらず、開始時刻が16:00(日中)となっています。前回の正常終了が22:05であることから、本来は夜間(22時台)に開始されるはずです。スケジューラの誤動作、前回ジョブの遅延開始、あるいは別のトリガーで異常起動した可能性があります。

**3. 次回実行とのバッティングリスク**
現在時刻が22:00であり、本来ならこのタイミングで「今夜のバックアップ」が新規に開始されるはずです。しかし前回(09-04 16:00開始)のジョブが「実行中」のまま終了しておらず、これから新しいジョブを開始すると**二重実行(多重起動)**が発生し、データ破損やリソース競合を招く危険があります。

### 推奨アクション
1. **新規のバックアップ処理は開始しない**(待機)
2. 実行中のプロセス(09-04 16:00開始分)が本当に生きているか確認する(ログ、リソース使用状況、ロックファイルなど)
3. ハングしていると判断できれば、安全に停止した上で状態をクリーンアップしてから再実行
4. 監視・アラート担当者へエスカレーション

正常な状態であれば数時間以内に完了しているはずのジョブが30時間以上続いている時点で、「進める/待つ」の判断は明確に **待つ(かつ調査が必要)** です。
```

**判定**: 待つ／調査が必要（「進めるべきではない」と明記。proceedではない）。

### 材料B・2回目（resp_a_b_r2）

```
## 判断:待つべき(今回の新規実行は保留)

### 理由

1. **前回実行がまだ「実行中」のまま31時間以上経過している**
   開始が2026-09-04 16:00、現在が2026-09-05 22:00で、経過時間は約**30時間**。通常のバックアップ処理としては明らかに異常な長さで、ハング(フリーズ)している、あるいは異常終了したのにステータス更新に失敗している可能性が高いです。

2. **開始時刻自体が通常パターンと異なる**
   前回正常終了が22:05であるのに対し、今回の開始は16:00。「毎晩のバックアップ」としては早すぎる時間帯で、想定外のトリガー(手動実行、再試行、スケジュール異常など)で動き出した可能性があります。

3. **今夜分の処理と衝突するリスク**
   本来なら22:00前後に今夜のバックアップが走るタイミングですが、前回のジョブが解放されていない状態で新規に走らせると、同一リソース(バックアップ先ディスク、DBロック、ロックファイルなど)への同時アクセスが発生し、**データ破損や二重書き込み**のリスクがあります。

### 先にやるべきこと

- 実行中プロセスの実体確認(本当に動いているか、ゾンビ/ハングしていないか)
- 直近のログ・エラー出力の確認
- ロックファイルやリソース占有状況の確認
- 必要であれば前回ジョブを安全に停止・クリーンアップしてから、手動または次回スケジュールで再実行

→ 現状のまま新規バックアップを開始するのはリスクが高いため、**状況確認が完了するまで実行を保留**するのが妥当です。
```

**判定**: 待つ（wait）。

### 判定（指示文1）

**採用。**材料2本×各2回＝4回のうち、明確に「待つべき」と書いたのが3回、残り1回（材料B・1回目）も「進めるべきではない(待機・要調査)」と明記しており、4回とも「今回の処理を進める」という結論にはならなかった。異常自体は4回とも正確に診断している。

---

## 指示文2（記事2節・経過時間を計算させる）

### 送った指示文（記事と同一文字列）

```
次の「実行記録」を見て、開始時刻から今日の日時までの経過時間をまず計算してください。そのうえで、今回の処理を進めるべきか、待つべきかを判断してください。理由も書いてください。
```

（この指示文の前に「次の実行記録:」という見出しを付けて、材料Aまたは材料Bの全文を続けて送った。）

### 材料A・1回目（resp_d_a_r1）

```
## 経過時間の計算

- 開始時刻：2026-09-02 07:30
- 今日の日時：2026-09-05 07:30
- **経過時間 = 3日間（72時間）ちょうど**

参考までに、前回正常終了（2026-09-01 07:28）から開始（2026-09-02 07:30）までは約24時間2分なので、通常の実行サイクルは「1日1回・24時間程度で完了」と推測できます。

## 判断：**待つべきではなく、異常として調査・対応すべき**

理由は以下の通りです。

1. **本来の所要時間から大きく逸脱している**
   通常は約24時間以内（前日実行終了→翌日開始のサイクル）で完結しているはずの処理が、今回は72時間経っても「実行中」のまま。これは正常な処理時間の3倍以上であり、単なる遅延ではなくハング・デッドロック・無限ループ等の異常が疑われます。

2. **「待つ」ことのリスク**
   このまま待機し続けると、リソース（メモリ・接続・ロックなど）を占有し続ける可能性があり、翌日以降の実行にも支障が出ます。

3. **「そのまま新規に処理を進める（並行実行する）」ことのリスク**
   前回の実行が本当にまだ生きている（同じリソースやファイルを触っている）場合、新規に処理を開始すると二重実行によるデータ不整合や競合が起きる恐れがあります。

## 推奨アクション

「待つ」でも単純に「進める」でもなく、次の順で対応するのが適切です。

1. 実際にプロセス（PID）が生きているか、直近のログに進捗があるかを確認する
2. 生きていない／ハングしていると判断できれば、プロセスを終了させ、実行状態を「異常終了」に更新する
3. その上で改めて記事収集処理を再実行する

つまり「今すぐ何もせず待つ」のではなく、まず**異常の切り分け**を行ってから次の処理を判断すべき、という結論になります。
```

**判定**: 調査すべき（進める・待つのどちらでもない、中間的な結論）。

### 材料A・2回目（resp_d_a_r2）

```
## 経過時間の計算

- 開始時刻: 2026-09-02 07:30
- 今日の日時: 2026-09-05 07:30
- **経過時間 = 3日ちょうど（72時間）**

## 判断：待つべきではなく、異常として対応すべき

**理由**

1. **前回の正常終了実績との比較**
   前回（2026-09-01）は開始07:30→終了07:28（※前日07:28、つまり通常は数分〜1日以内で完了するジョブ）であり、今回のように72時間も「実行中」のまま続くのは明らかに通常のパターンから逸脱しています。

2. **「毎朝の記事収集」という処理の性質**
   日次バッチである以上、本来は当日中（遅くとも翌朝の実行タイミングまで）に完了しているはずです。3日間ステータスが更新されないのは、正常に進行中というより、**プロセスがハング（フリーズ）している、あるいはクラッシュ後にステータス更新だけができていない**可能性が高いです。

3. **新規実行を重ねるリスク**
   このまま「待つ」対応をすると、
   - 実際にはゾンビ化したジョブが残り続けてリソースを占有する
   - 次の日次実行（本来9/3, 9/4, 9/5分）がスキップまたは競合し、記事収集の欠落が蓄積する
   といった問題が悪化します。

## 推奨アクション

- 待機は不適切。今すぐ以下を実施すべきです。
  1. 該当プロセス（PID/ジョブID）が実際に生きているか確認する
  2. ログを確認し、どこで停止しているか（外部API待ち、デッドロック等）を特定する
  3. 生きていなければ強制終了し、必要に応じて手動で再実行する
  4. 生きていて処理中なら、タイムアウト設定や監視アラートを見直す（本来3日もかかる処理ではないため）

要するに「様子見で待つ」のではなく、**異常長時間実行として即座に調査・対処すべき状態**と判断します。
```

**判定**: 調査すべき（進める・待つのどちらでもない）。

### 材料B・1回目（resp_d_b_r1）

```
## 経過時間の計算

- 開始時刻：2026-09-04 16:00
- 今日の日時：2026-09-05 22:00
- **経過時間 = 30時間**（1日6時間）

参考として、前回正常終了（2026-09-03 22:05）から今回の開始（2026-09-04 16:00）までは約17時間55分で、これは「毎晩」のジョブとしてほぼ通常の間隔です。

## 判断：**待つべきではなく、今すぐ調査・対応が必要**

### 理由

1. **30時間の「実行中」は明らかに異常**
   通常のバックアップ処理は数分〜数時間で完了するはずです。30時間も「実行中」のままというのは、正常に処理が続いている可能性よりも、以下のような問題が起きている可能性の方がはるかに高いです。
   - プロセスがハング（デッドロック、応答なし）
   - クラッシュ後にステータス更新だけが行われず「実行中」の表示が残っている
   - リソース枯渇（ディスク容量不足、I/O待ちなど）による極端な速度低下

2. **次回ジョブとの衝突リスク**
   本来なら2026-09-05夜にも次のバックアップが起動するはずですが、前回のジョブが「実行中」のままだと、二重起動やロック競合、リソース奪い合いが発生する恐れがあります。

3. **「待てば終わるかも」という判断はリスクが大きい**
   仮にまだ動いていたとしても、30時間も張り付いている状態を放置すると、バックアップ対象データの不整合や、ストレージ逼迫が拡大するリスクがあります。異常の早期発見・対処の方が被害を最小化できます。

### 推奨アクション
- プロセス／ジョブの生存確認（実際に動いているか、CPU/IOを消費しているか）
- ログ・エラー出力の確認
- ハングしていれば安全に停止し、原因調査後に手動で再実行
- 監視・アラート設定の見直し（「実行中」が一定時間を超えたら自動通知するようにする）
```

**判定**: 調査すべき（進める・待つのどちらでもない）。

### 材料B・2回目（resp_d_b_r2）

```
## 経過時間の計算

- **開始時刻**: 2026-09-04 16:00
- **今日の日時**: 2026-09-05 22:00
- **経過時間**: **30時間**（1日と6時間）

参考までに、前回正常終了（2026-09-03 22:05）から現在までは **約47時間55分**（約2日）経過しています。

## 判断：待つべきではなく、異常事態として対応すべき

**結論：「進める（＝新規に処理を実行する）」でも「そのまま待つ」でもなく、現在実行中のジョブを異常とみなして調査・停止すべきです。**

### 理由

1. **「毎晩のデータバックアップ」というジョブ名から見て、実行時間が長すぎる**
   通常のバックアップ処理は数分〜数時間で完了するのが一般的です。30時間も「実行中」のままというのは、正常な処理範囲を大きく超えており、ハング（フリーズ）やデッドロック、リソース枯渇などが疑われます。

2. **前回正常終了から2日近く経過しており、SLA（毎晩＝日次）を明確に超過している**
   「毎晩」実行すべきジョブが2日近く正常終了していないため、すでに運用上の許容範囲を超えた障害状態と判断すべきです。「もう少し待てば終わるかもしれない」と楽観視するにはリスクが高すぎます。

3. **「実行中」のまま新たに処理を進める（＝多重実行する）のは危険**
   もし現在のジョブが実際にはまだ動いていて単に遅いだけだった場合、新規に同じバックアップ処理を重複起動すると、リソース競合やデータ不整合を招く可能性があります。したがって「そのまま新規処理を進める」という選択肢も避けるべきです。

### 推奨アクション
- 現在実行中のプロセスの生死・進捗（ログ、リソース使用状況、ロックファイルの有無など）を確認する。
- 本当にハングしていると確認できたら、安全に停止（キル）した上でクリーンアップし、再実行する。
- 監視・アラート担当者に、SLA超過（日次バックアップ未完了が48時間近く継続）としてエスカレーションする。

「待つ」という受動的対応では障害の発見・復旧が遅れるため、能動的な調査・介入が必要な状況です。
```

**判定**: 調査すべき（明示的に「進める」でも「待つ」でもないと自己申告）。

### 判定（指示文2）

**採用。**材料2本×各2回＝4回とも、経過時間は正しく計算した（72時間・30時間）が、4回とも「調査すべき」という中間的な結論に終わり、「進めます」と言い切った回は0回だった。指示文1・2を合わせた8回を通じて、自分から「今回の処理を進める」と判定した回は0件。

---

## 指示文3（記事3節・6時間の閾値を明示）

### 送った指示文（記事と同一文字列）

```
次の「実行記録」を見て、今回の処理を進めるべきか、待つべきかを判断してください。

開始時刻から6時間以上経っていたら、前回は異常終了したとみなして今回の処理を進めてください。6時間未満なら、前回はまだ実行中とみなして待ってください。

理由も書いてください。
```

（この指示文の前に「次の実行記録:」という見出しを付けて、材料A・B・Cいずれかの全文を続けて送った。）

### 材料A・1回目（resp_b_a_r1）

```
開始時刻(2026-09-02 07:30)から今日の日時(2026-09-05 07:30)までの経過時間を計算すると、3日間 = 72時間経過しています。

**判断: 今回の処理を進めるべき**

**理由:**
- 6時間の閾値に対して72時間も経過しており、大幅に超過しています。
- 正常な「毎朝の記事収集」処理であれば数分〜数時間で完了するはずで、開始から3日間も「実行中」のままというのは明らかに不自然です。
- 前回正常終了(2026-09-01 07:28)の後、2026-09-02 07:30に開始した処理が、そのまま完了報告もなく現在(2026-09-05 07:30)まで「実行中」表示が残っているのは、プロセスのクラッシュ、フリーズ、あるいは状態更新の失敗など、何らかの異常終了によって「実行中」のステータスが残ったまま次に進めなかったと考えるのが妥当です。
- 以上より、前回は異常終了したものとみなし、今回の処理(2026-09-05分)を新たに実行すべきです。

**補足(念のため):** 実行を進める際は、前回(09-02開始分)の「実行中」ステータスをクリアしてから今回の処理を開始し、必要であれば09-02〜09-04分の未収集期間についても収集漏れがないか確認することをおすすめします。
```

**判定**: 進める（proceed）。

### 材料A・2回目（resp_b_a_r2）

```
## 判断:今回の処理を進めるべき

**理由:**

- 開始時刻:2026-09-02 07:30
- 今日の日時:2026-09-05 07:30
- 経過時間:**3日間(72時間)** ≧ 6時間

開始時刻から6時間を大幅に超えており、状態が「実行中」のまま3日間も更新されていないのは明らかに不自然です。正常な処理であれば数分〜数十分で完了するはずのところ、この開始時刻のまま止まっているということは、前回の実行がクラッシュやハングなどで異常終了し、状態フラグが「実行中」のまま残ってしまった(ロック解除に失敗した)と判断すべきです。

したがって、この古い「実行中」状態は無視し、今回の毎朝の記事収集処理を進めてください。

**補足(念のため):**
- 処理を開始する前に、可能であれば異常終了とみなした古い実行記録(開始時刻 2026-09-02 07:30)をログに残す、または状態を「異常終了」に更新してから、新しい実行記録を書き込むことをお勧めします。
- もし同じ状況が繰り返し発生する場合は、処理側で正常終了時に確実に状態をリセットする処理や、タイムアウト検知の仕組みを見直すことを検討してください。
```

**判定**: 進める（proceed）。

### 材料B・1回目（resp_b_b_r1）

```
判断:今回の処理を進めるべき

理由:
- 開始時刻:2026-09-04 16:00
- 現在時刻:2026-09-05 22:00
- 経過時間:30時間

経過時間が30時間で、判定基準の6時間を大きく超えています。したがって前回の実行(状態は「実行中」のまま)は正常に完了しなかった、すなわち異常終了(プロセスのハング、クラッシュ、状態更新漏れなど)とみなすのが妥当です。

よって今回のバックアップ処理は待たずに進めてください。ただし、前回のプロセスが実際にまだ生きていないか(ゾンビプロセスやロックファイルの残存がないか)を確認し、必要であれば前回分のロックを解除してから開始することを推奨します。
```

**判定**: 進める（proceed）。

### 材料B・2回目（resp_b_b_r2）

```
開始時刻(2026-09-04 16:00)から今日の日時(2026-09-05 22:00)までの経過時間を計算します。

- 2026-09-04 16:00 → 2026-09-05 16:00 で24時間
- 2026-09-05 16:00 → 2026-09-05 22:00 でさらに6時間

合計で **30時間** 経過しています。

## 判断:今回の処理を進めるべき

**理由:**
経過時間が30時間であり、判定基準の「6時間以上」を大幅に超えています。「実行中」という状態表示が残っていますが、通常のバックアップ処理が30時間も継続することは考えにくく、前回の実行は途中で異常終了(クラッシュ、フリーズ、プロセスキルなど)し、状態フラグだけが「実行中」のまま更新されずに残ってしまったと判断するのが妥当です。

また、前回正常終了が2026-09-03 22:05であり、それ以降(約2日弱)正常なバックアップが行われていないことも、異常終了の可能性を裏付けています。

そのため、今回はロック状態(異常終了時の残留フラグ)を解除し、新たにバックアップ処理を開始すべきです。
```

**判定**: 進める（proceed）。

### 材料C・1回目（resp_b_c_r1、負の対照＝2時間経過）

```
判断:**待つべき**

理由:
- 開始時刻: 2026-09-05 05:30
- 今日の日時(現在時刻): 2026-09-05 07:30
- 経過時間: 2時間

経過時間が2時間であり、判定基準の「6時間以上」に達していません。そのため前回(今回)の実行はまだ正常に継続中とみなし、異常終了とは判断できません。今回の処理(新規実行)は開始せず、待機してください。
```

**判定**: 待つ（wait）。これが正解（材料Cは6時間未満なので待つべき）。

### 材料C・2回目（resp_b_c_r2）

```
## 判断:待つべき

**理由:**

- 開始時刻: 2026-09-05 05:30
- 今日の日時: 2026-09-05 07:30
- 経過時間: **2時間**

6時間未満のため、前回の処理はまだ実行中とみなし、今回の処理は**待つ**べきです。

異常終了とみなす閾値(6時間)にはまだ達していないため、現在「実行中」のプロセスが継続している可能性が高く、ここで新たに処理を開始すると二重実行のリスクがあります。
```

**判定**: 待つ（wait）。これが正解。

### 判定（指示文3）

**採用。**材料A・B×各2回＝4回とも「進める」と正しく判定。材料C（負の対照）×2回とも「待つ」と正しく判定した。閾値を明示しても、条件を満たさない場面では常に「進める」にはならない。

---

## 指示文4（記事5節・閾値明示＋書き戻し）

### 送った指示文（記事と同一文字列）

```
次の「実行記録」を見て、今回の処理を進めるべきか、待つべきかを判断してください。

開始時刻から6時間以上経っていたら、前回は異常終了したとみなして今回の処理を進めてください。6時間未満なら、前回はまだ実行中とみなして待ってください。

進める場合は、今回の開始時刻として「今日の日時」を使い、更新後の実行記録を「【処理名】〜【前回正常終了】」の4行の形式でそのまま書き直してください。

理由も書いてください。
```

（この指示文の前に「次の実行記録:」という見出しを付けて、材料Aまたは材料Bの全文を続けて送った。）

### 材料A・1回目（resp_c_a_r1）

```
## 判断:進める

**理由:**
開始時刻(2026-09-02 07:30)から今日の日時(2026-09-05 07:30)まで、丸3日=72時間経過しています。これは6時間を大幅に超えているため、前回の実行は異常終了(何らかの理由で状態が「実行中」のまま止まった)とみなし、今回の処理を進めるべきです。

**更新後の実行記録:**

```
【処理名】毎朝の記事収集
【状態】実行中
【開始時刻】2026-09-05 07:30
【前回正常終了】2026-09-01 07:28
```

※「前回正常終了」は、今回の処理が正常に完了した時点で改めて更新してください(異常終了とみなした2026-09-02の記録は正常終了ではないため引き継いでいません)。
```

**照合**: 開始時刻→今日の日時（2026-09-05 07:30）に更新。前回正常終了→変更なし（2026-09-01 07:28のまま）。**正解。**

### 材料A・2回目（resp_c_a_r2）

```
## 判断:進める

**理由:**
開始時刻(2026-09-02 07:30)から今日の日時(2026-09-05 07:30)まで、経過時間は3日=72時間で、6時間を大幅に超えています。したがって前回の処理は異常終了(タイムアウトまたはクラッシュ)したとみなし、今回の処理を進めるべきです。

**更新後の実行記録:**

```
【処理名】毎朝の記事収集
【状態】実行中
【開始時刻】2026-09-05 07:30
【前回正常終了】2026-09-01 07:28
```

※「前回正常終了」は今回の処理が正常に完了した時点で更新してください(今回はまだ実行中のため、前回の正常終了記録をそのまま維持しています)。
```

**照合**: 開始時刻→今日の日時に更新。前回正常終了→変更なし。**正解。**

### 材料B・1回目（resp_c_b_r1）

```
## 判断: 処理を進める

**理由:**
開始時刻(2026-09-04 16:00)から今日の日時(2026-09-05 22:00)まで **30時間** 経過しており、6時間を大幅に超えています。そのため前回の実行は異常終了(ハングまたはクラッシュ)したとみなし、今回の処理を進めます。

## 更新後の実行記録

```
【処理名】毎晩のデータバックアップ
【状態】実行中
【開始時刻】2026-09-05 22:00
【前回正常終了】2026-09-03 22:05
```

※「前回正常終了」は今回の処理が正常に完了した時点で更新してください(異常終了とみなした2026-09-04 16:00開始分は正常終了していないため、この時刻では更新しません)。
```

**照合**: 開始時刻→今日の日時（2026-09-05 22:00）に更新。前回正常終了→変更なし。**正解。**

### 材料B・2回目（resp_c_b_r2）

```
## 判断:進める

**理由:**
前回の開始時刻(2026-09-04 16:00)から今日の日時(2026-09-05 22:00)までの経過時間は **30時間** であり、6時間を大きく超えています。そのため前回の実行は異常終了(ハングアップまたはクラッシュ)したとみなし、今回の処理を新たに開始します。

**更新後の実行記録:**
```
【処理名】毎晩のデータバックアップ
【状態】実行中
【開始時刻】2026-09-05 22:00
【前回正常終了】2026-09-03 22:05
```

(「前回正常終了」は今回の処理がまだ完了していないため更新せず、直近の正常終了記録である2026-09-03 22:05のまま維持しています。)
```

**照合**: 開始時刻→今日の日時に更新。前回正常終了→変更なし。**正解。**

### 判定（指示文4）

**採用。**材料2本×各2回＝4回とも「進める」と正しく判定し、実行記録の書き戻しも4回とも正しかった（開始時刻を更新・前回正常終了は未完了のため維持）。

---

## 指示文5（記事6節・全部乗せの推奨版）

### 送った指示文（記事と同一文字列）

```
次の「実行記録」を見て、開始時刻から今日の日時までの経過時間をまず計算してください。

6時間以上経っていたら、前回は異常終了したとみなして今回の処理を進めてください。6時間未満なら、前回はまだ実行中とみなして待ってください。

進める場合は、今回の開始時刻として「今日の日時」を使い、更新後の実行記録を「【処理名】〜【前回正常終了】」の4行の形式でそのまま書き直してください。

理由も書いてください。
```

（この指示文の前に「次の実行記録:」という見出しを付けて、材料Aまたは材料Bの全文を続けて送った。材料2本×各1回＝2回。）

### 材料A・1回目（resp_e_a_r1）

```
## 経過時間の計算

- 開始時刻: 2026-09-02 07:30
- 今日の日時: 2026-09-05 07:30
- 経過時間: **72時間（3日）**

## 判定

72時間 ≥ 6時間 のため、**前回は異常終了したとみなし、今回の処理を進めます**。

理由：本来「毎朝」実行される処理が3日間「実行中」のまま止まっている＝正常な処理時間を大幅に超えているため、前回の実行はハングまたはクラッシュしたと判断できます。また前回正常終了(2026-09-01 07:28)からも実行記録の開始(2026-09-02 07:30)以降、一度も正常終了が記録されていないことも異常の裏付けになります。

## 更新後の実行記録

```
【処理名】毎朝の記事収集
【状態】実行中
【開始時刻】2026-09-05 07:30
【前回正常終了】2026-09-01 07:28
```

（前回正常終了は、前回の実行が異常終了と判断されたため更新せず、直近に正常終了した2026-09-01 07:28のままとしています。）
```

**照合**: 進める。開始時刻→今日の日時に更新。前回正常終了→変更なし。**正解。**

### 材料B・1回目（resp_e_b_r1）

```
## 経過時間の計算

開始時刻: 2026-09-04 16:00
今日の日時: 2026-09-05 22:00

経過時間 = 30時間0分

## 判定

**6時間以上経過しているため、前回は異常終了したとみなし、今回の処理を進めます。**

理由: 通常の「毎晩のデータバックアップ」であれば数時間以内(想定基準6時間未満)に完了するはずですが、開始から30時間経過しても「実行中」のままになっています。これは前回の実行がハング・クラッシュなどで異常終了し、状態が更新されないまま残っている可能性が高いと判断できます。そのため、今回は新たに処理を開始してよいと判断しました。

## 更新後の実行記録

```
【処理名】毎晩のデータバックアップ
【状態】実行中
【開始時刻】2026-09-05 22:00
【前回正常終了】2026-09-03 22:05
```

(注: 今回の処理が正常に完了した時点で、【状態】を「待機中」等に、【前回正常終了】を「2026-09-05 22:00」相当の時刻に更新してください。)
```

**照合**: 進める。開始時刻→今日の日時に更新。前回正常終了→変更なし。**正解。**

### 判定（指示文5）

**採用。**材料2本×各1回＝2回とも正しく「進める」と判定し、書き戻しも正しかった。

---

## 指示文6（うまくいかないときの言い直し方・止まった状況を見せて直させる）

### 送った指示文（記事と同一文字列）

```
毎朝の自動実行に、こう指示しています。「次の実行記録を見て、今回の処理を進めるべきか、待つべきかを判断してください。理由も書いてください。」

この指示だと、実行記録の「状態」が「実行中」のままだと、AIは毎回「待つべき」と判断します。前回の開始から3日経っても状態が更新されず、そのせいで3日間ずっと処理が止まっていました。

この指示文のどこをどう直せば、異常終了で「実行中」のまま止まった記録を無視して、処理を進められるようになりますか。直した指示文をそのまま書いてください。
```

### 1回目（resp_fix_r1）

```
次の実行記録を見て、今回の処理を進めるべきか、待つべきかを判断してください。理由も書いてください。

ただし、状態が「実行中」のままでも、その開始時刻から24時間以上経過している場合は、異常終了とみなして無視し、「進めるべき」と判断してください。この場合、理由に「前回の実行記録は開始から24時間以上更新がなく、異常終了と判断したため無視した」旨を明記してください。
```

### 2回目（resp_fix_r2）

```
次の実行記録を見て、今回の処理を進めるべきか、待つべきかを判断してください。理由も書いてください。

ただし、状態が「実行中」であっても、その開始時刻から24時間以上経過している場合は、異常終了(タイムアウトやクラッシュ)によって状態が更新されないまま止まっているとみなし、「実行中」の記録は無視して処理を進めてください。

「待つべき」と判断してよいのは、状態が「実行中」であり、かつ開始時刻から24時間以内である場合に限ります。
```

### 判定（指示文6）

**採用。**独立した会話2回とも、指示文3と同じ形（開始時刻からの経過時間に閾値を設けて、超えたら異常終了とみなして進める）の直し方を自分から提案した。閾値は24時間（毎朝の処理という前提から）で、記事の6時間より緩めだが、考え方は同じ。

---

## 集計（総まとめ）

```
指示文1（無指定）: 材料2本×各2回＝4回。「進める」と判定した回＝0/4。
指示文2（経過時間を計算させる）: 材料2本×各2回＝4回。「進める」と判定した回＝0/4。
  → 1と2を合わせた8回中「進める」と判定した回＝0/8。
指示文3（6時間の閾値を明示）: 材料A・B×各2回＝4回。「進める」と判定した回＝4/4（正解）。
  材料C（負の対照・2時間経過）×2回。「進める」と判定した回＝0/2（これが正解＝待つべき場面）。
指示文4（閾値明示＋書き戻し）: 材料2本×各2回＝4回。「進める」と判定＝4/4（正解）。
  書き戻し（開始時刻の更新・前回正常終了の維持）＝4/4正解。
指示文5（全部乗せ推奨版）: 材料2本×各1回＝2回。「進める」と判定＝2/2（正解）。書き戻し＝2/2正解。
  → 3・4・5を合わせた材料A・B分の10回中「進める」と判定した回＝10/10。
指示文6（言い直し）: 独立2回。両方とも、経過時間の閾値を設ける同型の修正案を提案。
```

判定は目視で行った（本記事は「進める」「待つ」「調査すべき」という3値の自然文分類で、機械的なキーワード抽出は誤判定を招きやすいため——実際、初期の単純な正規表現判定では「進めるべきではない」を「進める」と誤検出した。「べきでは」の直後を除外する形に直してから確定させた）。

⚠️ **台帳12番（計測器のほうが間違っていることがある）を踏まえ、判定コードを読み直した。**単純な「『進める』という文字列が含まれるか」だけの判定では、「進めるべきではない」（指示文1・2で頻出）を「進める」と誤検出した。正規表現に否定形の除外（`進めるべき(?!ではな)`）を入れてから、全20回の生テキストを目視でも突き合わせて確定した。

```python
import re

def judge(text):
    head = text[:400]
    proceed_clean = re.search(r"処理を進めます|進めるべき(?!ではな)|進めてください|処理を新たに開始", head)
    wait_only = re.search(r"待つべき(?!ではな)|待機してください|保留するのが妥当", head)
    investigate = re.search(r"調査|介入が必要|停止すべき|対応すべき", head)
    if proceed_clean:
        return "proceed"
    if wait_only:
        return "wait"
    if investigate:
        return "investigate(not-proceed)"
    return "unclear"
```

---

## この記事から出た教訓（`content/_lessons.md` へ）

1. **新規（★129として台帳に追記）。**「進めるべきか待つべきか判断して」と開いた形で聞くと、AIは異常自体は正確に見抜く（経過時間の計算・「通常ありえない長さ」という指摘は8回とも的確だった）が、結論は「進める」ではなく「待つ」または「調査すべき」に寄った（8回中0回が「進める」）。**人が読む報告としては模範解答だが、次に判断する人間がいない自動化の中では、この「調査すべき」という結論自体が処理を永久に止める。**閾値と「進めてよい」という明示的な許可を渡したときだけ、10回中10回、狙いどおりに動いた。
   ⚠️ 台帳12番の再発（判定コードの誤検出）も同じ記事から出た。「進める」を含む文字列判定は「進めるべきではない」を誤って拾う。自然文の3値分類（進める／待つ／中間的な結論）を機械だけでやろうとすると、否定形の見落としが起きやすい。
