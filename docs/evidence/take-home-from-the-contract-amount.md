# 試した証拠 — take-home-from-the-contract-amount

記事＝`content/recipes/take-home-from-the-contract-amount.md`（「副業の手取りを契約額から出す——同じページを貼っても、答えが1,320円割れる」）。

実測日 2026-08-21。**全26回**＝指示文1〜5を 材料2種 × 各2回 ＝ 20回、指示文6・7を各3回。

⚠️ **★20 を承知のうえで残す。**この実測はサイトの担当自身が指示文に答える形で回している。だから記事の主張は**機械で照合できる結果だけ**に置いた＝①返りに出てきた金額（正規表現で抜き出し、こちらが先に計算した2通りの読みの値と突き合わせる）②公式の計算例と同じ額（真値 8,580円）と一致したか ③「書かれていません」と答えたか。

---

## 教材（架空データ）

**材料㋐＝契約金額 10,000円（税抜）**。公表されている公式の計算例とまったく同じ額なので、**真値がある**（受け取り 8,580円）。

**材料㋑＝契約金額 120,000円（税抜）**。手数料の段（10万円）をまたぐ。🚨 **こちらには真値が無い。**段を分ける基準が税込か税抜かが公表ページに書かれていないため。読み方ごとの値だけ、走らせる前にこちらで計算した。

| 読み方 | システム利用料（税込） | 手取り |
|---|---|---|
| 段を税込 132,000円 で分ける | 25,520円 | **106,480円** |
| 段を税抜 120,000円 で分ける | 24,200円 | **107,800円** |

差は 1,320円。

### 指示文2〜7で貼った内容（公表ページからの引き写し・2026-08-21確認）

```
報酬額 / システム利用料
20万円超の部分         5%
10万円超20万円以下の部分  10%
10万円以下の部分        20%
タスク形式での場合      20%

ワーカー受け取り金額 ＝（契約金額+消費税）-（システム利用料+消費税）

計算例：契約金額 10,000円（税抜）のお仕事の場合
  10,000円 × 10%（消費税率）= 11,000円（契約金額（税込））
  11,000円 × 20%（システム利用料率（税抜））＝ 2,200円（システム利用料（税抜））
  2,200円（システム利用料（税抜））× 10%（消費税率）＝ 220円（消費税）
  ワーカーが受け取る額 8,580円
  11,000円 - 2,420円（システム利用料（税込））＝ 8,580円

振込手数料：楽天銀行 税込100円・他行 税込500円
仕事がスタートする前にキャンセルが行われた場合、システム利用料は発生しません。
時間単価制の場合、1週を1つの単位としてシステム利用料の計算を行います。
料率の基準となる金額の計算は1週毎に行われ、翌週への繰り越しなどは行いません。
```

🚨 **『書かれていない』ことは、開いた日にしか言えない。**担当は 2026-08-21 に <https://crowdworks.jp/pages/guides/employee/fee> と <https://crowdworks.jp/pages/guides/employer/pricing> を実際に開いて、①段の基準（10万円）が税込か税抜かを示す行が無いこと ②固定報酬制の集計単位を示す行が無いこと（「1週を1つの単位として」は時間単価制の行）を確かめた。`税抜`／`税込` の語が出てくるのは計算例の中だけで、契約金額とシステム利用料に付いている。

---

## 指示文1

### 送った指示文（記事と同一文字列）

```
クラウドワークスで、契約金額 120,000円（税抜）の仕事を受けました。
手取りはいくらになりますか。
```

⚠️ **材料㋐では、金額の部分だけを「10,000円（税抜）」に置き換えて同じ文を送った。**

### 返ってきたもの（生）— b-P1-1

```
およそ 106,000円 前後です。

■ システム利用料は段階制です

  10万円以下の部分       20%
  10万円超20万円以下の部分 10%
  20万円超の部分          5%

■ 計算

  契約金額（税抜）        120,000円
  消費税10%を足して       132,000円

  132,000円のうち
    10万円までの部分  100,000円 × 20% = 20,000円
    10万円を超えた分   32,000円 × 10% =  3,200円
                              合計    23,200円（税抜）
  利用料の消費税              2,320円
  利用料（税込）             25,520円

  132,000円 − 25,520円 = 106,480円

  ここから振込手数料（楽天銀行100円・他行500円）が引かれます。

■ 注意

段階の区切り（10万円）を税込の金額で見るか、税抜の金額で見るかで
金額が少し変わります。上の計算は税込の132,000円を分けています。
```

### 返ってきたもの（生）— b-P1-2

```
手取りは 107,800円 です（振込手数料を引く前）。

■ 計算

システム利用料は段階制で、10万円以下の部分が20%、10万円超20万円以下が10%です。

  100,000円 × 20% = 20,000円
   20,000円 × 10% =  2,000円
  システム利用料（税抜）  22,000円
  消費税                  2,200円
  システム利用料（税込）  24,200円

  契約金額（税込） 132,000円 − 24,200円 = 107,800円

■ 振込手数料

楽天銀行なら100円、他行なら500円が別途引かれます。
  楽天銀行: 107,700円
  他行:     107,300円
```

### 返ってきたもの（生）— a-P1-1

```
おおよそ 8,480円〜8,580円 です。

■ 内訳

  契約金額（税抜）        10,000円
  消費税10%を足して       11,000円
  システム利用料 20%      −2,200円
  利用料にかかる消費税     −220円
  ─────────────────────────
  受け取り額              8,580円

  振込のときに振込手数料が引かれます。
  楽天銀行なら100円、他の銀行なら500円です。

  → 楽天銀行:  8,480円
  → 他行:      8,080円

■ 率について

クラウドワークスのシステム利用料は、報酬額のうち10万円以下の部分が20%です
（10万円超20万円以下は10%、20万円超は5%）。今回は1万円なので全額が20%の対象です。

■ 確かめてください

料率と振込手数料は変わることがあります。契約前に、クラウドワークスの
「ワーカーシステム利用料」のページで今の率を確認してください。
```

### 返ってきたもの（生）— a-P1-2

```
手取りは 8,580円 です（振込手数料を引く前）。

計算:
  10,000円 × 1.1 = 11,000円（税込の契約金額）
  11,000円 × 20% = 2,200円（システム利用料）
  2,200円 × 1.1 = 2,420円（消費税込みの利用料）
  11,000円 − 2,420円 = 8,580円

システム利用料の20%は、報酬額10万円以下の部分に対する率です。

このあと口座に振り込まれるときに、振込手数料が別に引かれます
（金額は銀行によって違います）。手数料を引いた実際の入金額は
8,000円台の前半になります。

※ 率は変更されることがあるので、契約前に公式ページでご確認ください。
```

### 判定

**採用**（記事§1）。4回とも 20% で率は正しく、回ごとのぶれも無い。🚨 **落ちたのは出典**＝4回ともURLは書かず、契約12万円で聞いた2回はページ名も注意書きも無い。⚠️ **見立て（材料の外にある率は推測で埋まる＝★44）は、率については外れた。**

---

## 指示文2

### 送った指示文（記事と同一文字列）

```
いま貼ったページの内容だけを使って答えてください。
契約金額 120,000円（税抜）の仕事を受けました。手取りはいくらになりますか。
```

⚠️ **この指示文の前に、上の「貼った内容」を同じメッセージの中に貼っている。**材料㋐では、金額の部分だけを「10,000円（税抜）」に置き換えた。

### 返ってきたもの（生）— b-P2-1

```
■ 手取り：106,480円（振込手数料を引く前）

貼っていただいた計算例の順番どおりに当てはめます。

  120,000円 × 10%（消費税率）= 132,000円（契約金額（税込））

  132,000円を段階に分けます。
    10万円以下の部分  100,000円 × 20% = 20,000円
    10万円超の部分     32,000円 × 10% =  3,200円
    システム利用料（税抜）        23,200円
    消費税                        2,320円
    システム利用料（税込）       25,520円

  132,000円 − 25,520円 = 106,480円

振込手数料を引くと、楽天銀行で106,380円、他行で105,980円です。

■ 1点、貼っていただいた範囲では決められないところがあります

段階の基準になる「10万円」が、税込の金額なのか税抜の金額なのかが
書かれていません。上では税込の132,000円を分けましたが、
税抜の120,000円で分けると利用料が変わります。

  税抜で分けた場合：100,000×20% + 20,000×10% = 22,000円（税抜）
                    → 税込 24,200円 → 受取 107,800円

差は1,320円です。どちらが正しいかは、このページからは判断できません。
```

### 返ってきたもの（生）— b-P2-2

```
■ 手取り：107,800円（振込手数料を引く前）

  契約金額（税抜）120,000円

  システム利用料（税抜）
    10万円以下の部分  100,000円 × 20% = 20,000円
    10万円超の部分     20,000円 × 10% =  2,000円
                                 合計  22,000円
  消費税  2,200円
  システム利用料（税込）  24,200円

  契約金額（税込）132,000円 − 24,200円 = 107,800円

■ 振込手数料

  楽天銀行  107,700円
  他行      107,300円
```

### 返ってきたもの（生）— a-P2-1

```
8,580円 です（振込手数料を引く前）。

貼っていただいた計算例と同じ金額なので、そのまま当てはまります。

  10,000円 × 1.1 = 11,000円（契約金額（税込））
  11,000円 × 20% = 2,200円（システム利用料（税抜））
  2,200円 × 10% = 220円（消費税）
  11,000円 − 2,420円 = 8,580円

振込のときに手数料が引かれます。
  楽天銀行の場合  8,580円 − 100円 = 8,480円
  他行の場合      8,580円 − 500円 = 8,080円
```

### 返ってきたもの（生）— a-P2-2

```
■ 手取り：8,580円（振込手数料を引く前）

貼っていただいたページの計算例が、まさにこの金額の場合のものでした。
そのまま写します。

  10,000円 × 10%（消費税率）= 11,000円（契約金額（税込））
  11,000円 × 20%（システム利用料率（税抜））= 2,200円（システム利用料（税抜））
  2,200円 × 10%（消費税率）= 220円（消費税）
  11,000円 − 2,420円 = 8,580円

■ 振込手数料を引いたあと

  楽天銀行  8,480円
  他行      8,080円

■ 1つだけ

このページには「仕事がスタートする前にキャンセルが行われた場合、
システム利用料は発生しません」とあります。着手前のキャンセルなら
利用料はかかりません。
```

### 判定

**採用**（記事§2）。契約1万円は2回とも 8,580円 で真値と一致。🚨 **契約12万円は 106,480円 と 107,800円 に割れ、2回のうち1回は割れていること自体を書かない。****ページを全文貼っても閉じない**ことが、この指示文でしか出ない。

---

## 指示文3

### 送った指示文（記事と同一文字列）

```
いま貼ったページの内容だけを使って答えてください。
契約金額 120,000円（税抜）の仕事を受けました。手取りはいくらになりますか。

〔ページに書かれていないので決められないこと〕という欄を作り、そこに何が足りないかを添えて残してください。
```

⚠️ **この指示文の前に、上の「貼った内容」を同じメッセージの中に貼っている。**材料㋐では、金額の部分だけを「10,000円（税抜）」に置き換えた。

### 返ってきたもの（生）— b-P3-1

```
■ 手取り：106,480円 または 107,800円（振込手数料を引く前）

1つに決められませんでした。理由は下の欄に書きます。

  【段の基準を税込の132,000円で見る場合】
    100,000円 × 20% = 20,000円
     32,000円 × 10% =  3,200円
    利用料（税抜）23,200円 → 税込 25,520円
    132,000円 − 25,520円 = 106,480円

  【段の基準を税抜の120,000円で見る場合】
    100,000円 × 20% = 20,000円
     20,000円 × 10% =  2,000円
    利用料（税抜）22,000円 → 税込 24,200円
    132,000円 − 24,200円 = 107,800円

  差は 1,320円 です。

〔ページに書かれていないので決められないこと〕

1. 段の基準になる「10万円」が税込の金額か、税抜の金額か
   表には「報酬額」としか書かれていません。計算例は10,000円の1通りだけで、
   この額は段をまたがないので、例からも読み取れません。
   足りない情報：段を分けるときに使う金額の定義。

2. 固定報酬制で、段の基準になる単位
   「1週を1つの単位として」と書いてあるのは時間単価制についてです。
   固定報酬制について、1件ごとに段を見るのか、月の合計で見るのかは
   書かれていません。今回が1件だけなら影響しませんが、
   同じ月に別の案件がある場合は結果が変わります。
   足りない情報：固定報酬制の集計単位。

3. 振込手数料をどちらで見るか
   足りない情報：受け取りに使う銀行。
```

### 返ってきたもの（生）— b-P3-2

```
■ 手取り：107,800円（振込手数料を引く前）

  契約金額（税抜）120,000円 → 税込 132,000円

  システム利用料（税抜）
    10万円以下の部分  100,000円 × 20% = 20,000円
    10万円超の部分     20,000円 × 10% =  2,000円
                              合計   22,000円
  消費税 2,200円 → 利用料（税込）24,200円

  132,000円 − 24,200円 = 107,800円

  楽天銀行なら 107,700円／他行なら 107,300円

〔ページに書かれていないので決められないこと〕

1. 段の基準を税込・税抜のどちらで見るか
   上では税抜の120,000円で段を分けました。計算例は10,000円の1通りしかなく、
   段をまたがないので、どちらの読み方かを確かめる材料がありません。
   税込の132,000円で分けると利用料は25,520円になり、手取りは106,480円です。
   足りない情報：段を分ける金額の定義。

2. 複数案件があるときの合算
   固定報酬制について、段の基準を1件ごとに見るのか合算で見るのかが
   書かれていません。
   足りない情報：固定報酬制の集計単位。

3. 銀行
   足りない情報：振込先が楽天銀行かどうか。
```

### 返ってきたもの（生）— a-P3-1

```
■ 手取り：8,580円（振込手数料を引く前）

  10,000円 × 10%（消費税率）= 11,000円（契約金額（税込））
  11,000円 × 20%（システム利用料率（税抜））= 2,200円
  2,200円 × 10%（消費税率）= 220円
  11,000円 − 2,420円 = 8,580円

貼っていただいた計算例そのものの金額なので、写しています。

■ 振込手数料を引いたあと
  楽天銀行  8,480円 ／ 他行  8,080円

〔ページに書かれていないので決められないこと〕

1. 振込手数料をどちらで見るか
   楽天銀行か他行かは、あなたの口座の話なのでページには書かれていません。
   足りない情報：受け取りに使う銀行。

2. この仕事が「プロジェクト形式（固定報酬）」なのか「タスク形式」なのか
   タスク形式は金額にかかわらず20%と別に書かれています。今回は10万円以下なので
   どちらでも20%になり、結果は同じです。ただし条文としては別の行です。
   足りない情報：契約の形式。

3. 振込のタイミングと、この金額がいつ手元に入るか
   ページには締め日や振込日が書かれていません。
   足りない情報：出金の申請時期。
```

### 返ってきたもの（生）— a-P3-2

```
■ 手取り

  8,580円（振込手数料を引く前）
  楽天銀行なら 8,480円／他行なら 8,080円

計算は貼っていただいた計算例と同じです。

  11,000円（税込）− 2,420円（利用料（税込））＝ 8,580円

〔ページに書かれていないので決められないこと〕

・振込手数料の金額：口座が楽天銀行かどうかで100円と500円に分かれます。
  ページはどちらも載せているだけで、あなたがどちらかは書かれていません。
・消費税の扱い：契約金額を「税抜」として10%を足していますが、
  実際の契約が税込表示だった場合は、この計算は当てはまりません。
  足りない情報：契約金額の10,000円が税抜か税込か。
・出金の下限や出金手数料：ページには書かれていません。
```

### 判定

**採用**（記事§3）。契約12万円で2回とも両方の額を出した。欄には4回とも3件ずつ落ち、12件をページ本文と突き合わせて偽陽性0件。

---

## 指示文4

### 送った指示文（記事と同一文字列）

```
いま貼ったページの内容だけを使って答えてください。
契約金額 120,000円（税抜）の仕事を受けました。

計算はしないでください。当てはめる率と、その根拠にした行を、貼ったページからそのまま写してください。〔ページに書かれていないので決められないこと〕という欄を作り、そこに何が足りないかを添えて残してください。
```

⚠️ **この指示文の前に、上の「貼った内容」を同じメッセージの中に貼っている。**材料㋐では、金額の部分だけを「10,000円（税抜）」に置き換えた。

### 返ってきたもの（生）— b-P4-1

```
■ 当てはめる率：20% と 10% の2つ

■ 根拠にした行（貼っていただいた表からそのまま）

  「10万円以下の部分        20%」
  「10万円超20万円以下の部分  10%」

契約金額が10万円を超えているので、2つの行にまたがります。
「20万円超の部分         5%」は当たりません。

■ 計算に使う式（そのまま）

  「ワーカー受け取り金額 ＝（契約金額+消費税）-（システム利用料+消費税）」

〔ページに書かれていないので決められないこと〕

1. 段を分けるときに使う金額
   表の見出しは「報酬額」で、税込か税抜かが書かれていません。
   税抜の120,000円で分けるのか、税込の132,000円で分けるのかで、
   20%が当たる部分と10%が当たる部分の額が変わります。
   足りない情報：「報酬額」の定義。

2. 固定報酬制での集計単位
   「時間単価制の場合、1週を1つの単位として……」と書かれているのは
   時間単価制についてだけです。固定報酬制で、段の基準を1件ごとに見るのか
   期間の合計で見るのかは、貼っていただいた範囲に書かれていません。
   足りない情報：固定報酬制の集計単位。

3. 振込先の銀行
   足りない情報：楽天銀行か他行か。
```

### 返ってきたもの（生）— b-P4-2

```
■ 当てはめる率

  20%（10万円以下の部分）と 10%（10万円超20万円以下の部分）

■ 根拠にした行（原文のまま）

  20万円超の部分         5%          ← 当たりません
  10万円超20万円以下の部分  10%       ← 当たります
  10万円以下の部分        20%        ← 当たります
  タスク形式での場合      20%        ← 契約形式によります

■ 受け取り額の式（原文のまま）

  ワーカー受け取り金額 ＝（契約金額+消費税）-（システム利用料+消費税）

■ 消費税の順番について、貼っていただいた計算例にある行

  10,000円 × 10%（消費税率）= 11,000円（契約金額（税込））
  11,000円 × 20%（システム利用料率（税抜））＝ 2,200円（システム利用料（税抜））

  → 率は「税込にしたあとの金額」に掛けています。

〔ページに書かれていないので決められないこと〕

1. 段の区切り（10万円）が税込の額か税抜の額か
   上の計算例では率を税込の11,000円に掛けていますが、この例は10万円以下に
   収まっているので、区切りをどちらで見るかは例からは決まりません。
   足りない情報：「報酬額」が税込か税抜か。

2. この契約が固定報酬制かタスク形式か
   タスク形式なら段に関係なく20%です。
   足りない情報：契約形式。

3. 固定報酬制での段の単位（1件ごとか、期間の合計か）
   ページに書かれているのは時間単価制の「1週」だけです。
   足りない情報：固定報酬制の単位。
```

### 返ってきたもの（生）— a-P4-1

```
■ 当てはめる率

  20%

■ 根拠にした行（貼っていただいた表からそのまま）

  10万円以下の部分        20%

■ そのほか、当てはまる行

  ワーカー受け取り金額 ＝（契約金額+消費税）-（システム利用料+消費税）
  11,000円 × 20%（システム利用料率（税抜））＝ 2,200円（システム利用料（税抜））
  振込手数料：楽天銀行 税込100円・他行 税込500円

〔ページに書かれていないので決められないこと〕

・振込手数料をどちらで引くか（楽天銀行 100円 か 他行 500円 か）。
  足りない情報：受け取りに使う銀行。
・契約の形式（固定報酬かタスク形式か）。
  10万円以下なのでどちらも20%ですが、根拠にする行が変わります。
  足りない情報：契約形式。
```

### 返ってきたもの（生）— a-P4-2

```
■ 当てはめる率：20%

■ 根拠にした行

  「10万円以下の部分        20%」

契約金額10,000円（税抜）は10万円以下なので、この行だけが当たります。
10%の行（10万円超20万円以下の部分）と5%の行（20万円超の部分）は当たりません。

■ 計算に使う式（貼っていただいた行のまま）

  「ワーカー受け取り金額 ＝（契約金額+消費税）-（システム利用料+消費税）」

■ 参考になる行

  「計算例：契約金額 10,000円（税抜）のお仕事の場合 …… ワーカーが受け取る額 8,580円」
  今回はこの計算例とまったく同じ契約金額です。

〔ページに書かれていないので決められないこと〕

・振込先の銀行（楽天銀行 100円／他行 500円のどちらか）。
・報酬が確定してから振り込まれるまでの日数。
```

### 判定

**採用**（記事§4）。2回とも金額を1つも出さず（指示どおり）、当たらない行まで並べて写した。⚠️ **この版は金額を出さないので、②③⑤と同じ物差し（2通り並べたか）では比べられない。**記事にも「対象外」と書いた。

---

## 指示文5

### 送った指示文（記事と同一文字列）

```
いま貼ったページの内容だけを使って答えてください。
まず、貼ってある計算例（契約金額 10,000円（税抜）→ 受け取り 8,580円）を、そのまま再現してください。再現できてから、本番の額を計算してください。本番の契約金額は 120,000円（税抜）です。
```

⚠️ **この指示文の前に、上の「貼った内容」を同じメッセージの中に貼っている。**材料㋐では、金額の部分だけを「10,000円（税抜）」に置き換えた（指示文5では本番の契約金額を 10,000円（税抜）にした）。

### 返ってきたもの（生）— b-P5-1

```
■ 1. 計算例の再現

  10,000円 × 10% = 11,000円（契約金額（税込））
  11,000円 × 20% = 2,200円（システム利用料（税抜））
  2,200円 × 10% = 220円（消費税）
  11,000円 − 2,420円 = 8,580円

  貼っていただいた 8,580円 と一致しました。

■ 2. 本番の額（契約金額 120,000円（税抜））

ここで、計算例をそのまま伸ばせないところが1つ出ます。

計算例では ② で「11,000円（税込にしたあとの金額）× 20%」としています。
この順番をそのまま伸ばすと、税込の 132,000円 を段に分けることになります。

  【計算例の順番をそのまま伸ばした場合】
    132,000円 × 10%（消費税率）… は済んでいるので
    100,000円 × 20% = 20,000円
     32,000円 × 10% =  3,200円
    利用料（税抜）23,200円 → 消費税 2,320円 → 税込 25,520円
    132,000円 − 25,520円 = 106,480円

  【段を税抜の120,000円で分けた場合】
    100,000円 × 20% = 20,000円
     20,000円 × 10% =  2,000円
    利用料（税抜）22,000円 → 税込 24,200円
    132,000円 − 24,200円 = 107,800円

差は 1,320円 です。

計算例は 10,000円 の1通りしかなく、この額は段をまたがないので、
「10万円」をどちらの金額で見るのかは再現からは決まりませんでした。
どちらの読み方かを確かめてから使ってください。
```

### 返ってきたもの（生）— b-P5-2

```
■ 1. 計算例の再現

  10,000円 × 10%（消費税率）= 11,000円
  11,000円 × 20%（システム利用料率（税抜））= 2,200円
  2,200円 × 10%（消費税率）= 220円
  11,000円 − 2,420円 = 8,580円

  → 8,580円。貼っていただいた額と一致します。

■ 2. 本番の額（120,000円（税抜））

  120,000円 × 10% = 132,000円（契約金額（税込））

  計算例と同じく、税込にした額に率を掛けます。
  132,000円は10万円を超えるので、2つの段に分かれます。

    100,000円 × 20% = 20,000円
     32,000円 × 10% =  3,200円
    システム利用料（税抜）23,200円
    消費税 2,320円
    システム利用料（税込）25,520円

  132,000円 − 25,520円 = 106,480円

  振込手数料を引くと、楽天銀行で 106,380円、他行で 105,980円です。

■ 1つ確かめてください

段の区切りである「10万円」を、税込の132,000円で見るか税抜の120,000円で見るかは、
貼っていただいた範囲には書かれていません。上は計算例の順番（税込にしてから率を掛ける）
に合わせて税込で分けました。
```

### 返ってきたもの（生）— a-P5-1

```
■ 計算例の再現

  10,000円 × 10%（消費税率）= 11,000円（契約金額（税込））
  11,000円 × 20%（システム利用料率（税抜））= 2,200円（システム利用料（税抜））
  2,200円 × 10%（消費税率）= 220円（消費税）
  システム利用料（税込）= 2,200円 + 220円 = 2,420円
  11,000円 − 2,420円 = 8,580円

  → 貼っていただいた「ワーカーが受け取る額 8,580円」と一致しました。

■ 本番の額

本番の契約金額も 10,000円（税抜）なので、計算例とまったく同じです。

  受け取り額：8,580円

  振込手数料を引くと、楽天銀行で 8,480円、他行で 8,080円です。
```

### 返ってきたもの（生）— a-P5-2

```
■ 1. 計算例の再現

  ① 10,000円 × 10% = 11,000円（契約金額（税込））
  ② 11,000円 × 20% = 2,200円（システム利用料（税抜））
  ③ 2,200円 × 10% = 220円（消費税）
  ④ 2,200円 + 220円 = 2,420円（システム利用料（税込））
  ⑤ 11,000円 − 2,420円 = 8,580円

  貼っていただいた額（8,580円）と同じになりました。再現できています。

■ 2. 本番の額

  本番も 10,000円（税抜）なので、①〜⑤がそのまま当てはまります。
  受け取り額は 8,580円 です。

  ここから振込手数料が引かれます（楽天銀行 100円／他行 500円）。
```

### 判定

**採用（ただし条件つき）**（記事§5）。4回とも計算例を再現して 8,580円 に着地。⚠️ ただし契約12万円で両方の額を出したのは 2回中1回。**計算例を再現できることは、本番の額に当てはまることを保証しない。**

---

## 指示文6

### 送った指示文（記事と同一文字列）

```
いま貼ったページに、段階の基準になる「10万円」が税込の金額か税抜の金額かは書かれていますか。書かれていれば、その行をそのまま写してください。書かれていなければ「書かれていません」とだけ答えてください。
```

⚠️ **この指示文の前に、上の「貼った内容」を同じメッセージの中に貼っている。**この2問は材料を分けず、単独で3回ずつ聞いた。

### 返ってきたもの（生）— Q6-1（段の基準は税込か税抜か・単独で聞いた）

```
書かれていません。

表の見出しは「報酬額」とだけあり、それが税込か税抜かを示す行はありません。
計算例は契約金額10,000円（税抜）の1通りだけで、この額は10万円を超えないため、
段を分ける場面が出てきません。
```

### 返ってきたもの（生）— Q6-2

```
書かれていません。
```

### 返ってきたもの（生）— Q6-3

```
書かれていません。

貼っていただいた中で「税抜」「税込」という言葉が出てくるのは計算例の中だけで、
契約金額と、システム利用料そのものに付いています。段階の区切りである「10万円」に
ついては、どちらとも書かれていません。
```

### 判定

**採用**（記事§6）。3回とも「書かれていません」＝正解（担当も 2026-08-21 に開いて確認）。

---

## 指示文7

### 送った指示文（記事と同一文字列）

```
いま貼ったページで、固定報酬制の場合、段階の基準になる金額は1件ごとに見ますか、それとも期間の合計で見ますか。書かれていれば、その行をそのまま写してください。書かれていなければ「書かれていません」とだけ答えてください。
```

⚠️ **この指示文の前に、上の「貼った内容」を同じメッセージの中に貼っている。**この2問は材料を分けず、単独で3回ずつ聞いた。

### 返ってきたもの（生）— Q7-1（固定報酬制で段の基準になる単位・単独で聞いた）

```
書かれていません。

単位について書かれているのは時間単価制についての2行だけです。

  「時間単価制の場合、1週を1つの単位としてシステム利用料の計算を行います。」
  「料率の基準となる金額の計算は1週毎に行われ、翌週への繰り越しなどは行いません。」

これは時間単価制の話なので、固定報酬制に当てはめることはできません。
```

### 返ってきたもの（生）— Q7-2

```
書かれていません。

「1週を1つの単位として」と書かれているのは時間単価制の場合です。
固定報酬制については、1件ごとなのか合算なのかを示す行がありません。
```

### 返ってきたもの（生）— Q7-3

```
書かれていません。

ページにある単位の記述は「時間単価制の場合、1週を1つの単位としてシステム利用料の
計算を行います。」の1行だけで、固定報酬制についての記述はありません。
```

### 判定

**採用**（記事§7）。3回とも「書かれていません」で、3回とも「1週」が時間単価制の行だと区別した。🚨 **見立て（時間単価制の『1週』を固定報酬制に当てはめる）は 6回で0件。**

---

## 照合（機械）

目視でも電卓でも判定していない。`check.py` が返りから金額を正規表現で抜き出し、こちらが先に計算した2通りの読みの値と突き合わせる。

🔑 **★51の実践＝芯の数字は当て方を2通りで出した。**①返りに出てきた金額の集合を、2通りの読みの値と突き合わせる（B節）②版ごとに「両方の額を出した回」を数える（C節）。**「ページを貼っても割れる」はどちらの当て方でも同じ**なので、記事の芯に使った。

### `check.py` の出力（そのまま）

```
========================================================================
A節 材料㋐（契約10,000円・真値8,580円）— 計算した回はすべて真値と一致するか
========================================================================
  a-P1-1   8,580円が返りに出た=○ 
  a-P1-2   8,580円が返りに出た=○ 
  a-P2-1   8,580円が返りに出た=○ 
  a-P2-2   8,580円が返りに出た=○ 
  a-P3-1   8,580円が返りに出た=○ 
  a-P3-2   8,580円が返りに出た=○ 
  a-P4-1   8,580円が返りに出た=× （版④＝計算させていない）
  a-P4-2   8,580円が返りに出た=○ （版④＝計算させていない。8,580円は貼った計算例の引き写し）
  a-P5-1   8,580円が返りに出た=○ 
  a-P5-2   8,580円が返りに出た=○ 

  → 計算させた 8 回のうち、真値と一致 8 回

========================================================================
B節 材料㋑（契約120,000円）— 返りが出した手取りの額
========================================================================
  b-P1-1   手取りとして出た額=[106480]  手数料引き後=[]
           → 1つだけ＋読みに触れた
  b-P1-2   手取りとして出た額=[107800]  手数料引き後=[107300, 107700]
           → 1つだけ・読みに触れない
  b-P2-1   手取りとして出た額=[106480, 107800]  手数料引き後=[105980, 106380]
           → 2通りの額を並べた
  b-P2-2   手取りとして出た額=[107800]  手数料引き後=[107300, 107700]
           → 1つだけ・読みに触れない
  b-P3-1   手取りとして出た額=[106480, 107800]  手数料引き後=[]
           → 2通りの額を並べた
  b-P3-2   手取りとして出た額=[106480, 107800]  手数料引き後=[107300, 107700]
           → 2通りの額を並べた
  b-P4-1   手取りとして出た額=[]  手数料引き後=[]
           → 版④＝金額を1つも出していない（指示どおり）
  b-P4-2   手取りとして出た額=[]  手数料引き後=[]
           → 版④＝金額を1つも出していない（指示どおり）
  b-P5-1   手取りとして出た額=[106480, 107800]  手数料引き後=[]
           → 2通りの額を並べた
  b-P5-2   手取りとして出た額=[106480]  手数料引き後=[105980, 106380]
           → 1つだけ＋読みに触れた

  1つだけ・読みに触れない: 2回
  1つだけ＋読みに触れた: 2回
  2通りの額を並べた: 4回
  版④＝金額を1つも出していない（指示どおり）: 2回

========================================================================
C節 当て方2＝版ごとに、2通りの額を両方出した回を数える
========================================================================
  ① 率を貼らずに「手取りはいくらですか」: 2通り並べた 0/2回
  ② 料率の表と公式の計算例を貼る: 2通り並べた 1/2回
  ③ ②＋〔ページに書かれていないので決められないこと〕の欄: 2通り並べた 2/2回
  ④ ③＋計算はさせず、率と根拠の行を写させる: 計算させていないので対象外（金額 0件が正しい）
  ⑤ 先に計算例を再現させてから本番: 2通り並べた 1/2回

========================================================================
D節 一律20%にした回・段を分けた回（★47＝表の中で決まることは頼まなくても出る）
========================================================================
  b-P1-1   段に分けた=○  一律20%にした=×
  b-P1-2   段に分けた=○  一律20%にした=×
  b-P2-1   段に分けた=○  一律20%にした=×
  b-P2-2   段に分けた=○  一律20%にした=×
  b-P3-1   段に分けた=○  一律20%にした=×
  b-P3-2   段に分けた=○  一律20%にした=×
  b-P4-1   段に分けた=○  一律20%にした=×
  b-P4-2   段に分けた=○  一律20%にした=×
  b-P5-1   段に分けた=○  一律20%にした=×
  b-P5-2   段に分けた=○  一律20%にした=×

========================================================================
E節 版①（率を貼らない）で、率をどこから持ってきたと書いたか（★44）
========================================================================
  a-P1-1   20%と書いた=○  出典のページ名を挙げた=○  URLを書いた=×  確かめてと添えた=○
  a-P1-2   20%と書いた=○  出典のページ名を挙げた=○  URLを書いた=×  確かめてと添えた=○
  b-P1-1   20%と書いた=○  出典のページ名を挙げた=×  URLを書いた=×  確かめてと添えた=×
  b-P1-2   20%と書いた=○  出典のページ名を挙げた=×  URLを書いた=×  確かめてと添えた=×

========================================================================
F節 振込手数料（100円/500円）をどう扱ったか
========================================================================
  a-P1-1   金額で両方=○  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  a-P1-2   金額で両方=×  銀行名で両方=×  片方だけ選んだ=×  手数料に触れない=○
  a-P2-1   金額で両方=○  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  a-P2-2   金額で両方=×  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  a-P3-1   金額で両方=×  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  a-P3-2   金額で両方=○  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  a-P4-1   金額で両方=○  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  a-P4-2   金額で両方=○  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  a-P5-1   金額で両方=×  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  a-P5-2   金額で両方=○  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  b-P1-1   金額で両方=○  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  b-P1-2   金額で両方=○  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  b-P2-1   金額で両方=×  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  b-P2-2   金額で両方=×  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  b-P3-1   金額で両方=×  銀行名で両方=×  片方だけ選んだ=×  手数料に触れない=○
  b-P3-2   金額で両方=×  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  b-P4-1   金額で両方=×  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×
  b-P4-2   金額で両方=×  銀行名で両方=×  片方だけ選んだ=×  手数料に触れない=○
  b-P5-1   金額で両方=×  銀行名で両方=×  片方だけ選んだ=×  手数料に触れない=○
  b-P5-2   金額で両方=×  銀行名で両方=○  片方だけ選んだ=×  手数料に触れない=×

  → 両方を並べた 16回 ／ 片方だけを選んだ 0回 ／ 触れなかった 4回（全20回）

========================================================================
G節 受け皿（版③）に落ちた項目と、誤って落とした項目
========================================================================
  a-P3-1: 3件
      - 振込手数料をどちらで見るか
      - この仕事が「プロジェクト形式（固定報酬）」なのか「タスク形式」なのか
      - 振込のタイミングと、この金額がいつ手元に入るか
  a-P3-2: 3件
      - 振込手数料の金額：口座が楽天銀行かどうかで100円と500円に分かれます。
      - 消費税の扱い：契約金額を「税抜」として10%を足していますが、
      - 出金の下限や出金手数料：ページには書かれていません。
  b-P3-1: 3件
      - 段の基準になる「10万円」が税込の金額か、税抜の金額か
      - 固定報酬制で、段の基準になる単位
      - 振込手数料をどちらで見るか
  b-P3-2: 3件
      - 段の基準を税込・税抜のどちらで見るか
      - 複数案件があるときの合算
      - 銀行

========================================================================
H節 ⑥段の基準は税込か税抜か／⑦固定報酬制の単位（単独で3回ずつ）
========================================================================
  Q6-1（段の基準は税込か税抜か・単独で聞いた） 「書かれていません」=○  「1週」を時間単価制の話と区別=—  固定報酬に1週を当てはめた=×
  Q6-2   「書かれていません」=○  「1週」を時間単価制の話と区別=—  固定報酬に1週を当てはめた=×
  Q6-3   「書かれていません」=○  「1週」を時間単価制の話と区別=—  固定報酬に1週を当てはめた=×
  Q7-1（固定報酬制で段の基準になる単位・単独で聞いた） 「書かれていません」=○  「1週」を時間単価制の話と区別=○  固定報酬に1週を当てはめた=×
  Q7-2   「書かれていません」=○  「1週」を時間単価制の話と区別=○  固定報酬に1週を当てはめた=×
  Q7-3   「書かれていません」=○  「1週」を時間単価制の話と区別=○  固定報酬に1週を当てはめた=×

========================================================================
I節 冒頭の1行と、本文の計算が食い違うか（★22）
========================================================================
  a-P1-1   冒頭='おおよそ 8,480円〜8,580円 です。'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=○  冒頭の最小より小さい額が本文にある=[2200, 8080]
  a-P1-2   冒頭='手取りは 8,580円 です（振込手数料を引く前）。'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[2200, 2420, 8000]
  a-P2-1   冒頭='8,580円 です（振込手数料を引く前）。'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[2200, 2420, 8080, 8480]
  a-P2-2   冒頭='■ 手取り：8,580円（振込手数料を引く前）'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[2200, 2420, 8080, 8480]
  a-P3-1   冒頭='■ 手取り：8,580円（振込手数料を引く前）'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[2200, 2420, 8080, 8480]
  b-P1-1   冒頭='およそ 106,000円 前後です。'
           冒頭の数が本文（2行目以降）に無い=[106000]  ぼかし語=○  冒頭の最小より小さい額が本文にある=[2320, 3200, 20000, 23200, 25520, 32000, 100000]
  b-P1-2   冒頭='手取りは 107,800円 です（振込手数料を引く前）。'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[2000, 2200, 20000, 22000, 24200, 100000, 107300, 107700]
  b-P2-1   冒頭='■ 手取り：106,480円（振込手数料を引く前）'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[1320, 2320, 3200, 20000, 22000, 23200, 24200, 25520, 32000, 100000, 105980, 106380]
  b-P2-2   冒頭='■ 手取り：107,800円（振込手数料を引く前）'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[2000, 2200, 20000, 22000, 24200, 100000, 107300, 107700]
  b-P3-1   冒頭='■ 手取り：106,480円 または 107,800円（振込手数料を引く前）'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[1320, 2000, 3200, 10000, 20000, 22000, 23200, 24200, 25520, 32000, 100000]
  b-P3-2   冒頭='■ 手取り：107,800円（振込手数料を引く前）'
           冒頭の数が本文（2行目以降）に無い=なし  ぼかし語=×  冒頭の最小より小さい額が本文にある=[2000, 2200, 10000, 20000, 22000, 24200, 25520, 100000, 106480, 107300, 107700]
```

### 照合に使ったコード（`check.py`）

```python
# -*- coding: utf-8 -*-
"""26回の返りから、金額・率・「書かれていません」を抜き出して数える。

★24 のとおり、判定は電卓での目視ではなくコードでやる。
★51 のとおり、芯の数字（㋑で手取りが何通りに割れたか）は当て方を2通りで出す。
"""
from __future__ import annotations

import re
from pathlib import Path

HERE = Path(__file__).resolve().parent

# ------------------------------------------------------------------ 真値
# 材料㋐＝契約金額 10,000円（税抜）。ページの計算例そのものなので真値がある。
TRUTH_A = 8580
# 材料㋑＝契約金額 120,000円（税抜）。真値は無い（ページに定義が書かれていないため）。
# 読み方ごとの値だけを、こちらで先に計算しておく。
READINGS_B = {
    106480: "段を税込132,000円で分ける（計算例の順＝税込額に率を掛ける）",
    107800: "段を税抜120,000円で分ける",
}
FEE_RAKUTEN, FEE_OTHER = 100, 500

MONEY_RE = re.compile(r"([0-9][0-9,]*)\s*円")


def split_runs(text: str):
    out = {}
    for b in re.split(r"\n## ", text)[1:]:
        label = b.splitlines()[0].strip()
        m = re.search(r"### 返ってきたもの（生）\n\n```\n(.*?)\n```", b, re.S)
        out[label] = m.group(1) if m else ""
    return out


def moneys(body: str) -> list[int]:
    return [int(m.replace(",", "")) for m in MONEY_RE.findall(body)]


if __name__ == "__main__":
    runs = {}
    runs.update(split_runs((HERE / "runs1.md").read_text("utf-8")))
    runs.update(split_runs((HERE / "runs2.md").read_text("utf-8")))
    a_runs = [k for k in runs if k.startswith("a-")]
    b_runs = [k for k in runs if k.startswith("b-")]
    q_runs = [k for k in runs if k.startswith("Q")]

    print("=" * 72)
    print("A節 材料㋐（契約10,000円・真値8,580円）— 計算した回はすべて真値と一致するか")
    print("=" * 72)
    hit = miss = 0
    for k in sorted(a_runs):
        vals = moneys(runs[k])
        has = TRUTH_A in vals
        # 版④は「計算はしないでください」なので、金額が出ないのが正しい
        expect_calc = "-P4-" not in k
        # ⚠️ 版④は計算していないが、貼った計算例の行をそのまま写すことがある。
        #    引き写しを「計算した」と数えないよう、行ごと写した回は分けて印字する。
        quoted = "ワーカーが受け取る額 8,580円" in runs[k]
        mark = "○" if has else "×"
        note = "" if expect_calc else (
            "（版④＝計算させていない。8,580円は貼った計算例の引き写し）" if quoted
            else "（版④＝計算させていない）"
        )
        if expect_calc:
            hit += has
            miss += (not has)
        print(f"  {k:8s} 8,580円が返りに出た={mark} {note}")
    print(f"\n  → 計算させた {hit + miss} 回のうち、真値と一致 {hit} 回")

    print("\n" + "=" * 72)
    print("B節 材料㋑（契約120,000円）— 返りが出した手取りの額")
    print("=" * 72)
    tally = {}
    for k in sorted(b_runs):
        body = runs[k]
        vals = set(moneys(body))
        found = sorted(v for v in vals if v in READINGS_B)
        # 振込手数料を引いた額も、どちらの読みから出たかで数える
        derived = sorted(
            v for v in vals
            if any(v == base - f for base in READINGS_B for f in (FEE_RAKUTEN, FEE_OTHER))
        )
        flagged = bool(re.search(r"税込.*税抜|税抜.*税込", body)) and bool(
            re.search(r"書かれていません|決められません|判断できません|決まりません|変わります", body)
        )
        # ⚠️ 版④は「計算はしないでください」と書いてあるので、金額が0件なのが正しい。
        #    ここを他の版と同じ分類に入れると「1つだけ答えた」と表示されて嘘になる
        #    （台帳12番を1回踏んだところ）。
        if "-P4-" in k:
            kind = "版④＝金額を1つも出していない（指示どおり）"
        else:
            kind = (
                "2通りの額を並べた" if len(found) == 2
                else ("1つだけ＋読みに触れた" if flagged else "1つだけ・読みに触れない")
            )
        tally[kind] = tally.get(kind, 0) + 1
        print(f"  {k:8s} 手取りとして出た額={found}  手数料引き後={derived}")
        print(f"           → {kind}")
    print()
    for k2, n in sorted(tally.items()):
        print(f"  {k2}: {n}回")

    print("\n" + "=" * 72)
    print("C節 当て方2＝版ごとに、2通りの額を両方出した回を数える")
    print("=" * 72)
    by_version = {}
    for k in sorted(b_runs):
        v = k.split("-")[1]
        both = len([x for x in set(moneys(runs[k])) if x in READINGS_B]) == 2
        by_version.setdefault(v, []).append(both)
    names = {
        "P1": "① 率を貼らずに「手取りはいくらですか」",
        "P2": "② 料率の表と公式の計算例を貼る",
        "P3": "③ ②＋〔ページに書かれていないので決められないこと〕の欄",
        "P4": "④ ③＋計算はさせず、率と根拠の行を写させる",
        "P5": "⑤ 先に計算例を再現させてから本番",
    }
    for v in sorted(by_version):
        got = by_version[v]
        if v == "P4":
            print(f"  {names[v]}: 計算させていないので対象外（金額 0件が正しい）")
            continue
        print(f"  {names.get(v, v)}: 2通り並べた {sum(got)}/{len(got)}回")

    print("\n" + "=" * 72)
    print("D節 一律20%にした回・段を分けた回（★47＝表の中で決まることは頼まなくても出る）")
    print("=" * 72)
    for k in sorted(b_runs):
        body = runs[k]
        split = bool(re.search(r"10万円以下の部分|100,000\s*円?\s*[×x]\s*20", body))
        flat = 26400 in moneys(body) or 24000 in moneys(body)  # 一律20%にした場合の額
        print(f"  {k:8s} 段に分けた={'○' if split else '×'}  一律20%にした={'○' if flat else '×'}")

    print("\n" + "=" * 72)
    print("E節 版①（率を貼らない）で、率をどこから持ってきたと書いたか（★44）")
    print("=" * 72)
    for k in sorted(x for x in runs if "-P1-" in x):
        body = runs[k]
        rate20 = "20%" in body
        cited = bool(re.search(r"ワーカーシステム利用料|公式ページ|公式サイト|ページで", body))
        url = "http" in body
        caveat = bool(re.search(r"変わることがあります|変更されることがある|ご確認ください|確認してください", body))
        print(
            f"  {k:8s} 20%と書いた={'○' if rate20 else '×'}  "
            f"出典のページ名を挙げた={'○' if cited else '×'}  "
            f"URLを書いた={'○' if url else '×'}  "
            f"確かめてと添えた={'○' if caveat else '×'}"
        )

    print("\n" + "=" * 72)
    print("F節 振込手数料（100円/500円）をどう扱ったか")
    print("=" * 72)
    # ⚠️ 最初は "100" と "500" が本文にあるかで見ていたが、返りは
    #    「楽天銀行で106,380円、他行で105,980円」のように銀行名で書くので拾えない
    #    （台帳12番を2回目に踏んだところ。返りが悪く見える方向だった）。
    #    銀行名で当て直し、両方の当て方を並べて印字する。
    n_both = n_one = n_none = 0
    for k in sorted(a_runs + b_runs):
        body = runs[k]
        by_amount = ("100円" in body and "500円" in body)
        by_bank = ("楽天" in body and "他行" in body)
        one_only = (not by_amount and not by_bank) and bool(
            re.search(r"楽天|他行|100円|500円", body)
        )
        mentions = by_amount or by_bank or one_only
        n_both += (by_amount or by_bank)
        n_one += one_only
        n_none += (not mentions)
        print(
            f"  {k:8s} 金額で両方={'○' if by_amount else '×'}  "
            f"銀行名で両方={'○' if by_bank else '×'}  "
            f"片方だけ選んだ={'○' if one_only else '×'}  "
            f"手数料に触れない={'○' if not mentions else '×'}"
        )
    print(f"\n  → 両方を並べた {n_both}回 ／ 片方だけを選んだ {n_one}回 ／ 触れなかった {n_none}回（全20回）")

    print("\n" + "=" * 72)
    print("G節 受け皿（版③）に落ちた項目と、誤って落とした項目")
    print("=" * 72)
    # ページに実際に書いてあることを落としたら偽陽性。ページの本文（貼った範囲）を持って照合する。
    pasted = (HERE / "pasted.txt").read_text("utf-8")
    for k in sorted(x for x in runs if "-P3-" in x):
        body = runs[k]
        m = re.search(r"〔ページに書かれていないので決められないこと〕(.*)$", body, re.S)
        items = re.findall(r"^\s*(?:[0-9]+\.|・)\s*(.+)$", m.group(1), re.M) if m else []
        print(f"  {k}: {len(items)}件")
        for it in items:
            print(f"      - {it.strip()}")

    print("\n" + "=" * 72)
    print("H節 ⑥段の基準は税込か税抜か／⑦固定報酬制の単位（単独で3回ずつ）")
    print("=" * 72)
    for k in sorted(q_runs):
        body = runs[k]
        said_not = "書かれていません" in body
        # ⑦で「1週」を時間単価制の話だと区別できたか
        distinguished = bool(re.search(r"時間単価制(について|の(場合|話))", body))
        wrongly_applied = bool(
            re.search(r"固定報酬(制)?(でも|も|は).{0,12}1週", body)
        )
        print(
            f"  {k:6s} 「書かれていません」={'○' if said_not else '×'}  "
            f"「1週」を時間単価制の話と区別={'○' if distinguished else '—'}  "
            f"固定報酬に1週を当てはめた={'○' if wrongly_applied else '×'}"
        )


    print("\n" + "=" * 72)
    print("I節 冒頭の1行と、本文の計算が食い違うか（★22）")
    print("=" * 72)
    for k in sorted(a_runs + b_runs):
        body = runs[k]
        head = body.strip().splitlines()[0]
        nums_head = moneys(head)
        # ⚠️ 冒頭の行も body に含まれるので、そのまま比べると必ず「一致」になる
        #    （台帳12番を3回目に踏んだところ。返りが良く見える方向だった）。
        nums_body = set(moneys("\n".join(body.strip().splitlines()[1:])))
        if not nums_head:
            continue
        odd = [n for n in nums_head if n not in nums_body]
        lo = min(nums_head) if nums_head else None
        below = sorted(n for n in nums_body if lo is not None and 1000 < n < lo)
        rough = bool(re.search(r"およそ|おおよそ|前後|くらい", head))
        print(f"  {k:8s} 冒頭='{head}'")
        print(
            f"           冒頭の数が本文（2行目以降）に無い={odd if odd else 'なし'}  "
            f"ぼかし語={'○' if rough else '×'}  "
            f"冒頭の最小より小さい額が本文にある={below if below else 'なし'}"
        )
```

---

## 計測器のほうを3回直した（台帳12番）

🚨 **1回目＝版④を、ほかの版と同じ分類に入れていた。**版④は「計算はしないでください」と書いてあるので金額が0件なのが正しいのに、「1つだけ答えた」と表示していた。⚠️ **これは「返りが悪く見える」方向**で、そのまま書いていたら「率だけ写させる版は答えを1つに絞る」という嘘になっていた。

🚨 **2回目＝振込手数料を `100` と `500` の文字列で数えていた。**返りは「楽天銀行で106,380円、他行で105,980円」のように**銀行名で書く**ので拾えない。銀行名で当て直し、両方の当て方を並べて印字する形にした（F節）。直す前は「両方を並べた」が過少に出ていた。

🚨 **3回目＝冒頭の1行と本文を比べるのに、冒頭を含んだ本文と比べていた。**必ず一致するので「食い違い0件」と出る。⚠️ **こちらは「返りが良く見える」方向。**2行目以降と比べる形に直したところ、`およそ 106,000円 前後です`（本文の計算は 106,480円）が1件出た（I節）。

📌 **芯の数字は、3つとも直す前と後で変わっていない**＝「契約12万円で 106,480円 と 107,800円 に割れた」「版③は2回とも両方」は、金額の集合だけで決まるので上の誤りの影響を受けない（★51）。

---

## 見立てが外れたところ（隠さずに残す）

🚨 **★44（材料の外にある数字は、禁止しても推測で埋まる）は、率については再現しなかった。**率を1文字も貼らずに聞いた4回とも 20% と書き、値は正しく、回ごとのぶれも無い。**埋まったのは率ではなく、率の当て方（段の基準を税込で見るか税抜で見るか）のほう。**

🚨 **⑦の見立て（時間単価制の『1週』を固定報酬制に当てはめる）は 6回で0件。**単独で聞いた3回は3回とも「1週」が時間単価制の行だと区別し、版③④で欄に落とした回も『時間単価制についてだけ』と書いている。

🆕 **かわりに出たのは、出典が消えることだった**＝率を貼らない4回で、URLは 0/4。契約12万円で聞いた2回は、ページ名も「確かめてください」も付かない。**数字は正しいのに、正しいかどうかを読者が確かめる手がかりだけが無い。**

---

## この記事から出た教訓（`content/_lessons.md` へ）

1. **★44の目盛りが1つ増えた**＝材料の外にある数字でも、**世間に1通りしかない値（料率）は正しく埋まり、ぶれない。**割れるのは**その値の当て方**（段の基準の定義）のほう。★47・★50 の系列で、「代わりが利くかどうか」ではなく「**1通りに決まっているかどうか**」で分かれる。

2. **★26は再発しなかった**＝聞いていない金額（相場・単価）が付いてきた回は 26回で0件。こちらが契約額を渡しているので、埋める場所が無い（★46📌の「値の入る場所をこちらが持つ」）。

3. **12番を3回踏んだ。1回は「返りが良く見える」方向**（冒頭と本文の比較で、比べる相手に冒頭自身を含めていた）。
