# 試した証拠 — youtube-payout-thresholds

記事＝`content/recipes/youtube-payout-thresholds.md`
（「YouTubeの入金しきい値が分かる——「8,000円」は関所の1つでしかない」）。
[副業のお金は3つの流れで届く](https://ai-tsukaikata.com/tools/money-map-three-routes/)の各論。

実測日 2026-08-30。**実行はのべ21回**＝指示文7種（版A〜G）を各3回、独立に実行。

## 実行のしかた

**実測用の回答は `claude` CLI（`claude -p`）を、リポジトリの外（`mktemp -d` で作った空ディレクトリ）で
1回ずつ新規プロセスとして実行して得た**（この会話の文脈もリポジトリの内容も一切引き継がない）。

- `mcp__Claude_Code_Remote__create_session` を先に4回試したが、今夜も
  `the parent session's permission mode is not yet available` で失敗（★84/★91の再発。
  `permission_mode` を明示指定すると別のエラー〈`exceeds parent session's "default"`〉に変わる
  ことまでは分かったが、動く組み合わせは見つからなかった）。Agentツール（`general-purpose`
  サブエージェント）もこのハーネスには存在しない。
- 代わりに使えたのが `claude -p "<指示文>"` — CLIの非対話モードで、呼び出しごとに完全に新しいプロセスが立つ。
  `--disallowedTools "WebSearch,WebFetch,Bash,Read,Write,Edit,Glob,Grep,Task"` を付け、
  調査ツール・ファイル読み書きを封じた（事前知識だけで答えさせる）。作業ディレクトリは
  `mktemp -d` で毎回新規に作った空ディレクトリ（`--add-dir`）＝このリポジトリの `CLAUDE.md` や
  `content/` を読める場所に置いていない。
- 回答の全文は `docs/evidence/_raw/youtube-payout-thresholds/run01.md`〜`run21.md` に生のまま保存。
  判定コード＝同ディレクトリの `judge.py`（正規表現の文字列照合のみ）、出力＝`judge_output.txt`。

⚠️ **`claude -p` を独立実行に使う方式は、この記事で初めて使った（`--safe-mode` を使った既存解とは別の変種）。**`_recipe_queue.md` に手順として追記した
（次回以降の担当が同じ壁で止まらないように）。

## 真値の出典（2026-08-30 に実際に開いて本文まで確認）

全文のスナップショット＝`docs/evidence/_raw/youtube-payout-thresholds/sources.md`。要点だけ再掲する。

| | |
|---|---|
| 参加条件（数字） | 登録者1,000人+直近12か月の総再生時間4,000時間 **または** 登録者1,000人+直近90日のShorts視聴回数1,000万回（[YPP概要と参加資格](https://support.google.com/youtube/answer/72851?hl=ja)） |
| 参加条件（前提6項目） | 収益化ポリシー遵守／対象国居住／有効なコミュニティガイドライン違反警告なし／2段階認証／上級者向け機能の利用資格／AdSense for YouTube連携（同ページ） |
| 「登録者500人」の下位ティア | **現在のページに記載なし**（2通りの聞き方で確認） |
| 収益分配率 | ウォッチページ広告**55%**／Shortsフィード広告**45%**／メンバーシップ・Super Chat等（コマース）**70%**（すべて[YouTubeパートナー収益の概要](https://support.google.com/youtube/answer/72902?hl=ja)） |
| 支払いサイクル | 確定した前月分は当月7〜12日ごろに残高加算、しきい値到達済みなら当月21〜26日ごろに振込（同ページ） |
| 金額のしきい値（日本円） | 税務情報なし／認証$10相当額／**お支払い方法選択¥1,000**／**お支払い¥8,000**／**キャンセル¥1,000**（[お支払い基準額](https://support.google.com/adsense/answer/1709871?hl=ja)）＝**5行、金額は3種類** |
| 「$10」の位置づけ | [支払いを受け取るまでの流れ](https://support.google.com/adsense/answer/1709858?hl=ja)は「認証」しきい値の説明として$10とだけ書き、「お支払い」しきい値（¥8,000）との違いを明示していない |

---

## 指示文A（素朴に聞く・3回）

### 送った指示文（記事と同一文字列）

```
YouTubeで広告収入を得て稼ぐには、何をどう満たせばいいですか。条件と、実際にお金が入るまでの流れを教えてください。
```

生の全文＝`run01_a1_A_baseline.md`〜`run03_a3_A_baseline.md`。

### 判定（3回中・機械照合）

| 項目 | 回数 |
|---|---|
| 「登録者500人」という、現在のページに無い下位ティアを条件として書いた | **1/3** |
| 「8,000円」を振込の最低額として書いた | 3/3 |

🔑 **1回（run01）だけ、現在の公式ページに無い「登録者500人・過去90日3本投稿・過去365日3,000時間
または過去90日Shorts300万回」という条件を、確認もなく「YPP参加の最低条件」として書いた。**
数字の並び自体（500人・3,000時間・300万回）は現実離れした作り話ではなく、YouTubeが過去に導入した
別の仕組みに近い水準だが、**担当が2通りの聞き方で確認した現在のページには出てこない**。
古い・別の情報が、確認なしで「今の条件」として混ざる典型例。

---

## 指示文B（収益分配率を聞く・3回）

### 送った指示文（記事と同一文字列）

```
YouTubeの広告収益は、クリエイターと運営(YouTube)でどんな割合に分けられますか。
```

生の全文＝`run04_b1_B_split_rate.md`〜`run06_b3_B_split_rate.md`。

### 判定（3回中・機械照合）

| 項目 | 回数 |
|---|---|
| ウォッチページ広告=55% | 3/3 |
| Shorts=**45%**（真値と一致） | **2/3** |
| Shorts=55%（真値と不一致・長尺と同じ比率と誤って書いた） | **1/3** |
| メンバーシップ・Super Chat等=70% | 3/3 |

🔑 **1回（run06）は「2023年2月以降、長尺動画と同じ『55:45』の分配方式に統一されました」と
明記して、Shortsの取り分を55%とした。**公式ページの原文は「45%」（コマース以外の3モジュールで
唯一45%）。統一されたという記述の出典は、担当が確認した4ページのいずれにも無い。
✅ メンバーシップ・Super Chat等の70%は3回とも正確だった。

---

## 指示文C（振込の最低額だけを聞く・3回）

### 送った指示文（記事と同一文字列）

```
YouTubeの収益はいくら貯まったら実際に銀行口座へ振り込まれますか。最低額を教えてください。
```

生の全文＝`run07_c1_C_threshold_amount.md`〜`run09_c3_C_threshold_amount.md`。

### 判定（3回中・機械照合）

| 項目 | 回数 |
|---|---|
| 「8,000円」（真値と一致） | **3/3** |
| 金額のしきい値が複数段階あることに触れた | **0/3** |

🔑 **金額そのもの（8,000円）は3回とも正確。**ただしこの聞き方では、公式ページの表が実際には
「税務情報・認証・お支払い方法選択・お支払い・キャンセル」の**5行**（金額は3種類）あることには
1回も触れられなかった。「振込の最低額」という単数形の聞き方は、単数の答えしか引き出さない。

---

## 指示文D（材料を渡して計算だけさせる・3回）

### 送った指示文（記事と同一文字列）

```
公式ページで確認した情報を貼ります。これ以外の数字は使わないでください。

・YouTubeパートナープログラムの参加条件は、チャンネル登録者1,000人以上で、直近12か月の対象動画の総再生時間が4,000時間以上、または直近90日の対象ショート動画の視聴回数が1,000万回以上
・ウォッチページ広告の収益は、YouTubeが55%をクリエイターに分配する
・確定した前月分の収益は、当月7日から12日ごろにYouTubeの支払いアカウント残高に加算され、支払いのしきい値に達していれば21日から26日ごろに振り込まれる

私はこれからチャンネルを始めます。上の情報だけから、収益化に参加してから実際にお金が振り込まれるまでの流れを、順を追って説明してください。上に無い数字（支払いのしきい値の具体額など）は使わず、必要なら「材料に無い」と書いてください。
```

生の全文＝`run10_d1_D_materials_fed.md`〜`run12_d3_D_materials_fed.md`。

### 判定（3回中・機械照合）

| 項目 | 回数 |
|---|---|
| 「材料に無い」と明記した | 3/3 |
| 材料に無い金額（円・ドル）を勝手に埋めた | 0/3 |

✅ **3回とも、渡していない金額を作らなかった。**ただしrun12（d3）は「達していなければ、翌月以降に
残高が繰り越され、しきい値に達した月の21〜26日ごろに振り込まれる」という**繰り越しの挙動**を
自分から補い、末尾で「繰り越しの扱いの詳細…は材料に無い」と自己申告した。材料を貼っても、
自然な推測を足したうえでそれを自己申告するかどうかには差が出る。

---

## 指示文E（ページ名だけを聞く・3回）

### 送った指示文（記事と同一文字列）

```
YouTubeパートナープログラムについて、参加条件が書かれている公式ページの名前を、条件の中身や数字は書かずに教えてください。
```

生の全文＝`run13_e1_E_page_name_only.md`〜`run15_e3_E_page_name_only.md`。

### 判定（3回中・機械照合）

| 項目 | 回数 |
|---|---|
| 数字・%・円などが1つも出なかった（縛りを守った） | **3/3** |

✅ 3回とも「YouTube パートナー プログラムの概要と参加資格」（表記ゆれあり）とだけ答え、数字は
一切出さなかった。Xの記事では条件の中身が漏れる回があったが、この聞き方・このAIでは3回とも守られた。

---

## 指示文F（確認順だけを聞く・3回）

### 送った指示文（記事と同一文字列）

```
私はYouTubeで収益化を検討しています。条件や金額は変わることがあるので、自分で公式ページを確認したいです。

私が確認すべき箇所を、確認する順番に並べてください。

⚠️ あなたは金額や条件の数字を書かないでください。数字は私が公式ページで確認します。あなたが書くのは「どこで何を確認するか」だけにしてください。
```

生の全文＝`run16_f1_F_confirm_order_only.md`〜`run18_f3_F_confirm_order_only.md`。

### 判定（3回中・機械照合）

| 項目 | 回数 |
|---|---|
| 数字・%・円・ドルなどが1つも出なかった | **3/3** |

✅ 3回とも数字を出さず、確認先として公式ヘルプセンター・AdSense管理画面・YouTube Studioを順に挙げた。

---

## 指示文G（金額のしきい値が何段階あるか聞く・3回）

### 送った指示文（記事と同一文字列）

```
YouTubeの収益は、条件を満たしてから実際に銀行口座へ振り込まれるまでに、金額に関わる基準(しきい値)は何段階ありますか。段階ごとに、何のための基準で、金額はいくらか教えてください。
```

生の全文＝`run19_g1_G_stage_count.md`〜`run21_g3_G_stage_count.md`。

### 判定（3回中・機械照合）

| 項目 | 回数 |
|---|---|
| 「2段階」と答えた（真値は5行・金額は3種類） | **3/3** |
| 「5段階」など真値に近い数を答えた | 0/3 |
| 出典に無い「2023年6月に1,000円から8,000円へ改定」という具体的な沿革を書いた | **1/3** |

🔑 **この記事でいちばん濃い誤答。**3回とも「PIN確認の$10」と「お支払いしきい値の8,000円/$100」の
**2段階だけ**を挙げ、公式の表が実際には持つ「お支払い方法選択（¥1,000）」「キャンセル（¥1,000）」
「税務情報」の行には1回も触れなかった。しかも1回（run19）は確認できていない改定時期
（「2023年6月の改定で1,000円から引き上げられました」）まで、留保なしに書いた。
**「何段階か」と数を聞かれると、AIは自分が知っている主要な2つに絞り込み、表の全行までは開かない。**

---

## この記事から出た教訓（`content/_lessons.md` へ）

1. 🆕 **`claude -p`（CLIの非対話モード）を空ディレクトリで動かすと、`create_session`や
   Agentツールが使えない環境でも独立回答が取れる。**★84/★91（実測用の別セッションを立てられない）の
   別解として `_recipe_queue.md` に追記した。
2. ★84/★91 が再発した（`create_session` は今夜も同じエラー。加えて `permission_mode` を
   明示すると別のエラーに変わることが新たに分かった）。

## 追記（2026-08-30・記事公開後の確認）

`content/_lessons.md` ★91 に、この壁への既存の解決策として `claude --safe-mode --tools "" -p`
（2026-08-24に確立）が既に記録されていたことを、この実測を終えたあとに見つけた。
`--safe-mode` は CLAUDE.md・skills・plugins・hooks・過去の会話を丸ごと無効化するので、
今回使った `--disallowedTools` 方式（作業ディレクトリはリポジトリ外の空ディレクトリだが、
ツール自体は個別に禁止しただけ）より厳密に事前知識ゼロへ近い。次に同じ壁へ当たる担当は
`--safe-mode` を先に試すこと。今回の21回は空ディレクトリで実行しており汚染の実害は
無かったと見ているが、`--safe-mode` ほど確認はできていない。
