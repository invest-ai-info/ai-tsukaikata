# 「みんながAIで何をしたいか」の調査（2026-08-10）

オーナー指示「いまみんながAIを使ってやりたいことを大規模にリサーチして、まとめて保存」。
目的は**レシピの題材を、勘ではなく需要から決めること**。

## 何を測ったのか（先に限界を書く）

🚨 **「みんながやりたいこと」は直接測れない。**取れるのは全部**代理指標**で、それぞれ偏る。

| 使った | 分かること | 偏り |
|---|---|---|
| **Google サジェスト**（今回の主力） | 実際に打たれている語。日本語・現在 | 検索する人だけ。**回数は分からない** |
| Search Console | **このサイトに来た検索語** | ⭐ 最強だが**未登録**（運営者の宿題） |
| Q&Aサイト・Reddit | 詰まっている具体的な場面 | 困った人だけ。成功例は出ない |
| 公的調査（情報通信白書等） | 利用率・属性 | 半年〜1年遅れ。粒度が粗い |

⚠️ **WebSearch はツール側の仕様で US 限定**なので、日本語圏の需要には使えなかった。

🔑 **いちばん重要な限界: サジェストに件数は付いてこない。**下の「語数」は
**言い回しの種類の多さ**であって検索回数ではない。順位の目安にはなるが、
「230語だから213語より需要が大きい」とは言えない。**ノイズも混ざる**
（「ai 翻訳 ひで」「哀 例文」のような無関係な一致が実際にあった）。

## 方法（再現できる形で）

種30語 × 語尾32種 = **930クエリ**を Google サジェストに投げ、**5,104語**を集めた。

```python
import json, time, urllib.parse, urllib.request
def suggest(q):
    url = ("https://suggestqueries.google.com/complete/search?client=firefox&hl=ja&"
           + urllib.parse.urlencode({"q": q}))
    req = urllib.request.Request(url, headers={"User-Agent": "Mozilla/5.0"})
    with urllib.request.urlopen(req, timeout=15) as r:
        return json.loads(r.read().decode("utf-8"))[1]
```

種＝ツール名（ChatGPT/Claude/Gemini/生成AI/AI）× 作業（文章・資料・エクセル・メール・
議事録・要約・翻訳・画像・勉強・家事・家計）× 文脈（仕事・効率化・自動化・副業）。
語尾＝ひらがな28種＋「方法/コツ/例/やり方」。

情報収集・商用・無関係を落として（とは/料金/口コミ/求人/資格/株/危険/規制 など）
**4,239語**を「やりたいこと」として扱い、17テーマに束ねて既存22記事と突き合わせた。

## 結果: 需要があるのに1本も書いていない領域

| テーマ | 語数 | 既存記事 | 待ち行列 |
|---|---|---|---|
| **翻訳させる** | 230 | **ゼロ** | **ゼロ** |
| **勉強・学習に使う** | 213 | **ゼロ** | **ゼロ** |
| **文章を直させる（校正・添削）** | 58 | **ゼロ** | **ゼロ** |
| **資料・スライドを作らせる** | 38 | **ゼロ** | **ゼロ** |
| 要約させる | 265 | あり | — |
| 議事録・文字起こし | 233 | あり | — |
| 頼み方そのもの | 193 | あり | あり |
| 自動化する | 180 | あり | あり |
| 表計算を扱わせる | 135 | あり | あり |

→ **上の4件を待ち行列の先頭に入れた**（`content/_recipe_queue.md`）。

⚠️ **「あり」判定は雑**（記事本文にその語が出るかを見ただけ）。**「ゼロ」のほうは
全文検索で確かめてあるので信用してよいが、「あり」は薄い扱いかもしれない。**

## 判断: 書かなかったもの

- **画像生成（66語）** — 需要はあるが、このサイトの読者像（非エンジニアの会社員が
  仕事を自動化する）から外れる。既存記事もある
- **コードを書かせる（20語）** — 読者定義と正面から矛盾する（「プログラミング不要」）
- **「AIに仕事を奪われるか」系** — 検索は多いが**このサイトが答えるべき問いではない**。
  ノイズとして落とした

## 次にやるなら

1. **Search Console を登録する**（運営者の5分作業）。実データが溜まれば、
   世間一般の推測ではなく**この読者が探している語**で題材を決められる。
   ⭐ **同じ手間で桁違いに良い結果になるので、大規模化より先にこれ**
2. 種を増やして再実行（このファイルのコードをそのまま流す）→ ✅ 8/11 に実施（下記）
3. Q&Aサイト・Reddit で「詰まっている場面」を拾う（サジェストは語であって場面ではない）

## 2026-08-11 再実行（進化ループ輪3の初回）

種を「作業」15語に入れ替えて再実行: 要約／スライド／資料作成／企画書／報告書／
議事録まとめ／献立／家計簿／旅行計画／勉強法／プレゼン／読書／スケジュール／整理／
お礼メール × ツール5 × 語尾16種 = **1,200クエリ・1,430語**（除外後1,268語）。
手順は上のコードの種と語尾を差し替えただけ。

| テーマ | 語数 | 判断 |
|---|---|---|
| スライド 277＋プレゼン 134 | 411 | 既にキュー先頭（需要の裏付けが増えた） |
| 要約 | 227（8/10は265） | ★キューへ（両回とも最大級） |
| 整理（ファイル・フォルダ・写真＝PC内） | 167 | ★キューへ（キューの「片付け」は物理で別物） |
| スケジュール | 115 | 「予定と持ち物」（キュー内）の隣接。見送り |
| 資料作成 87＋企画書 33 | 120 | ★キューへ（⚠️スライドと要切り分け） |
| 献立 | 51 | ★キューへ（暮らし場面の補強） |
| 読書 | 50 | **見送り**＝言い回しの実態は「読書感想文」で宿題の代筆。サイトの信頼と衝突 |
| 報告書 | 38 | キュー内にあり |
| 家計簿 | 31 | 見送り（アプリ比較の言い回しが主で、頼み方の型が薄い） |
| 旅行 | 23 | 「予定と持ち物」の隣接。見送り |
| お礼メール | 3 | 語数不足 |

- ⚠️ **「ゼロ／あり」の機械判定は語面の一致だけ。**プレゼンは「ゼロ」と出たが
  スライド（キュー先頭）と同一需要（プレゼン資料=スライド）。逆に「整理」は本文の
  散文と一致して「あり」に見えたが、PC内のファイル整理の記事は無かった。
  **家族テーマの束ねと最終判断は人間がやること**
- キューへの追加は4件（要約・整理・企画書資料・献立）＋8/10実測から1件（表記ゆれ）＝計5件
